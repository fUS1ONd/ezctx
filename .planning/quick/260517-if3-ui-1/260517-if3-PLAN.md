---
phase: quick-260517-if3-ui-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ui/screens/processing_screen.dart
  - lib/ui/screens/home_screen.dart
  - lib/ui/screens/settings_screen.dart
  - lib/app.dart
  - lib/ui/widgets/primary_button.dart
autonomous: true
requirements: [UI-FIX-01]
---

<objective>
Закрыть все 10 актуальных UI-багов фазы 1 (processing/home/settings/app/primary_button) согласно ручному UI-ревью.

Purpose: Привести экраны к утверждённому дизайну (glass design system, pulse-анимации, токены, fade transitions).
Output: 5 файлов с исправлениями; визуальный обзор всех экранов соответствует спецификации.
</objective>

<execution_context>
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@lib/ui/screens/processing_screen.dart
@lib/ui/screens/home_screen.dart
@lib/ui/screens/settings_screen.dart
@lib/app.dart
@lib/ui/widgets/primary_button.dart
@lib/ui/widgets/glass_card.dart
@lib/ui/widgets/glass_tile.dart
@lib/ui/theme/app_theme.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Processing screen — pulseDot, glass pill control bar, safe area, GlassTile</name>
  <files>lib/ui/screens/processing_screen.dart</files>
  <action>
  Открыть `lib/ui/screens/processing_screen.dart` и внести 4 исправления:

  (1) Pulse-анимация активной точки pipeline. Преобразовать виджет в `StatefulWidget` (или использовать существующий state), добавить `AnimationController` с `duration: Duration(milliseconds: 1200)`, `..repeat(reverse: true)`, и `CurvedAnimation(curve: Curves.easeInOut)`. Активная точка (текущий шаг pipeline) оборачивается в `AnimatedBuilder`/`ScaleTransition` + `FadeTransition` — scale от 1.0 до 1.25, opacity от 0.6 до 1.0. Неактивные точки остаются статичными. Не забыть `controller.dispose()` в `dispose()`. Комментарии на русском.

  (2) Заменить голый `TextButton('Отменить обработку')` на floating glass pill bar. Использовать `GlassCard` с `borderRadius: BorderRadius.all(AppRadius.pill)` (или `AppRadius.pill` если это число — обернуть в `Radius.circular`). Внутри `Row` с двумя элементами: слева — elapsed time (`Text` со стилем `AppTextStyles.mono`, формат `mm:ss`, обновляется через тот же ticker/AnimationController либо отдельный `Timer.periodic`), справа — `TextButton`/`IconButton` отмены с прежним коллбэком. Pill расположен внизу как floating element (см. п.3 для отступа).

  (3) Bottom safe area: заменить статический `SizedBox(height: AppSpacing.lg)` (внизу скролла/Column) на `SizedBox(height: MediaQuery.of(context).padding.bottom + 32)`. Если есть несколько мест с нижним отступом — поправить именно последний перед концом контента.

  (4) Карточка файла (та, что показывает имя/размер/длительность файла): заменить `GlassCard` на `GlassTile`. Если `GlassTile` имеет другой API — адаптировать параметры (radius 30px вместо 22px достигается автоматически через `GlassTile`).

  Все литералы spacing/radius/colors брать из токенов (`AppSpacing`, `AppRadius`, `AppColors`). Запустить анализатор по этому файлу через context-mode после правок.
  </action>
  <verify>
    <automated>flutter analyze lib/ui/screens/processing_screen.dart</automated>
    <human-check>Запустить приложение, открыть processing-экран: (a) активная точка pipeline пульсирует 1.2s, (b) внизу floating glass pill с таймером и кнопкой отмены, (c) карточка файла имеет более скруглённые углы (GlassTile r=30), (d) нет визуального налезания на системный bottom inset.</human-check>
  </verify>
  <done>Pulse работает; pill bar отрисован как GlassCard с pill-радиусом; bottom padding учитывает safe area + 32; карточка файла = GlassTile.</done>
</task>

<task type="auto">
  <name>Task 2: Home upload card — dashed border, "Из файлов" pill, minHeight constraint</name>
  <files>lib/ui/screens/home_screen.dart</files>
  <action>
  Открыть `lib/ui/screens/home_screen.dart`. Изменения внутри upload card (GlassTile с иконкой загрузки):

  (1) Пунктирная рамка вокруг иконки. Создать локальный `class _DashedCirclePainter extends CustomPainter` (или `_DashedBorderPainter` для прямоугольника, если иконка не круглая — определить по текущей разметке) который рисует пунктир `AppColors.accent.withOpacity(0.55)`, ширина штриха ~6px, gap ~4px, толщина 1.5px. Обернуть существующую иконку в `CustomPaint(painter: _DashedCirclePainter(...), child: <icon container>)`. Комментарии на русском.

  (2) Под подсказкой о форматах добавить pill-метку «Из файлов»: `Container` с `padding: EdgeInsets.symmetric(horizontal: AppSpacing.md, vertical: AppSpacing.sm)`, `decoration: BoxDecoration(color: AppColors.accent.withOpacity(0.12), borderRadius: BorderRadius.all(Radius.circular(999)))`, `child: Text('Из файлов', style: AppTextStyles.label.copyWith(color: AppColors.accent))`. Расположить как отдельный child в `Column` карточки после format hint.

  (3) Обернуть `GlassTile` (саму upload card) в `ConstrainedBox(constraints: BoxConstraints(minHeight: 260), child: GlassTile(...))`. Если карточка уже в `SizedBox` фиксированной высоты — заменить на `ConstrainedBox` с minHeight.

  Все цвета/отступы — через токены.
  </action>
  <verify>
    <automated>flutter analyze lib/ui/screens/home_screen.dart</automated>
    <human-check>Открыть home screen: (a) вокруг иконки upload видна пунктирная рамка accent-цвета, (b) под форматами есть pill «Из файлов», (c) карточка не схлопывается ниже 260px по высоте.</human-check>
  </verify>
  <done>DashedBorder через CustomPaint отрисован; pill «Из файлов» виден; minHeight=260 применён через ConstrainedBox.</done>
</task>

<task type="auto">
  <name>Task 3: Settings status card + app fade transitions + primary_button token</name>
  <files>lib/ui/screens/settings_screen.dart, lib/app.dart, lib/ui/widgets/primary_button.dart</files>
  <action>
  Три независимые мелкие правки:

  (A) `lib/ui/screens/settings_screen.dart` — добавить status card «Подключено к Groq» вверху списка настроек (после AppBar, до полей ввода). Использовать `GlassTile` с `Row`:
  - `Container` 48×48px, `decoration: BoxDecoration(gradient: AppGradients.accent, borderRadius: BorderRadius.circular(14))`, `child: Icon(Icons.vpn_key_rounded, color: AppColors.inkPrimary, size: 24)` — аватар-иконка ключа;
  - `SizedBox(width: AppSpacing.md)`;
  - `Expanded(child: Column(crossAxisAlignment: start, children: [Text('Подключено к Groq', style: AppTextStyles.heading), Row(children: [Container 8×8 good dot: BoxDecoration(color: AppColors.good, shape: circle), SizedBox(width: AppSpacing.sm), Text('API ключ сохранён', style: AppTextStyles.label)])]))`.
  Карточку показывать всегда (если в state есть условие «ключ задан» — соблюсти его; иначе всегда). Комментарии на русском.

  (B) `lib/app.dart` — заменить дефолтные `MaterialPageRoute` на `PageRouteBuilder` с 300ms fade. Если используется `MaterialApp.routes` / `onGenerateRoute` — переписать `onGenerateRoute` так:
  ```
  onGenerateRoute: (settings) {
    final builder = _routeBuilders[settings.name];
    if (builder == null) return null;
    return PageRouteBuilder(
      settings: settings,
      transitionDuration: const Duration(milliseconds: 300),
      pageBuilder: (ctx, a, sa) => builder(ctx),
      transitionsBuilder: (ctx, a, sa, child) => FadeTransition(
        opacity: CurvedAnimation(parent: a, curve: Curves.easeInOut),
        child: child,
      ),
    );
  }
  ```
  Если структура иная (`MaterialApp.router` / именованные routes через map) — адаптировать минимально: каждый маршрут отдаёт `PageRouteBuilder` с 300ms easeInOut FadeTransition.

  (C) `lib/ui/widgets/primary_button.dart` — заменить `SizedBox(width: 8)` на `SizedBox(width: AppSpacing.sm)`. Импорт `AppSpacing` должен уже быть; если нет — добавить.
  </action>
  <verify>
    <automated>flutter analyze lib/ui/screens/settings_screen.dart lib/app.dart lib/ui/widgets/primary_button.dart</automated>
    <human-check>(a) Settings: сверху виден status card с иконкой ключа на градиенте, зелёной точкой и текстом «Подключено к Groq». (b) Переход между экранами — плавный fade ~300ms, не slide. (c) primary button с иконкой имеет корректный отступ (визуально без регрессии).</human-check>
  </verify>
  <done>Status card отрисован; все переходы — fade 300ms easeInOut; magic number 8 заменён на AppSpacing.sm.</done>
</task>

</tasks>

<verification>
- `flutter analyze` чистый по 5 затронутым файлам.
- Ручная проверка трёх экранов (home/processing/settings) + переходов между ними по чек-листу из `<human-check>`.
</verification>

<success_criteria>
Все 10 пунктов из task_context закрыты:
1. pulseDot ✓ (Task 1)
2. glass pill cancel bar ✓ (Task 1)
3. bottom safe area + 32 ✓ (Task 1)
4. GlassTile для file card ✓ (Task 1)
5. dashed border ✓ (Task 2)
6. «Из файлов» pill ✓ (Task 2)
7. minHeight 260 ✓ (Task 2)
8. Groq status card ✓ (Task 3A)
9. fade transitions 300ms ✓ (Task 3B)
10. AppSpacing.sm в primary_button ✓ (Task 3C)
</success_criteria>

<output>
По завершении создать `.planning/quick/260517-if3-ui-1/260517-if3-SUMMARY.md` с перечнем фактически изменённых строк/файлов и скриншот-чеклистом для ручного ревью.
</output>
