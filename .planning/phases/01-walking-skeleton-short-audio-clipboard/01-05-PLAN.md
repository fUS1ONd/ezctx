---
phase: 01-walking-skeleton-short-audio-clipboard
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - lib/features/transcription/result_args.dart
  - lib/features/transcription/transcript_writer.dart
  - lib/ui/screens/result_screen.dart
  - lib/ui/widgets/primary_button.dart
  - test/widget/result_screen_test.dart
autonomous: false
requirements:
  - OUT-02
  - OUT-03
  - OUT-05

must_haves:
  truths:
    - "После транскрибации ResultScreen открывается с полученным текстом (OUT-05)"
    - "Расшифровка отображается через SelectableText в Body 16px (читаемо, можно выделить, скопировать вручную)"
    - "Кнопка «Скопировать» помещает полный текст расшифровки в системный буфер обмена через Clipboard.setData (OUT-03)"
    - "После tap на «Скопировать» кнопка меняет цвет на good (зелёный) и текст «Скопировано» на 1.5 секунды, затем возвращается в исходное состояние"
    - "Текст расшифровки сохраняется в transcript.txt в Application Documents Directory (OUT-02)"
    - "Команда `flutter test test/widget/result_screen_test.dart` зелёная — проверяет вызов Clipboard.setData с правильным текстом и UI state транзишн"
    - "На экране видны метаданные: имя файла, размер, формат (mono стиль)"
    - "Кнопка-back в шапке возвращает на главный экран Home (popUntil(first))"
  artifacts:
    - path: "lib/features/transcription/result_args.dart"
      provides: "Argument-объект для маршрута /result (file + transcription result)"
      exports: ["ResultArgs"]
    - path: "lib/features/transcription/transcript_writer.dart"
      provides: "Сервис записи transcript.txt в Application Documents Directory"
      exports: ["TranscriptWriter"]
    - path: "lib/ui/screens/result_screen.dart"
      provides: "Экран результата с Clipboard, SelectableText, save to txt"
    - path: "test/widget/result_screen_test.dart"
      provides: "Widget тест Clipboard interaction (наполняет Wave 0 заглушку Plan 01)"
      contains: "Clipboard.setData"
  key_links:
    - from: "lib/ui/screens/result_screen.dart"
      to: "Clipboard.setData"
      via: "flutter/services.dart import"
      pattern: "Clipboard\\.setData"
    - from: "lib/ui/screens/result_screen.dart"
      to: "lib/features/transcription/transcript_writer.dart"
      via: "вызов write(text) при открытии экрана"
      pattern: "TranscriptWriter"
    - from: "lib/features/transcription/transcript_writer.dart"
      to: "path_provider"
      via: "getApplicationDocumentsDirectory"
      pattern: "getApplicationDocumentsDirectory"
    - from: "lib/ui/screens/processing_screen.dart"
      to: "lib/features/transcription/result_args.dart"
      via: "Navigator.pushReplacementNamed с ResultArgs"
      pattern: "ResultArgs"
---

# Plan 05 — Result Screen + Clipboard + Save txt + E2E verification

<objective>
Закрыть OUT-02, OUT-03, OUT-05 и провести end-to-end проверку всей Phase 1 на физическом устройстве.

После Plan 05 пользователь:
1. Видит расшифровку на экране результата (OUT-05).
2. Нажимает «Скопировать» → весь текст в буфере обмена → вставляется в Telegram/любое приложение (OUT-03).
3. Файл `transcript.txt` создан в постоянном хранилище приложения (OUT-02) — это задел для Phase 6 (History).

Purpose: Завершить вертикальный срез Walking Skeleton. После этого плана Phase 1 closed.

Output:
- `ResultArgs` — типобезопасный argument для маршрута `/result`.
- `TranscriptWriter` — запись текста в файл через `path_provider`.
- `ResultScreen` — полноценный экран с Clipboard и save-логикой.
- `PrimaryButton` обновлён — поддерживает state `copied` (transition default → good 1.5s → default).
- Widget тест проверяет Clipboard interaction.
- Финальный checkpoint: запуск всего сценария на физическом устройстве (add key → pick file → transcribe → copy → paste).
</objective>

<execution_context>
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/workflows/execute-plan.md
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-VALIDATION.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-04-SUMMARY.md
@lib/core/constants/app_constants.dart
@lib/core/constants/design_tokens.dart
@lib/features/transcription/transcription_result.dart
@lib/features/transcription/selected_audio_file.dart
@lib/ui/screens/result_screen.dart
@lib/ui/screens/processing_screen.dart
@lib/ui/widgets/glass_card.dart
@lib/ui/widgets/glass_icon_btn.dart
@lib/ui/widgets/primary_button.dart
</context>

<interfaces>
Из Plan 01: `Clipboard.setData(ClipboardData(text: ...))` доступен через `package:flutter/services.dart`.

Из pubspec.yaml: `path_provider: ^2.1.5` — `getApplicationDocumentsDirectory()` возвращает `Future<Directory>` (на Android — `/data/data/com.ezctx.app/files/...`).

Из Plan 04: ProcessingScreen вызывает `Navigator.pushReplacementNamed(context, '/result', arguments: ...)`. В Plan 04 был использован стаб `_ResultArgs` — в Plan 05 заменяется на публичный `ResultArgs`.

```dart
// Plan 04: lib/features/transcription/transcription_result.dart
class TranscriptionResult {
  final String text;
  final String language;
  final double duration;
  final List<WordTimestamp> words;
}

// Plan 03: lib/features/transcription/selected_audio_file.dart
class SelectedAudioFile {
  final String path;
  final String name;
  final int sizeBytes;
  final String extension;
  String get sizeFormatted;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ResultArgs + TranscriptWriter + wire ProcessingScreen</name>
  <files>lib/features/transcription/result_args.dart, lib/features/transcription/transcript_writer.dart, lib/ui/screens/processing_screen.dart</files>
  <read_first>
    - lib/features/transcription/transcription_result.dart
    - lib/features/transcription/selected_audio_file.dart
    - lib/ui/screens/processing_screen.dart (Plan 04 — стаб _ResultArgs)
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-RESEARCH.md (Don't Hand-Roll — path_provider)
  </read_first>
  <action>
    `lib/features/transcription/result_args.dart`:
    ```dart
    import 'selected_audio_file.dart';
    import 'transcription_result.dart';

    /// Аргументы для маршрута /result.
    /// Публичный заменитель приватного _ResultArgs из Plan 04.
    class ResultArgs {
      final SelectedAudioFile file;
      final TranscriptionResult result;
      const ResultArgs({required this.file, required this.result});
    }
    ```

    `lib/features/transcription/transcript_writer.dart`:
    ```dart
    import 'dart:io';

    import 'package:path_provider/path_provider.dart';

    /// Сохраняет расшифровку в постоянное хранилище приложения.
    /// Используется в Plan 05 (OUT-02 — transcript.txt) и расширится в Plan 5 milestone'а (SRT).
    class TranscriptWriter {
      const TranscriptWriter();

      /// Записывает текст в `<app docs>/transcripts/<baseName>.txt`.
      /// Возвращает абсолютный путь к созданному файлу.
      /// `baseName` — имя без расширения (например, имя исходного аудиофайла).
      Future<String> writeTxt({
        required String baseName,
        required String text,
      }) async {
        final docs = await getApplicationDocumentsDirectory();
        final dir = Directory('${docs.path}/transcripts');
        if (!await dir.exists()) {
          await dir.create(recursive: true);
        }
        final safeBaseName = _sanitize(baseName);
        final file = File('${dir.path}/$safeBaseName.txt');
        await file.writeAsString(text, flush: true);
        return file.path;
      }

      static String _sanitize(String name) {
        // Удаляем расширение исходного аудио + опасные символы
        var n = name;
        final dotIdx = n.lastIndexOf('.');
        if (dotIdx > 0) n = n.substring(0, dotIdx);
        return n.replaceAll(RegExp(r'[^\w\-\. ]+'), '_').trim();
      }
    }
    ```

    Обновить `lib/ui/screens/processing_screen.dart`:
    - Удалить приватный `_ResultArgs` (Plan 04 stub).
    - Импортировать `ResultArgs` из `result_args.dart`.
    - В `_onStateChange` при `TranscriptionSuccess`:
      ```dart
      Navigator.pushReplacementNamed(
        context,
        AppConstants.routeResult,
        arguments: ResultArgs(file: _file!, result: s.result),
      );
      ```
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter analyze --no-fatal-infos && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - Три файла существуют/обновлены.
    - В `processing_screen.dart` нет приватного `_ResultArgs` (старый stub удалён).
    - `grep -c "ResultArgs" lib/ui/screens/processing_screen.dart` >= 1.
    - В `transcript_writer.dart` используется `getApplicationDocumentsDirectory()` (не hand-roll path).
    - `flutter analyze` — 0 ошибок.
    - `flutter test` зелёный.
  </acceptance_criteria>
  <done>Аргументы маршрута типизированы; сервис записи txt готов; ProcessingScreen передаёт результат правильно.</done>
</task>

<task type="auto">
  <name>Task 2: PrimaryButton — добавить state "copied" с transition</name>
  <files>lib/ui/widgets/primary_button.dart</files>
  <read_first>
    - lib/ui/widgets/primary_button.dart (Plan 01 базовая реализация)
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md (Interaction States — Кнопка «Скопировать»: Default → Copied (1.5s) → Default; Animations — Copy → Copied 1.5s state transition)
    - lib/core/constants/design_tokens.dart
  </read_first>
  <action>
    Расширить `PrimaryButton`. Добавить опциональный параметр `variant` (enum: `accent`, `good`) и опциональный `icon`. Параметр `label` уже есть.

    Если в Plan 01 PrimaryButton был StatelessWidget с фиксированным gradient — перевести в StatefulWidget с поддержкой состояний:
    ```dart
    enum PrimaryButtonVariant { accent, good }

    class PrimaryButton extends StatelessWidget {
      final String label;
      final VoidCallback? onPressed;
      final IconData? icon;
      final PrimaryButtonVariant variant;

      const PrimaryButton({
        super.key,
        required this.label,
        required this.onPressed,
        this.icon,
        this.variant = PrimaryButtonVariant.accent,
      });

      @override
      Widget build(BuildContext context) {
        final gradient = switch (variant) {
          PrimaryButtonVariant.accent => AppGradients.accent,
          PrimaryButtonVariant.good => const LinearGradient(
              colors: [Color(0xFF3FCB9A), AppColors.good],
            ),
        };
        // ... остальной build (height 52, borderRadius, shadow, label, icon) ...
      }
    }
    ```

    Цвет shadow тоже зависит от variant (accent: `AppColors.accent.withOpacity(0.32)`; good: `AppColors.good.withOpacity(0.32)`).

    На уровне ResultScreen в Task 3 будет AnimatedSwitcher между двумя экземплярами PrimaryButton (accent и good) — это даст smooth transition. Сам PrimaryButton не нужно делать stateful — просто параметризовать variant и label.
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter analyze --no-fatal-infos lib/ui/widgets/primary_button.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` — 0 ошибок.
    - В файле определён enum `PrimaryButtonVariant` с минимум `accent` и `good`.
    - Конструктор принимает параметр `variant` со значением по умолчанию `accent` (обратная совместимость с использованиями из Plan 01/02/03/04).
    - `flutter test` зелёный.
  </acceptance_criteria>
  <done>PrimaryButton поддерживает два варианта цвета для visual feedback копирования.</done>
</task>

<task type="auto">
  <name>Task 3: ResultScreen — full UI + Clipboard + save txt + widget test</name>
  <files>lib/ui/screens/result_screen.dart, test/widget/result_screen_test.dart</files>
  <read_first>
    - lib/ui/screens/result_screen.dart (Plan 01 — заглушка)
    - lib/features/transcription/result_args.dart
    - lib/features/transcription/transcript_writer.dart
    - lib/features/transcription/transcription_result.dart
    - lib/features/transcription/selected_audio_file.dart
    - lib/ui/widgets/primary_button.dart (с добавленным variant из Task 2)
    - lib/ui/widgets/glass_card.dart, lib/ui/widgets/glass_icon_btn.dart
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md (Screen 3: Result / Detail, Copywriting Contract)
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-RESEARCH.md (Pattern 3 Clipboard copy with visual feedback)
  </read_first>
  <action>
    Полностью переписать `lib/ui/screens/result_screen.dart`.

    ```dart
    import 'package:flutter/material.dart';
    import 'package:flutter/services.dart';

    import '../../core/constants/app_constants.dart';
    import '../../core/constants/design_tokens.dart';
    import '../../features/transcription/result_args.dart';
    import '../../features/transcription/transcript_writer.dart';
    import '../widgets/glass_card.dart';
    import '../widgets/glass_icon_btn.dart';
    import '../widgets/gradient_background.dart';
    import '../widgets/primary_button.dart';

    class ResultScreen extends StatefulWidget {
      const ResultScreen({super.key});

      @override
      State<ResultScreen> createState() => _ResultScreenState();
    }

    class _ResultScreenState extends State<ResultScreen> {
      ResultArgs? _args;
      bool _copied = false;
      String? _savedPath;

      @override
      void didChangeDependencies() {
        super.didChangeDependencies();
        if (_args == null) {
          final raw = ModalRoute.of(context)?.settings.arguments;
          if (raw is ResultArgs) {
            _args = raw;
            // OUT-02: автоматически сохраняем txt после открытия экрана
            _saveTranscriptTxt();
          } else {
            WidgetsBinding.instance.addPostFrameCallback((_) {
              Navigator.popUntil(context, (r) => r.isFirst);
            });
          }
        }
      }

      Future<void> _saveTranscriptTxt() async {
        try {
          final path = await const TranscriptWriter().writeTxt(
            baseName: _args!.file.name,
            text: _args!.result.text,
          );
          if (mounted) setState(() => _savedPath = path);
        } catch (_) {
          // тихо — UI не блокируется ошибкой записи; пользователь всё равно может скопировать
        }
      }

      Future<void> _onCopyTap() async {
        if (_args == null) return;
        await Clipboard.setData(ClipboardData(text: _args!.result.text));
        if (!mounted) return;
        setState(() => _copied = true);
        Future.delayed(const Duration(milliseconds: 1500), () {
          if (mounted) setState(() => _copied = false);
        });
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(
            content: Text('Скопировано'),
            duration: Duration(seconds: 1),
          ),
        );
      }

      @override
      Widget build(BuildContext context) {
        if (_args == null) {
          return const Scaffold(body: SizedBox.shrink());
        }
        final file = _args!.file;
        final r = _args!.result;
        final formattedDate = _formatNow();

        return Scaffold(
          body: GradientBackground(
            child: SafeArea(
              child: Padding(
                padding: const EdgeInsets.symmetric(horizontal: AppSpacing.lg),
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    // Шапка
                    Row(
                      children: [
                        GlassIconBtn(
                          icon: Icons.arrow_back,
                          semanticLabel: 'Назад',
                          onPressed: () => Navigator.popUntil(context, (r) => r.isFirst),
                        ),
                        const SizedBox(width: AppSpacing.md),
                        Text(formattedDate, style: AppTextStyles.body.copyWith(color: AppColors.inkSecondary)),
                        const Spacer(),
                        // share icon placeholder (deferred до Phase 5 milestone)
                      ],
                    ),
                    const SizedBox(height: AppSpacing.lg),
                    // Имя файла + метаданные
                    Text(file.name, style: AppTextStyles.heading),
                    const SizedBox(height: AppSpacing.xs),
                    Text(
                      '${file.sizeFormatted} · ${file.extension.toUpperCase()} · ${r.duration.toStringAsFixed(1)} сек',
                      style: AppTextStyles.mono,
                    ),
                    const SizedBox(height: AppSpacing.lg),
                    // Кнопка копирования (transition default → good)
                    AnimatedSwitcher(
                      duration: const Duration(milliseconds: 200),
                      child: PrimaryButton(
                        key: ValueKey(_copied),
                        label: _copied ? 'Скопировано' : 'Скопировать',
                        icon: _copied ? Icons.check : Icons.copy_outlined,
                        variant: _copied ? PrimaryButtonVariant.good : PrimaryButtonVariant.accent,
                        onPressed: _onCopyTap,
                      ),
                    ),
                    const SizedBox(height: AppSpacing.lg),
                    // Карточка с текстом (scrollable)
                    Expanded(
                      child: GlassCard(
                        padding: const EdgeInsets.all(AppSpacing.md),
                        child: SingleChildScrollView(
                          child: SelectableText(
                            r.text,
                            style: AppTextStyles.body,
                          ),
                        ),
                      ),
                    ),
                    const SizedBox(height: AppSpacing.md),
                    if (_savedPath != null)
                      Padding(
                        padding: const EdgeInsets.only(bottom: AppSpacing.sm),
                        child: Text(
                          'Сохранено: $_savedPath',
                          style: AppTextStyles.label.copyWith(color: AppColors.inkTertiary),
                          maxLines: 1,
                          overflow: TextOverflow.ellipsis,
                        ),
                      ),
                  ],
                ),
              ),
            ),
          ),
        );
      }

      String _formatNow() {
        final n = DateTime.now();
        final hh = n.hour.toString().padLeft(2, '0');
        final mm = n.minute.toString().padLeft(2, '0');
        return 'Сегодня · $hh:$mm';
      }
    }
    ```

    `test/widget/result_screen_test.dart` (заменяет Wave 0 skip-заглушку):
    ```dart
    import 'package:ezctx/features/transcription/result_args.dart';
    import 'package:ezctx/features/transcription/selected_audio_file.dart';
    import 'package:ezctx/features/transcription/transcription_result.dart';
    import 'package:ezctx/ui/screens/result_screen.dart';
    import 'package:flutter/material.dart';
    import 'package:flutter/services.dart';
    import 'package:flutter_test/flutter_test.dart';

    void main() {
      const transcriptText = 'Это тестовая расшифровка лекции для проверки буфера.';

      ResultArgs makeArgs() => const ResultArgs(
            file: SelectedAudioFile(
              path: '/tmp/test.mp3',
              name: 'test.mp3',
              sizeBytes: 1024 * 1024,
              extension: 'mp3',
            ),
            result: TranscriptionResult(
              text: transcriptText,
              language: 'russian',
              duration: 4.2,
              words: [],
            ),
          );

      testWidgets('ResultScreen отображает текст расшифровки в SelectableText', (tester) async {
        await tester.pumpWidget(
          MaterialApp(
            onGenerateRoute: (settings) => MaterialPageRoute(
              builder: (_) => const ResultScreen(),
              settings: RouteSettings(arguments: makeArgs()),
            ),
          ),
        );
        await tester.pumpAndSettle();

        expect(find.text(transcriptText), findsOneWidget);
        expect(find.byType(SelectableText), findsWidgets);
      });

      testWidgets('Tap «Скопировать» вызывает Clipboard.setData с текстом расшифровки', (tester) async {
        String? capturedText;

        // Мокаем системный канал clipboard
        TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger
            .setMockMethodCallHandler(SystemChannels.platform, (call) async {
          if (call.method == 'Clipboard.setData') {
            capturedText = (call.arguments as Map)['text'] as String?;
          }
          return null;
        });

        await tester.pumpWidget(
          MaterialApp(
            onGenerateRoute: (settings) => MaterialPageRoute(
              builder: (_) => const ResultScreen(),
              settings: RouteSettings(arguments: makeArgs()),
            ),
          ),
        );
        await tester.pumpAndSettle();

        await tester.tap(find.text('Скопировать'));
        await tester.pump();

        expect(capturedText, transcriptText);
      });

      testWidgets('После tap «Скопировать» отображается «Скопировано»', (tester) async {
        TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger
            .setMockMethodCallHandler(SystemChannels.platform, (_) async => null);

        await tester.pumpWidget(
          MaterialApp(
            onGenerateRoute: (settings) => MaterialPageRoute(
              builder: (_) => const ResultScreen(),
              settings: RouteSettings(arguments: makeArgs()),
            ),
          ),
        );
        await tester.pumpAndSettle();

        await tester.tap(find.text('Скопировать'));
        await tester.pump(const Duration(milliseconds: 100));

        expect(find.text('Скопировано'), findsOneWidget);
      });
    }
    ```

    > Удалить ранее существующий skip-блок в `test/widget/result_screen_test.dart` (Plan 01 Wave 0 заглушка).
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter test test/widget/result_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/widget/result_screen_test.dart` зелёный.
    - Минимум 3 проходящих widget теста (отображение текста, Clipboard.setData, UI state transition).
    - В `result_screen.dart` импорт `package:flutter/services.dart` (для Clipboard).
    - `grep -c "Clipboard.setData" lib/ui/screens/result_screen.dart` >= 1.
    - `grep -c "SelectableText" lib/ui/screens/result_screen.dart` >= 1 (OUT-05 — текст можно выделять).
    - `grep -c "TranscriptWriter" lib/ui/screens/result_screen.dart` >= 1 (OUT-02).
    - `flutter analyze` — 0 ошибок.
    - `flutter test` (полная suite) — все тесты зелёные.
  </acceptance_criteria>
  <done>ResultScreen полностью функционален: отображает текст, копирует в буфер, сохраняет txt; widget тесты подтверждают Clipboard.</done>
</task>

<task type="auto">
  <name>Task 4: Финальный rebuild + manual SUMMARY of Phase 1</name>
  <files>—</files>
  <read_first>
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/SKELETON.md (Walking Skeleton Acceptance)
    - .planning/ROADMAP.md (Phase 1 Success Criteria)
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-VALIDATION.md
  </read_first>
  <action>
    Финальный rebuild и подготовка к чекпоинту:

    1. `flutter clean` — гарантировать чистую сборку.
    2. `flutter pub get`.
    3. `flutter analyze --no-fatal-infos` — должен быть код 0.
    4. `flutter test` — должен быть код 0 (все тесты passing, никаких skip кроме явно помеченных).
    5. `flutter build apk --debug` — собрать финальный APK.
    6. Записать размер и SHA1 финального APK в task summary.
    7. Записать команду установки: `adb install -r build/app/outputs/flutter-apk/app-debug.apk`.
    8. Записать в task summary полный список прохождения Phase 1 Success Criteria из ROADMAP.md (5 пунктов) — для каждого: на каком плане закрыт.
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter clean && flutter pub get && flutter analyze --no-fatal-infos && flutter test && flutter build apk --debug && test -f build/app/outputs/flutter-apk/app-debug.apk</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` — 0 ошибок.
    - `flutter test` — все тесты зелёные, минимум 25 passing тестов (5 secure_storage + 8 api_key_repo + 13 file_validator + 5 groq + 3 result widget).
    - `build/app/outputs/flutter-apk/app-debug.apk` существует.
    - Task summary содержит маппинг ROADMAP Success Criteria → Plan.
  </acceptance_criteria>
  <done>Финальный APK собран, все тесты зелёные, готов к ручной проверке на устройстве.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end сценарий на физическом Android устройстве</name>
  <files>—</files>
  <action>Human-verified checkpoint. Verification is performed by the user via the steps in &lt;how-to-verify&gt;; no automated action is executed.</action>
  <verify>
    <human-check>Все шаги в &lt;how-to-verify&gt; выполнены успешно; пользователь ввёл «approved» в resume-signal.</human-check>
  </verify>
  <done>Пользователь подтвердил полное прохождение всех шагов чекпоинта; никаких блокирующих проблем не обнаружено.</done>
  <what-built>
    Phase 1 (Walking Skeleton) полностью реализована. Приложение ezctx умеет: добавлять Groq API-ключ (с маскированием и удалением), выбирать короткий аудиофайл с устройства (с валидацией расширения и размера), отправлять его в Groq Whisper API одним запросом с verbose_json, отображать расшифровку на экране результата, копировать текст в системный буфер обмена с visual feedback. Файл transcript.txt сохраняется в Application Documents Directory. CI собирает debug APK на каждый push в main.
  </what-built>
  <how-to-verify>
    Полный сквозной сценарий «студент → расшифровка в Telegram» на физическом устройстве:

    **Подготовка:**
    1. Получить Groq API ключ на https://console.groq.com/keys (бесплатно).
    2. Подготовить короткий аудиофайл < 19 MB (например, 1-2 минуты речи в .mp3 или .m4a).
    3. Подключить Android-устройство к WSL через usbipd-win.
    4. Установить APK: `adb install -r build/app/outputs/flutter-apk/app-debug.apk`.

    **Сценарий 1 — Happy path:**
    1. Открыть приложение «ezctx» — открывается главный экран.
    2. Нажать «Транскрибировать» БЕЗ выбранного файла — кнопка disabled (или не реагирует) — ОК.
    3. Нажать на upload-card — открывается системный file_picker.
    4. Выбрать тестовый .mp3 файл — preview обновляется (имя файла + размер).
    5. Нажать «Транскрибировать» — появляется диалог «Добавьте API-ключ» (ключей ещё нет).
    6. Нажать «Перейти в настройки» — открывается ApiKeysScreen.
    7. Вставить Groq ключ в поле, нажать «Добавить ключ» — snackbar «Ключ сохранён», в списке появилась строка с маскированным ключом (`••••••••••••••••XXXX`).
    8. Вернуться назад дважды (на Settings, потом на Home).
    9. Снова нажать «Транскрибировать» — переход на ProcessingScreen с shimmer и pipeline.
    10. Дождаться завершения (5-30 сек) — переход на ResultScreen с текстом расшифровки.
    11. Нажать «Скопировать» — кнопка становится зелёной «Скопировано», snackbar «Скопировано».
    12. Открыть Telegram → отправить «Saved Messages» → вставить из буфера — должен прийти текст расшифровки.

    **Сценарий 2 — Персистентность ключа:**
    13. Закрыть приложение полностью (свайп из недавних задач).
    14. Открыть снова → Settings → API-ключи — ключ всё ещё в списке.

    **Сценарий 3 — Валидация файла:**
    15. На главном экране нажать upload-card → попытаться выбрать .txt файл — file_picker не позволит (allowedExtensions); либо если позволит, сразу появится сообщение «Формат файла не поддерживается».

    **Сценарий 4 — Ошибки:**
    16. На Settings → API-ключи удалить ключ (свайп / кнопка удалить → подтверждение «Удалить?»).
    17. Альтернатива: добавить НЕВАЛИДНЫЙ ключ (`gsk_invalidlongenoughstring123456789`) → Transcribe → должно показаться «Ключ не подошёл. Проверьте его в console.groq.com → API Keys.»
    18. Отключить интернет на устройстве → попробовать транскрибировать → «Не удалось подключиться к Groq.» + кнопка «Повторить».

    **Сценарий 5 — CI:**
    19. Запушить любой коммит в main (или открыть существующий PR).
    20. Открыть GitHub Actions tab → убедиться что workflow «Build Debug APK» запустился и завершился успешно.
    21. Скачать артефакт `debug-apk-<sha>` → распаковать → установить → запустить — приложение работает.

    **Если что-то падает:**
    - Записать: какой сценарий, какой шаг, что наблюдалось vs что ожидалось.
    - Приложить `adb logcat -d | grep -i ezctx` если есть.
  </how-to-verify>
  <resume-signal>
    Введите «approved» если все 21 шаг прошли успешно. В противном случае опишите проблемы — будет создан gap closure plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App memory → System Clipboard | Текст расшифровки попадает в системный буфер, доступный другим приложениям |
| App → File System (app docs) | Запись transcript.txt в private storage приложения |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Текст расшифровки в clipboard | accept | Цель функции — поделиться текстом; пользователь явно нажимает «Скопировать». На Android 10+ clipboard всплывает уведомление для пользователя. |
| T-05-02 | Information Disclosure | transcript.txt доступен другим приложениям | mitigate | `getApplicationDocumentsDirectory()` возвращает `/data/data/com.ezctx.app/files/` — private app storage, не доступно другим приложениям без root |
| T-05-03 | Tampering | Имя файла из user input → path injection | mitigate | `TranscriptWriter._sanitize` фильтрует все символы кроме `\w\-\. ` |
| T-05-04 | DoS (self) | Очень большой text → крах при write | accept | Phase 1 ограничивает входной файл 19 MB; ожидаемый размер txt < 200 KB; стандартный writeAsString справляется |
| T-05-05 | Information Disclosure | Текст расшифровки в SelectableText доступен screenshot/screen recording | accept | Природа функции — отображение текста пользователю; защита от скриншотов (FLAG_SECURE) для txt-расшифровок — over-engineering для MVP |
| T-05-06 | Repudiation | Нет audit log копирований | accept | Однопользовательский клиент; audit не требуется |
</threat_model>

<verification>
- `flutter analyze` — 0 ошибок.
- `flutter test` — все тесты зелёные (>= 25 passing).
- `flutter build apk --debug` — успешная сборка.
- Полный E2E сценарий (21 шаг) выполнен на физическом устройстве.
</verification>

<success_criteria>
- Все 8 must-haves.truths выполняются.
- OUT-02, OUT-03, OUT-05 закрыты вертикальным срезом UI → Clipboard / File system.
- ROADMAP Phase 1 Success Criteria (5 пунктов) все выполнены:
  1. Debug APK устанавливается на Android устройство (Plan 01).
  2. Пользователь может ввести Groq-ключ, ключ переживает перезапуск (Plan 02).
  3. Выбор аудиофайла < 19 MB через file_picker, отклонение неподдерживаемых форматов (Plan 03).
  4. Single-shot отправка в Groq Whisper с verbose_json, отображение текста (Plan 04 + 05).
  5. Кнопка «Скопировать» помещает текст в буфер, вставка в Telegram работает (Plan 05).
</success_criteria>

<output>
Создать `.planning/phases/01-walking-skeleton-short-audio-clipboard/01-05-SUMMARY.md`. Включить:
- Полный список passing тестов (>= 25).
- Размер и SHA1 финального APK.
- Маппинг 5 ROADMAP Success Criteria → планы 01–05.
- Замеченные нестыковки/баги (если есть) для возможного gap closure плана.
- Подтверждение прохождения всех 21 шага чекпоинта.
</output>
