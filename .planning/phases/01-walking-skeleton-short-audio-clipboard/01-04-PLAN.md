---
phase: 01-walking-skeleton-short-audio-clipboard
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - lib/features/transcription/transcription_result.dart
  - lib/features/transcription/groq_api_service.dart
  - lib/features/transcription/transcription_controller.dart
  - lib/ui/screens/processing_screen.dart
  - lib/ui/screens/home_screen.dart
  - lib/ui/widgets/shimmer_bar.dart
  - test/unit/groq_service_test.dart
autonomous: true
requirements:
  - TRANS-03
  - TRANS-07

must_haves:
  truths:
    - "GroqApiService.transcribe(file: SelectedAudioFile, apiKey: String) выполняет POST на https://api.groq.com/openai/v1/audio/transcriptions с multipart/form-data"
    - "Multipart запрос содержит поля: file, model=whisper-large-v3, response_format=verbose_json, timestamp_granularities[]=word (TRANS-07)"
    - "Authorization header содержит 'Bearer <api-key>'"
    - "После tap на «Транскрибировать» на HomeScreen приложение переходит на ProcessingScreen с передачей SelectedAudioFile + первого активного ключа из ApiKeyRepository"
    - "ProcessingScreen показывает имя файла, размер, shimmer-bar прогресса и pipeline из 3 шагов (Загрузка → Распознавание → Готово)"
    - "По завершении транскрибации ProcessingScreen вызывает Navigator.pushReplacementNamed с маршрутом /result и передаёт TranscriptionResult"
    - "При 401 от Groq показывается сообщение «Ключ не подошёл. Проверьте его в console.groq.com → API Keys.» и кнопка «Назад»"
    - "При сетевой ошибке показывается «Не удалось подключиться к Groq. Проверьте интернет и попробуйте снова.» и кнопка «Повторить»"
    - "Команда `flutter test test/unit/groq_service_test.dart` зелёная — покрывает структуру multipart, успешный 200, 401, 5xx"
    - "Если ApiKeyRepository.listKeys() пуст — на HomeScreen tap «Транскрибировать» показывает диалог с предложением перейти в настройки добавить ключ"
  artifacts:
    - path: "lib/features/transcription/transcription_result.dart"
      provides: "Модель ответа Groq + парсер verbose_json"
      exports: ["TranscriptionResult", "WordTimestamp"]
    - path: "lib/features/transcription/groq_api_service.dart"
      provides: "HTTP сервис для отправки файла в Groq Whisper"
      exports: ["GroqApiService"]
    - path: "lib/features/transcription/transcription_controller.dart"
      provides: "Координатор: ключ → service → state для UI"
      exports: ["TranscriptionController", "TranscriptionState"]
    - path: "lib/ui/screens/processing_screen.dart"
      provides: "Экран прогресса с pipeline и shimmer"
    - path: "test/unit/groq_service_test.dart"
      provides: "Unit тесты с MockClient (наполняет Wave 0 заглушку Plan 01)"
      contains: "MockClient"
  key_links:
    - from: "lib/ui/screens/processing_screen.dart"
      to: "lib/features/transcription/transcription_controller.dart"
      via: "конструктор/инстанцирование контроллера и подписка на state"
      pattern: "TranscriptionController"
    - from: "lib/features/transcription/transcription_controller.dart"
      to: "lib/features/transcription/groq_api_service.dart"
      via: "вызов transcribe(...)"
      pattern: "GroqApiService"
    - from: "lib/features/transcription/transcription_controller.dart"
      to: "lib/features/settings/api_key_repository.dart"
      via: "listKeys() — берёт первый ключ"
      pattern: "ApiKeyRepository|listKeys"
    - from: "lib/features/transcription/groq_api_service.dart"
      to: "https://api.groq.com/openai/v1/audio/transcriptions"
      via: "AppConstants.groqApiUrl"
      pattern: "groqApiUrl|api\\.groq\\.com"
    - from: "lib/ui/screens/home_screen.dart"
      to: "/processing"
      via: "Navigator.pushNamed при tap транскрибировать"
      pattern: "pushNamed.*routeProcessing"
---

# Plan 04 — Groq Transcription: HTTP сервис + ProcessingScreen

<objective>
Доставить вертикальный срез TRANS-03 + TRANS-07: пользователь нажимает «Транскрибировать» → приложение делает один HTTP-запрос с multipart/form-data на Groq Whisper API → парсит `verbose_json` → переходит на экран результата.

Purpose: Соединить компоненты Plan 02 (ApiKey) + Plan 03 (SelectedAudioFile) → реально расшифровать короткий файл через Groq.

Output:
- `GroqApiService` — HTTP multipart запрос, обработка 200/401/network errors.
- `TranscriptionResult` — модель ответа с `text`, `language`, `duration`, `words`.
- `TranscriptionController` — связывает ApiKeyRepository + GroqApiService и отдаёт state для UI.
- `ProcessingScreen` — экран с pipeline (3 шага), shimmer-bar, обработкой ошибок.
- `HomeScreen` — нажатие «Транскрибировать» переводит на ProcessingScreen с передачей файла.
- `ShimmerBar` widget — переиспользуемый shimmer для прогресса.
- Unit тесты для GroqApiService с `MockClient` из `package:http/testing.dart`.
</objective>

<execution_context>
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/workflows/execute-plan.md
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-02-SUMMARY.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-03-SUMMARY.md
@lib/core/constants/app_constants.dart
@lib/core/error/app_exception.dart
@lib/features/settings/api_key_repository.dart
@lib/features/transcription/selected_audio_file.dart
@lib/features/transcription/file_picker_service.dart
@lib/ui/screens/processing_screen.dart
@lib/ui/screens/home_screen.dart
@lib/ui/widgets/glass_card.dart
@lib/ui/widgets/glass_tile.dart
@lib/ui/widgets/glass_icon_btn.dart
@lib/ui/widgets/primary_button.dart
</context>

<interfaces>
Из предыдущих планов:

```dart
// Plan 02: lib/features/settings/api_key_repository.dart
class ApiKeyView { final String raw; final String masked; }
class ApiKeyRepository {
  Future<List<ApiKeyView>> listKeys();
  // ...
}

// Plan 03: lib/features/transcription/selected_audio_file.dart
class SelectedAudioFile {
  final String path;
  final String name;
  final int sizeBytes;
  final String extension;
  String get sizeFormatted;
}

// Plan 01: lib/core/error/app_exception.dart
class NetworkException extends AppException { ... }
class AuthException extends AppException { ... }
class ValidationException extends AppException { ... }
class InternalException extends AppException { ... }

// Plan 01: lib/core/constants/app_constants.dart
class AppConstants {
  static const String groqApiUrl = 'https://api.groq.com/openai/v1/audio/transcriptions';
  static const String groqDefaultModel = 'whisper-large-v3';
  static const String groqResponseFormat = 'verbose_json';
  static const String groqTimestampGranularity = 'word';
  static const String routeProcessing = '/processing';
  static const String routeResult = '/result';
  static const String routeApiKeys = '/settings/api-keys';
}
```

Из pubspec.yaml: `http: ^1.4.0` (есть MockClient в `package:http/testing.dart`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TranscriptionResult model + GroqApiService + полные unit-тесты с MockClient</name>
  <files>lib/features/transcription/transcription_result.dart, lib/features/transcription/groq_api_service.dart, test/unit/groq_service_test.dart</files>
  <read_first>
    - lib/core/constants/app_constants.dart
    - lib/core/error/app_exception.dart
    - lib/features/transcription/selected_audio_file.dart
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-RESEARCH.md (Pattern 1 Groq Multipart Request, Groq Whisper API — Детали, Pitfall 3 timestamp_granularities)
  </read_first>
  <behavior>
    - GroqApiService.transcribe(file, apiKey) при моке HTTP 200 возвращает TranscriptionResult с правильно распарсенным text.
    - В отправленном multipart присутствуют ВСЕ поля: file, model=whisper-large-v3, response_format=verbose_json, timestamp_granularities[]=word.
    - Authorization header = 'Bearer <apiKey>'.
    - При HTTP 401 бросается AuthException с сообщением «Ключ не подошёл. Проверьте его в console.groq.com → API Keys.».
    - При HTTP 500/503/524 бросается NetworkException с сообщением «Не удалось подключиться к Groq. Проверьте интернет и попробуйте снова.».
    - При SocketException (нет сети) бросается NetworkException с тем же сообщением.
    - При невалидном JSON в ответе 200 бросается InternalException.
    - TranscriptionResult.fromJson корректно парсит verbose_json с полями text, language, duration, words[].
    - Если поле words отсутствует в ответе — words возвращается пустым списком (не null, не crash).
  </behavior>
  <action>
    `lib/features/transcription/transcription_result.dart`:
    ```dart
    /// Одно слово с таймкодами из verbose_json ответа Groq.
    class WordTimestamp {
      final String word;
      final double start;
      final double end;
      const WordTimestamp({required this.word, required this.start, required this.end});

      factory WordTimestamp.fromJson(Map<String, dynamic> json) {
        return WordTimestamp(
          word: json['word'] as String? ?? '',
          start: (json['start'] as num?)?.toDouble() ?? 0.0,
          end: (json['end'] as num?)?.toDouble() ?? 0.0,
        );
      }
    }

    /// Результат транскрибации Groq Whisper.
    /// Phase 1 использует только `text`. Поле `words` понадобится в Phase 5 (SRT).
    class TranscriptionResult {
      final String text;
      final String language;
      final double duration;
      final List<WordTimestamp> words;

      const TranscriptionResult({
        required this.text,
        required this.language,
        required this.duration,
        required this.words,
      });

      factory TranscriptionResult.fromJson(Map<String, dynamic> json) {
        final wordsList = json['words'] as List<dynamic>?;
        return TranscriptionResult(
          text: json['text'] as String? ?? '',
          language: json['language'] as String? ?? '',
          duration: (json['duration'] as num?)?.toDouble() ?? 0.0,
          words: wordsList == null
              ? const <WordTimestamp>[]
              : wordsList
                  .map((w) => WordTimestamp.fromJson(w as Map<String, dynamic>))
                  .toList(),
        );
      }
    }
    ```

    `lib/features/transcription/groq_api_service.dart`:
    ```dart
    import 'dart:async';
    import 'dart:convert';
    import 'dart:io';

    import 'package:http/http.dart' as http;

    import '../../core/constants/app_constants.dart';
    import '../../core/error/app_exception.dart';
    import 'selected_audio_file.dart';
    import 'transcription_result.dart';

    /// HTTP-клиент для Groq Whisper. Phase 1: single-shot (один файл, один запрос).
    /// Чанкование и параллельность — Phase 2.
    class GroqApiService {
      /// `clientFactory` инжектится для тестирования через MockClient.
      /// В production вызов без аргумента создаёт стандартный http.Client.
      GroqApiService({http.Client Function()? clientFactory})
          : _clientFactory = clientFactory ?? (() => http.Client());

      final http.Client Function() _clientFactory;

      static const _authErrorMessage =
          'Ключ не подошёл. Проверьте его в console.groq.com → API Keys.';
      static const _networkErrorMessage =
          'Не удалось подключиться к Groq. Проверьте интернет и попробуйте снова.';

      /// Single-shot транскрибация. Бросает [AuthException]/[NetworkException]/[InternalException].
      Future<TranscriptionResult> transcribe({
        required SelectedAudioFile file,
        required String apiKey,
      }) async {
        final client = _clientFactory();
        try {
          final uri = Uri.parse(AppConstants.groqApiUrl);
          final request = http.MultipartRequest('POST', uri)
            ..headers['Authorization'] = 'Bearer $apiKey'
            ..fields['model'] = AppConstants.groqDefaultModel
            ..fields['response_format'] = AppConstants.groqResponseFormat
            // Pitfall 3: имя поля с '[]' обязательно (Groq API соглашение для массивов).
            ..fields['timestamp_granularities[]'] =
                AppConstants.groqTimestampGranularity
            ..files.add(await http.MultipartFile.fromPath('file', file.path));

          final streamed = await client.send(request);
          final response = await http.Response.fromStream(streamed);

          if (response.statusCode == 200) {
            try {
              final json = jsonDecode(response.body) as Map<String, dynamic>;
              return TranscriptionResult.fromJson(json);
            } catch (e) {
              throw const InternalException('Не удалось разобрать ответ Groq');
            }
          }
          if (response.statusCode == 401) {
            throw const AuthException(_authErrorMessage);
          }
          // 4xx (не 401), 5xx, 524 — сетевые/серверные ошибки
          throw const NetworkException(_networkErrorMessage);
        } on SocketException {
          throw const NetworkException(_networkErrorMessage);
        } on TimeoutException {
          throw const NetworkException(_networkErrorMessage);
        } on AppException {
          rethrow;
        } catch (e) {
          throw const NetworkException(_networkErrorMessage);
        } finally {
          client.close();
        }
      }
    }
    ```

    `test/unit/groq_service_test.dart` (заменяет Wave 0 skip-заглушку):
    ```dart
    import 'dart:convert';

    import 'package:ezctx/core/error/app_exception.dart';
    import 'package:ezctx/features/transcription/groq_api_service.dart';
    import 'package:ezctx/features/transcription/selected_audio_file.dart';
    import 'package:flutter_test/flutter_test.dart';
    import 'package:http/http.dart' as http;
    import 'package:http/testing.dart';

    void main() {
      // MockClient требует path к реальному файлу для http.MultipartFile.fromPath.
      // Создадим temp файл в setUp.
      late SelectedAudioFile testFile;

      setUpAll(() async {
        // создаём временный пустой файл
        final tmp = await _createTempFile();
        testFile = SelectedAudioFile(
          path: tmp,
          name: 'test.mp3',
          sizeBytes: 0,
          extension: 'mp3',
        );
      });

      test('успешный 200 → TranscriptionResult с text', () async {
        final service = GroqApiService(clientFactory: () => MockClient((req) async {
              // Проверяем структуру запроса
              expect(req.method, 'POST');
              expect(req.url.toString(), contains('api.groq.com'));
              expect(req.headers['authorization'], 'Bearer test-key');
              // multipart fields парсятся через request.bodyFields/multipartFields — здесь body уже сериализован
              // проверим что boundary включает обязательные поля
              expect(req.body, contains('whisper-large-v3'));
              expect(req.body, contains('verbose_json'));
              expect(req.body, contains('timestamp_granularities[]'));
              return http.Response(
                jsonEncode({
                  'text': 'Привет мир',
                  'language': 'russian',
                  'duration': 5.2,
                  'words': [
                    {'word': 'Привет', 'start': 0.0, 'end': 0.5},
                    {'word': 'мир', 'start': 0.5, 'end': 0.9},
                  ],
                }),
                200,
                headers: {'content-type': 'application/json'},
              );
            }));

        final result = await service.transcribe(file: testFile, apiKey: 'test-key');
        expect(result.text, 'Привет мир');
        expect(result.language, 'russian');
        expect(result.words.length, 2);
        expect(result.words.first.word, 'Привет');
      });

      test('HTTP 401 → AuthException', () async {
        final service = GroqApiService(
          clientFactory: () => MockClient((_) async => http.Response('', 401)),
        );
        expect(
          () => service.transcribe(file: testFile, apiKey: 'bad-key'),
          throwsA(isA<AuthException>()),
        );
      });

      test('HTTP 500 → NetworkException', () async {
        final service = GroqApiService(
          clientFactory: () => MockClient((_) async => http.Response('', 500)),
        );
        expect(
          () => service.transcribe(file: testFile, apiKey: 'k'),
          throwsA(isA<NetworkException>()),
        );
      });

      test('HTTP 524 (Cloudflare timeout) → NetworkException', () async {
        final service = GroqApiService(
          clientFactory: () => MockClient((_) async => http.Response('', 524)),
        );
        expect(
          () => service.transcribe(file: testFile, apiKey: 'k'),
          throwsA(isA<NetworkException>()),
        );
      });

      test('невалидный JSON в 200 → InternalException', () async {
        final service = GroqApiService(
          clientFactory: () => MockClient((_) async => http.Response('not json', 200)),
        );
        expect(
          () => service.transcribe(file: testFile, apiKey: 'k'),
          throwsA(isA<InternalException>()),
        );
      });

      test('ответ без поля words → пустой список', () {
        final r = TranscriptionResult.fromJson({
          'text': 'hello',
          'language': 'en',
          'duration': 1.0,
        });
        expect(r.words, isEmpty);
        expect(r.text, 'hello');
      });
    }

    Future<String> _createTempFile() async {
      // Создаём временный файл через dart:io
      // Используется в multipart fromPath
      final dir = Directory.systemTemp.createTempSync('ezctx_test');
      final f = File('${dir.path}/test.mp3');
      await f.writeAsBytes([]);
      return f.path;
    }
    ```

    > Удалить ранее существующий skip-блок в `test/unit/groq_service_test.dart` (Plan 01 Wave 0 заглушка).

    Если MockClient в текущей версии http не поддерживает MultipartRequest.body как строку для assert — переписать ассерты на `request.url`, `request.headers`, и проверять что фабрика вызвана. Сохранить минимум 4 теста: 200, 401, 5xx, fromJson edge case.
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter test test/unit/groq_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/unit/groq_service_test.dart` зелёный.
    - Минимум 5 проходящих тестов (200, 401, 5xx, 524, fromJson edge case).
    - В `groq_api_service.dart` строго используется `AppConstants.groqApiUrl` (не хардкод URL).
    - В коде присутствуют все 3 multipart поля по именам из `AppConstants` (`groqDefaultModel`, `groqResponseFormat`, `groqTimestampGranularity`).
    - `timestamp_granularities[]` именно с квадратными скобками (Pitfall 3).
    - В файле обработаны `SocketException` и `TimeoutException`.
    - Отсутствует `print(apiKey)` или `print(.*token)` (security).
  </acceptance_criteria>
  <done>GroqApiService отправляет корректный запрос, обрабатывает 4 категории ответов; покрыт unit-тестами с MockClient.</done>
</task>

<task type="auto">
  <name>Task 2: TranscriptionController + ShimmerBar widget</name>
  <files>lib/features/transcription/transcription_controller.dart, lib/ui/widgets/shimmer_bar.dart</files>
  <read_first>
    - lib/features/transcription/groq_api_service.dart
    - lib/features/settings/api_key_repository.dart
    - lib/features/transcription/selected_audio_file.dart
    - lib/features/transcription/transcription_result.dart
    - lib/core/error/app_exception.dart
    - lib/core/constants/design_tokens.dart
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md (Animations — shimmer 1.6s linear)
  </read_first>
  <action>
    `lib/features/transcription/transcription_controller.dart`:

    Использовать `ChangeNotifier` (без сторонних state-management библиотек). Контроллер:
    - В конструкторе принимает `ApiKeyRepository` и `GroqApiService` (для тестируемости).
    - `Future<void> start(SelectedAudioFile file)` — оркестрирует: получить ключ → вызвать service → выставить result/error.
    - Состояние выражено через sealed класс `TranscriptionState`: `idle`, `loading`, `success(result)`, `error(message, retryable)`, `missingKey`.

    ```dart
    import 'package:flutter/foundation.dart';

    import '../../core/error/app_exception.dart';
    import '../settings/api_key_repository.dart';
    import 'groq_api_service.dart';
    import 'selected_audio_file.dart';
    import 'transcription_result.dart';

    sealed class TranscriptionState {
      const TranscriptionState();
    }
    class TranscriptionIdle extends TranscriptionState { const TranscriptionIdle(); }
    class TranscriptionLoading extends TranscriptionState { const TranscriptionLoading(); }
    class TranscriptionSuccess extends TranscriptionState {
      final TranscriptionResult result;
      const TranscriptionSuccess(this.result);
    }
    class TranscriptionError extends TranscriptionState {
      final String message;
      final bool retryable;
      const TranscriptionError(this.message, {required this.retryable});
    }
    class TranscriptionMissingKey extends TranscriptionState { const TranscriptionMissingKey(); }

    class TranscriptionController extends ChangeNotifier {
      TranscriptionController({
        required ApiKeyRepository keyRepository,
        required GroqApiService apiService,
      })  : _keys = keyRepository,
            _api = apiService;

      final ApiKeyRepository _keys;
      final GroqApiService _api;

      TranscriptionState _state = const TranscriptionIdle();
      TranscriptionState get state => _state;

      void _set(TranscriptionState s) {
        _state = s;
        notifyListeners();
      }

      /// Запустить транскрибацию. Файл передаётся снаружи (HomeScreen → ProcessingScreen).
      Future<void> start(SelectedAudioFile file) async {
        _set(const TranscriptionLoading());

        final keys = await _keys.listKeys();
        if (keys.isEmpty) {
          _set(const TranscriptionMissingKey());
          return;
        }

        try {
          final result = await _api.transcribe(file: file, apiKey: keys.first.raw);
          _set(TranscriptionSuccess(result));
        } on AuthException catch (e) {
          _set(TranscriptionError(e.message, retryable: false));
        } on NetworkException catch (e) {
          _set(TranscriptionError(e.message, retryable: true));
        } on InternalException catch (e) {
          _set(TranscriptionError(e.message, retryable: true));
        } catch (e) {
          _set(TranscriptionError('Неизвестная ошибка', retryable: true));
        }
      }
    }
    ```

    `lib/ui/widgets/shimmer_bar.dart`:

    Реализовать анимированный shimmer-bar (UI-SPEC Animations: shimmer 1.6s linear).
    - StatefulWidget с `AnimationController` 1600ms, repeat.
    - height: 6 (тонкий бар).
    - borderRadius: pill (9999).
    - LinearGradient с движущимся ярким сегментом (`AppColors.accent` с opacity overlay поверх `AppColors.glassDeep`).
    - Использовать `AnimatedBuilder` + `Transform.translate` для движения градиента.

    Достаточно базовой реализации; в Phase 2 будет настоящий progress с числовым процентом.
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter analyze --no-fatal-infos && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` — 0 ошибок.
    - В `TranscriptionController` 5 состояний (Idle, Loading, Success, Error, MissingKey) — все классы существуют.
    - Контроллер инжектит зависимости через конструктор (тестируемость).
    - `ShimmerBar` использует `AnimationController` (паттерн `AnimationController`).
    - `flutter test` зелёный (unit-тестов для контроллера не требуем в Phase 1 — это можно вынести в Plan 05).
  </acceptance_criteria>
  <done>Контроллер готов оркестрировать ключ + сервис; shimmer widget готов для ProcessingScreen.</done>
</task>

<task type="auto">
  <name>Task 3: ProcessingScreen — pipeline UI, ошибки, переход на Result</name>
  <files>lib/ui/screens/processing_screen.dart</files>
  <read_first>
    - lib/ui/screens/processing_screen.dart (Plan 01 — заглушка)
    - lib/features/transcription/transcription_controller.dart
    - lib/features/transcription/selected_audio_file.dart
    - lib/features/transcription/transcription_result.dart
    - lib/features/settings/api_key_repository.dart
    - lib/features/transcription/groq_api_service.dart
    - lib/ui/widgets/glass_card.dart, lib/ui/widgets/glass_tile.dart, lib/ui/widgets/glass_icon_btn.dart, lib/ui/widgets/primary_button.dart, lib/ui/widgets/shimmer_bar.dart
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md (Screen 2: Processing / Progress, Copywriting Contract — Error messages)
  </read_first>
  <action>
    Полностью переписать `processing_screen.dart`. Принимает `SelectedAudioFile` через `ModalRoute.of(context)!.settings.arguments`.

    Структура:
    ```dart
    class ProcessingScreen extends StatefulWidget {
      const ProcessingScreen({super.key});
      @override
      State<ProcessingScreen> createState() => _ProcessingScreenState();
    }

    class _ProcessingScreenState extends State<ProcessingScreen> {
      late final TranscriptionController _controller;
      SelectedAudioFile? _file;
      DateTime? _startedAt;
      Timer? _ticker;
      Duration _elapsed = Duration.zero;

      @override
      void initState() {
        super.initState();
        _controller = TranscriptionController(
          keyRepository: ApiKeyRepository(SecureStorageServiceImpl()),
          apiService: GroqApiService(),
        );
        _controller.addListener(_onStateChange);
      }

      @override
      void didChangeDependencies() {
        super.didChangeDependencies();
        if (_file == null) {
          final args = ModalRoute.of(context)?.settings.arguments;
          if (args is SelectedAudioFile) {
            _file = args;
            _startedAt = DateTime.now();
            _ticker = Timer.periodic(const Duration(seconds: 1), (_) {
              if (mounted) setState(() => _elapsed = DateTime.now().difference(_startedAt!));
            });
            _controller.start(_file!);
          } else {
            // Защита от прямого открытия маршрута без аргумента
            WidgetsBinding.instance.addPostFrameCallback((_) {
              Navigator.popUntil(context, (r) => r.isFirst);
            });
          }
        }
      }

      void _onStateChange() {
        final s = _controller.state;
        if (s is TranscriptionSuccess) {
          _ticker?.cancel();
          Navigator.pushReplacementNamed(
            context,
            AppConstants.routeResult,
            arguments: _ResultArgs(file: _file!, result: s.result),
          );
        }
        setState(() {}); // перерисовать ошибки/loading state
      }

      @override
      void dispose() {
        _ticker?.cancel();
        _controller.removeListener(_onStateChange);
        _controller.dispose();
        super.dispose();
      }

      // ... build ...
    }
    ```

    UI:
    1. Шапка: `GlassIconBtn(Icons.close, semanticLabel: 'Закрыть', onPressed: () => Navigator.popUntil(context, (r) => r.isFirst))` + Text «Обработка».

    2. Карточка файла (`GlassTile`): иконка wave (56×56 accent gradient), имя файла, mono метаданные `${file.sizeFormatted} · ${file.extension.toUpperCase()}`.

    3. `ShimmerBar` (виден только в loading состоянии).

    4. Pipeline-секция (`GlassCard`):
       - 3 шага: «Загрузка», «Распознавание», «Готово».
       - Активный шаг (определяется по state): accent dot с pulse-анимацией (упрощённо — Container с accent gradient).
       - Прошедшие шаги: good dot (зелёный).
       - Будущие шаги: ink-line outline.
       - Логика активности:
         - state == Loading → активный шаг «Распознавание»; «Загрузка» = done.
         - state == Success → все done (но этот state мгновенно вызывает pushReplacement, пользователь не успевает увидеть).
         - state == Error → активный шаг остаётся на «Распознавание», но красным.

    5. Floating bottom bar:
       - В loading: elapsed time в формате `MM:SS` (mono) + кнопка «Отменить обработку» (`PrimaryButton` с bad-color вариантом — добавить опцию `destructive: true` в PrimaryButton либо использовать `TextButton` с bad-цветом).
       - В error: сообщение об ошибке (label, bad color) + кнопка(и):
         - Если `retryable == true` — «Повторить» (вызывает `_controller.start(_file!)`).
         - Кнопка «Назад» (возврат на Home).
       - В missingKey: сообщение «Сначала добавьте API-ключ в настройках» + кнопка «Перейти в настройки» (`Navigator.pushNamed(context, AppConstants.routeApiKeys)`).

    6. Кнопка «Отменить обработку»: в Phase 1 при нажатии — `Navigator.popUntil(first)` (отмена реального HTTP-запроса требует http.Client.close, что и происходит в finally service — но прерывание in-flight отложено до Phase 2 где есть прогресс).

    Подключить логику elapsed time: `Timer.periodic` обновляющий `_elapsed` каждую секунду.
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter analyze --no-fatal-infos lib/ui/screens/processing_screen.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` — 0 ошибок.
    - В `processing_screen.dart` присутствуют импорты `TranscriptionController`, `ApiKeyRepository`, `GroqApiService`, `SecureStorageServiceImpl`.
    - `grep -c "ModalRoute.of" lib/ui/screens/processing_screen.dart` >= 1 (приём arguments).
    - `grep -c "pushReplacementNamed" lib/ui/screens/processing_screen.dart` >= 1.
    - `grep -c "AppConstants.routeResult" lib/ui/screens/processing_screen.dart` >= 1.
    - В файле есть обработка всех 5 состояний контроллера (Idle, Loading, Success, Error, MissingKey).
    - `flutter test` зелёный.
  </acceptance_criteria>
  <done>ProcessingScreen принимает файл, запускает транскрибацию, показывает прогресс, обрабатывает ошибки, переходит на Result.</done>
</task>

<task type="auto">
  <name>Task 4: HomeScreen — wire «Транскрибировать» button to /processing</name>
  <files>lib/ui/screens/home_screen.dart</files>
  <read_first>
    - lib/ui/screens/home_screen.dart (Plan 03 — заглушка _onTranscribeTap)
    - lib/features/settings/api_key_repository.dart
    - lib/features/transcription/selected_audio_file.dart
  </read_first>
  <action>
    Заменить заглушку `_onTranscribeTap` в HomeScreen.

    Новая логика:
    ```dart
    Future<void> _onTranscribeTap() async {
      if (_selectedFile == null) return;

      // Pre-flight check: есть ли хотя бы один ключ?
      final keys = await ApiKeyRepository(SecureStorageServiceImpl()).listKeys();
      if (!mounted) return;
      if (keys.isEmpty) {
        final goToSettings = await showDialog<bool>(
          context: context,
          builder: (ctx) => AlertDialog(
            title: const Text('Добавьте API-ключ'),
            content: const Text(
              'Для работы нужен ключ Groq. Это бесплатно.',
            ),
            actions: [
              TextButton(
                onPressed: () => Navigator.pop(ctx, false),
                child: const Text('Отмена'),
              ),
              TextButton(
                onPressed: () => Navigator.pop(ctx, true),
                child: const Text('Перейти в настройки'),
              ),
            ],
          ),
        );
        if (goToSettings == true && mounted) {
          Navigator.pushNamed(context, AppConstants.routeApiKeys);
        }
        return;
      }

      Navigator.pushNamed(
        context,
        AppConstants.routeProcessing,
        arguments: _selectedFile,
      );
    }
    ```

    Удалить старый SnackBar-stub. Добавить импорты `ApiKeyRepository`, `SecureStorageServiceImpl`.
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter analyze --no-fatal-infos lib/ui/screens/home_screen.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` — 0 ошибок.
    - `grep -c "pushNamed.*routeProcessing" lib/ui/screens/home_screen.dart` >= 1.
    - `grep -c "arguments: _selectedFile" lib/ui/screens/home_screen.dart` >= 1.
    - `grep -c "ApiKeyRepository" lib/ui/screens/home_screen.dart` >= 1.
    - В файле НЕТ строки «Транскрибация — в следующем плане» (заглушка Plan 03 удалена).
    - `flutter test` зелёный.
  </acceptance_criteria>
  <done>HomeScreen полностью функционален: проверяет ключ, передаёт файл на ProcessingScreen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Groq HTTPS endpoint | Сетевой запрос с API-ключом и аудио-контентом |
| Groq response → app memory | Парсинг внешнего JSON |
| `ModalRoute.arguments` → ProcessingScreen | Передача SelectedAudioFile между экранами |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | MITM к Groq | mitigate | Только HTTPS (`api.groq.com`); URL зафиксирован в `AppConstants.groqApiUrl`; никаких HTTP fallback'ов |
| T-04-02 | Information Disclosure | API-ключ в Authorization header | mitigate | TLS защищает в транспорте; ключ не логируется на клиенте (нет `print(apiKey)`) |
| T-04-03 | Tampering | Подмена ответа Groq | accept | TLS защищает; парсер устойчив к отсутствующим полям (fromJson с дефолтами) |
| T-04-04 | DoS (self) | Большой response body | mitigate | Phase 1 ограничивает размер входного файла (19 MB); response от Whisper пропорционален → реалистичный максимум ~5 MB JSON |
| T-04-05 | Repudiation | Нет server-side логов | accept | Однопользовательский клиент; audit trail не требуется |
| T-04-06 | Information Disclosure | path в multipart body раскрывает структуру FS | accept | Только в сегменте `file` content-disposition; Groq доверенный |
| T-04-07 | Information Disclosure | Логирование request body в случае ошибки | mitigate | `catch` ветки в `GroqApiService` бросают типизированные исключения без `response.body` в message |
</threat_model>

<verification>
- `flutter analyze` — 0 ошибок.
- `flutter test` — все тесты зелёные, включая `groq_service_test.dart` с 5+ кейсами MockClient.
- Ручная верификация на устройстве (опционально для Plan 04, обязательно для Plan 05):
  1. Добавить реальный Groq ключ через Settings.
  2. Выбрать короткий .mp3 файл.
  3. Нажать «Транскрибировать» → ProcessingScreen показывает shimmer и pipeline.
  4. Через 5-30 секунд приложение переходит на ResultScreen с текстом расшифровки.
  5. С невалидным ключом — показывается auth error + кнопка «Назад».
  6. Без подключения — показывается network error + кнопка «Повторить».
</verification>

<success_criteria>
- Все 10 must-haves.truths выполняются.
- TRANS-03 (single-shot < 19 MB) и TRANS-07 (verbose_json + word timestamps) закрыты.
- HTTP-уровень покрыт unit-тестами с MockClient (5+ кейсов).
- Architecture: UI (ProcessingScreen) → Controller → Service → API; разделение ответственности соблюдено.
</success_criteria>

<output>
Создать `.planning/phases/01-walking-skeleton-short-audio-clipboard/01-04-SUMMARY.md`. Включить: список тестов groq_service_test, описание архитектуры (UI ← Controller ← Service), список обработанных HTTP-кодов с поведением UI.
</output>
