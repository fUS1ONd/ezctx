---
phase: 1
slug: walking-skeleton-short-audio-clipboard
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-16
---

# Phase 1 — UI Design Contract: Walking Skeleton (Short Audio → Clipboard)

> Visual and interaction contract for Phase 1. Generated by gsd-ui-researcher.
> Flutter-реализация. Дизайн-источник: `design/screens.jsx` + `design/styles.css` (Liquid Glass).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Flutter custom widgets) |
| Preset | not applicable — Flutter, не web |
| Component library | Flutter Material 3 + кастомные виджеты с `BackdropFilter` |
| Icon library | `lucide_flutter` или `phosphor_flutter` (иконки из прототипа: upload, gear, mic, copy, check, wave, key, refresh, trash, warn, x) |
| Font | System UI (`-apple-system` / `SF Pro`) → на Android: `Roboto` (системный); для Display-заголовков отдельный `fontWeight: 700+` |

> Источник: `design/styles.css` строки 121–134 (font-family declaration), CONTEXT.md D-14/D-15.
> shadcn Gate: не применим — стек Flutter (Dart), не React/Next.js/Vite.

---

## Wallpaper / Background

**Светлая тема (по умолчанию для v1):**

```
Радиальные градиенты поверх линейного градиента:
  radial(60% 45% at 18% 8%)   #ffd2b8 → transparent
  radial(55% 40% at 92% 22%)  #f9c4dd → transparent
  radial(75% 55% at 50% 95%)  #c9bfff → transparent
  radial(45% 35% at 85% 78%)  #ffb39a → transparent
  radial(40% 30% at 8%  60%)  #ffe7a8 → transparent
  linear(180deg)               #fff3ea → #f3ecff
```

Flutter-реализация: `ShaderMask` + `BoxDecoration` с несколькими `RadialGradient`-ами либо предрендеренный `CustomPainter`. Лёгкий шум (0.18 opacity, overlay blend) — опционально, добавить в Phase 7 (Polish).

> Источник: `design/styles.css` строки 41–64.

---

## Glass Surface

Стеклянная поверхность — главный визуальный паттерн всех карточек и контейнеров.

| Свойство | Значение |
|----------|---------|
| Фон | `rgba(255, 255, 255, 0.48)` |
| Blur | `BackdropFilter(filter: ImageFilter.blur(sigmaX: 28, sigmaY: 28), saturation: 1.8)` |
| Верхняя рамка (inner) | `rgba(255, 255, 255, 0.85)` 0.5px |
| Нижняя рамка (inner) | `rgba(255, 255, 255, 0.12)` 0.5px |
| Боковые рамки (inner) | `rgba(255, 255, 255, 0.58)` / `rgba(255, 255, 255, 0.16)` 0.5px |
| Тень | `0 10px 24px rgba(20, 10, 30, 0.10), 0 1px 2px rgba(20, 10, 30, 0.06)` |

Deep-вариант (для полей ввода): фон `rgba(255, 255, 255, 0.66)`.

> Источник: `design/styles.css` строки 76–99.

---

## Spacing Scale

Объявленные значения (кратны 4):

| Токен | Значение | Использование |
|-------|---------|---------------|
| xs | 4px | Иконка ↔ текст (inline gap) |
| sm | 8px | Отступы внутри pill-кнопок, gap между кнопками |
| md | 16px | Padding карточек по горизонтали, отступы между элементами списка |
| lg | 24px | Padding секций, gap между крупными блоками |
| xl | 32px | Верхний отступ заголовка экрана от шапки |
| 2xl | 48px | Вертикальный отступ между секциями на экране |
| 3xl | 64px | Отступ от status bar до контента (62px → округлить до 64px) |

**Исключения:**
- Touch target минимум: **44px** (высота pill-кнопок, icon-кнопок — `GlassIconBtn` 36×36px в прототипе, но touch area 44px).
- Карточка загрузки (upload card) минимальная высота: **260px**.
- Отступ от нижнего края экрана до floating-кнопки: **30px** (из прототипа, соответствует safe area Android).

> Источник: `design/screens.jsx` (inline styles по всем экранам), `design/styles.css`.

---

## Border Radius

| Токен | Значение | Применение |
|-------|---------|-----------|
| `r-card` | 22px | Стеклянные карточки (glass container) |
| `r-row` | 16px | Строки списков, небольшие контейнеры |
| `r-tile` | 30px | Главные hero-тайлы (upload card, api-keys card) |
| `r-pill` | 9999px | Pill-кнопки, badges, progress bar |
| `r-icon` | 11–16px | Иконки в квадратных контейнерах (зависит от размера) |

> Источник: `design/styles.css` строки 4–10, подтверждено в `design/screens.jsx`.

---

## Typography

| Роль | Размер | Вес | Line Height | Применение |
|------|--------|-----|-------------|------------|
| Display | 34–38px | 700–760 | 1.04–1.12 | Заголовки экранов («Слух», «Расшифровки», «Настройки») |
| Heading | 18–26px | 600–700 | 1.12–1.2 | Заголовки карточек, имена файлов в шапке результата |
| Body | 15–16px | 400 | 1.45–1.55 | Основной текст, расшифровка, описания |
| Label / Meta | 11–13px | 400–600 | 1.2–1.4 | Метаданные файла, статусы, таймкоды |

**Шрифтовые классы:**
- `display` (`.display` в прототипе): `fontFamily: system-ui`, `fontWeight: 700+`, `letterSpacing: -0.035em`
- `mono` (`.mono` в прототипе): `fontFamily: monospace` (`JetBrains Mono` → на Android `RobotoMono`), для метаданных файла, таймкодов, размеров
- `body`: системный шрифт, `fontWeight: 400`, `letterSpacing: -0.01em`

> Источник: `design/styles.css` строки 121–138, `design/screens.jsx` (все экраны).

---

## Color

| Роль | Значение | Применение |
|------|---------|-----------|
| Dominant (60%) | `#fff3ea → #f3ecff` (градиент) | Фон-обои, основная поверхность |
| Secondary (30%) | `rgba(255, 255, 255, 0.48)` | Стеклянные карточки, pill-кнопки |
| Accent (10%) | `#ff5b3a` | Зарезервировано: см. ниже |
| Accent-2 | `#ff8a4d` | Только в градиентах с accent (shimmer, кнопки) |
| Good | `#2db585` | Статус «подключено», галочки в progress, badge «готово» |
| Bad / Destructive | `#e0395a` | Ошибки, кнопка «Удалить», badge «Ошибка», cancel-кнопка |
| Text primary (`ink`) | `#1a1421` | Основной текст |
| Text secondary (`ink-2`) | `rgba(26, 20, 33, 0.62)` | Вторичный текст, плейсхолдеры |
| Text tertiary (`ink-3`) | `rgba(26, 20, 33, 0.38)` | Метаданные, неактивные состояния |
| Divider (`ink-line`) | `rgba(26, 20, 33, 0.08)` | Разделители строк списка |

**Accent (#ff5b3a) зарезервирован строго для:**
1. Primary CTA кнопка «Транскрибировать» (gradient: `#ff8a4d → #ff5b3a`)
2. Кнопка «Скопировать» (тот же gradient, до нажатия)
3. Иконка-логотип приложения в шапке
4. Upload-иконка в drop-зоне
5. Shimmer-bar прогресса транскрибации
6. Active badge процесса (wave animation)
7. Dot-indicator активного шага в pipeline

> Источник: `design/styles.css` строки 6–26, `design/screens.jsx` все экраны.

---

## Screens in Phase 1

Phase 1 реализует следующие экраны (минимальный сет):

### Screen 1: Home / Empty State

**Компоненты:**
- Шапка: логотип-иконка (36×36px, gradient `#ff8a4d→#ff5b3a`, border-radius 11px) + название «Слух» (display, 19px) + gear icon button (стекло)
- Display заголовок: «Расшифруй / любой звук» (38px, letterSpacing -0.038em), subtitle 16px ink-2
- Upload card (glass sheen, `r-tile`): иконка upload (72×72px, border-radius 22px, dashed border accent 0.55 opacity), «Выберите файл» (18px w600), форматы (14px ink-2), кнопки «Из файлов»
- Primary CTA: кнопка «Транскрибировать» (заменяет «Записать с микрофона» из прототипа — запись аудио отложена до v2)

> Примечание: В прототипе ScreenEmpty есть кнопка «Записать с микрофона» — она deferred (REC-01 = v2). В Phase 1 эта кнопка ОТСУТСТВУЕТ. Upload card — единственный способ добавить файл.

### Screen 2: Processing / Progress

**Компоненты:**
- Шапка: «Обработка» (heading) + X-кнопка (glass icon btn)
- Карточка файла (glass sheen, `r-tile`): иконка wave (56×56px, gradient), имя файла (16px w600), метаданные mono (13px), прогресс-бар shimmer, «Осталось ~N сек» (12px ink-3)
- Pipeline steps (glass card, `r-card`): 5 шагов с dot-indicators (done=good, active=accent gradient с pulseDot, wait=ink-line border)
- Floating bottom bar (glass sheen, pill): elapsed time (mono) + кнопка «Отменить» (bad color, pill)

> В Phase 1 pipeline упрощён: «Загрузка» → «Распознавание речи» → «Готово» (только 3 шага, нет diarization).

### Screen 3: Result / Detail

**Компоненты:**
- Навигация: back button (glass) + дата (15px ink-2) + share icon (glass, deferred до Phase 5)
- Заголовок: имя файла (display, 26px), метаданные mono (13px ink-2)
- Action row: кнопка «Скопировать» (accent gradient, height 46px, border-radius 14px) + (опционально «Спросить ИИ» — deferred)
- Текст расшифровки (glass card, `r-card`): body 15px, line-height 1.55, ink

### Screen 4: Settings / API Keys

**Компоненты:**
- Заголовок «Настройки» (display, 34px)
- Status card (glass sheen, `r-tile`): аватар-иконка (48×48px), статус «Подключено к Groq», модель + good dot
- Секция «Подключение»: список (glass card) → строка «API-ключи» (detail: «1 активен»)
- API Keys screen: hero-карточка Groq (glass sheen, `r-tile`), поле ввода ключа (glass deep, mono), кнопки «Проверить» / «Удалить», info-блок с ссылкой на console.groq.com

---

## Component Inventory (Flutter Widgets)

| Компонент | Flutter widget | Описание |
|-----------|---------------|----------|
| GlassCard | `ClipRRect` + `BackdropFilter` + `Container` | Стеклянная карточка (r-card 22px) |
| GlassTile | GlassCard с `borderRadius: 30px` | Hero-тайл (upload card, api-keys card) |
| PrimaryButton | `Container` gradient + `InkWell` | Accent gradient кнопка (height 52px, pill) |
| GlassIconBtn | `ClipRRect` + `BackdropFilter` + `IconButton` | Стеклянная icon-кнопка 36×36px |
| GlassPill | `ClipRRect` + `BackdropFilter` + `Text` | Pill-badge / filter chip |
| ShimmerBar | `AnimatedBuilder` + `LinearGradient` | Прогресс-бар с shimmer анимацией |
| WaveDot | `AnimatedBuilder` + `Container` | Активный dot в pipeline |
| ListRow | `ListTile`-замена | Строка настроек с иконкой, title, detail |
| MonoText | `Text` с `fontFamily: 'RobotoMono'` | Метаданные, таймкоды, размеры |
| DisplayText | `Text` с `fontWeight: 700`, `letterSpacing: -0.035` | Заголовки экранов |

---

## Animations

| Анимация | Duration | Curve | Trigger |
|----------|---------|-------|---------|
| pulseDot | 1.2s infinite | ease-in-out | Active pipeline step |
| shimmer | 1.6s infinite | linear | Progress bar during transcription |
| waveBar | 1.2s infinite | ease-in-out | Active badge в истории (deferred Phase 6) |
| Copy → Copied | 1.5s (state) | — | Tap «Скопировать» → fade to `good` color |
| Screen transitions | 300ms | easeInOut | Переходы между экранами |

---

## Copywriting Contract

| Элемент | Текст |
|---------|-------|
| Primary CTA | «Транскрибировать» |
| Upload card heading | «Выберите файл» |
| Upload card subtext | «mp3, wav, m4a, ogg, flac · до 19 МБ» |
| Empty state (нет ключа) | heading: «Добавьте API-ключ», body: «Для работы нужен ключ Groq. Это бесплатно.», CTA: «Перейти в настройки» |
| Error: неподдерживаемый формат | «Формат файла не поддерживается. Выберите mp3, wav, m4a, ogg или flac.» |
| Error: файл > 19 МБ | «Файл слишком большой (максимум 19 МБ). Поддержка больших файлов — в следующем обновлении.» |
| Error: нет сети | «Не удалось подключиться к Groq. Проверьте интернет и попробуйте снова.» |
| Error: неверный ключ (401) | «Ключ не подошёл. Проверьте его в console.groq.com → API Keys.» |
| Кнопка копирования (до) | «Скопировать» |
| Кнопка копирования (после) | «Скопировано» (1.5 сек, затем возврат) |
| Отмена транскрибации | «Отменить» (без подтверждения — данные не теряются, файл не изменяется) |
| Settings: ссылка Groq | «Получить ключ на console.groq.com» |
| API key placeholder | «gsk_••••••••••••••••••••••...» (masked, последние 4 символа видны) |
| Подсказка под ключом | «Ключ хранится в защищённом хранилище устройства и не передаётся на сторонние серверы.» |

**Деструктивные действия в Phase 1:**
- «Удалить ключ» на экране API Keys → подтверждение: dialog «Удалить ключ? Это действие нельзя отменить.» с кнопками «Отмена» / «Удалить» (bad color)

> Источник: `design/screens.jsx` — тексты UI, REQUIREMENTS.md — функциональные требования. Русский язык интерфейса (из прототипа).

---

## Interaction States

### Кнопка «Транскрибировать»

| Состояние | Визуал |
|-----------|--------|
| Default | Gradient `#ff8a4d → #ff5b3a`, shadow `0 8px 18px rgba(255,91,58,0.32)` |
| Pressed | Opacity 0.88, shadow убирается |
| Disabled | Opacity 0.38, gradient серый |
| Loading | Заменяется ShimmerBar или spinner |

### Кнопка «Скопировать»

| Состояние | Визуал |
|-----------|--------|
| Default | Gradient `#ff8a4d → #ff5b3a` |
| Copied (1.5s) | Transition to `#2db585 (good)` + иконка check |
| После 1.5s | Возврат к Default |

### File picker (upload card)

| Состояние | Визуал |
|-----------|--------|
| Default | Dashed border `rgba(255,91,58,0.55)`, иконка upload |
| Pressed / tap | Scale 0.97, subtle shadow |
| File selected | Карточка трансформируется в file preview (имя + размер) |
| Invalid file | Border меняется на `bad (#e0395a)` + error text под карточкой |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Не применимо | Flutter-проект, не web. shadcn/third-party registry не используются. | N/A |

---

## Flutter-Specific Notes

1. **BackdropFilter** требует `ClipRRect` как родителя. Без клипинга blur выходит за границы карточки.
2. **Gradient background** реализуется через `CustomPainter` в `Stack` под контентом экрана.
3. **Letter spacing** в Flutter: `-0.035em` при размере 34px ≈ `letterSpacing: -1.2`.
4. **minSdkVersion**: определяется зависимостями `file_picker` (≥ API 21) и `flutter_secure_storage` (≥ API 23). Установить `minSdkVersion 23`.
5. **Safe area**: используется `SafeArea` widget или отступ `MediaQuery.of(context).padding.top + 16px` для шапки.
6. **Touch targets**: все интерактивные элементы имеют `constraints: BoxConstraints(minWidth: 44, minHeight: 44)`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
