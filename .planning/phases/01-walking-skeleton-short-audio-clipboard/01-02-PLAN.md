---
phase: 01-walking-skeleton-short-audio-clipboard
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - lib/features/settings/api_key_repository.dart
  - lib/ui/screens/api_keys_screen.dart
  - lib/ui/screens/settings_screen.dart
  - test/unit/api_key_repository_test.dart
autonomous: true
requirements:
  - KEYS-01
  - KEYS-02

must_haves:
  truths:
    - "На экране ApiKeysScreen пользователь может ввести строку, нажать «Сохранить», и ключ добавляется в список ключей (KEYS-01)"
    - "Сохранённый ключ переживает полный перезапуск приложения и виден при повторном входе в ApiKeysScreen (KEYS-02 — реальная запись в KeyStore через flutter_secure_storage)"
    - "На экране ApiKeysScreen ключ отображается в маскированном виде: `gsk_••••••••••••••••YXYZ` (последние 4 символа видны)"
    - "В SettingsScreen строка «API-ключи» показывает актуальный счётчик: «N активен» где N — количество сохранённых ключей"
    - "Команда `flutter test test/unit/api_key_repository_test.dart` зелёная — покрывает add/list/remove/idempotency"
    - "В коде проекта отсутствуют любые места, где ключ читается или записывается напрямую через FlutterSecureStorage минуя ApiKeyRepository (single source of truth)"
  artifacts:
    - path: "lib/features/settings/api_key_repository.dart"
      provides: "Repository для CRUD над списком ключей; единственный потребитель SecureStorageService"
      exports: ["ApiKeyRepository"]
    - path: "lib/ui/screens/api_keys_screen.dart"
      provides: "Полноценный UI добавления/удаления/маскирования ключей"
      contains: "ApiKeyRepository"
    - path: "test/unit/api_key_repository_test.dart"
      provides: "Unit тесты для repository (add/list/remove/idempotent)"
      contains: "test('add"
  key_links:
    - from: "lib/ui/screens/api_keys_screen.dart"
      to: "lib/features/settings/api_key_repository.dart"
      via: "конструктор и вызовы add/list/remove"
      pattern: "ApiKeyRepository"
    - from: "lib/features/settings/api_key_repository.dart"
      to: "lib/core/storage/secure_storage_service.dart"
      via: "конструктор принимает SecureStorageService"
      pattern: "SecureStorageService"
    - from: "lib/ui/screens/settings_screen.dart"
      to: "lib/features/settings/api_key_repository.dart"
      via: "загрузка количества ключей для отображения в subtitle"
      pattern: "ApiKeyRepository|listApiKeys"
---

# Plan 02 — API Keys: полноценный CRUD + маскирование + персистентность

<objective>
Заменить skeleton-кнопку «Сохранить тестовое значение» из Plan 01 на полноценный UI управления API-ключами Groq. Доставить вертикальный срез KEYS-01 + KEYS-02:
- Пользователь видит список своих ключей (с маскированием).
- Может добавить новый ключ через TextField + кнопку.
- Может удалить ключ (с подтверждением).
- Все операции идут через `ApiKeyRepository`, который сам использует `SecureStorageService` (single source of truth).
- В SettingsScreen виден актуальный счётчик ключей.

Purpose: Закрыть KEYS-01 и KEYS-02 одним вертикальным срезом UI→repository→storage. Это даёт следующим планам (особенно Plan 04 — Groq HTTP) надёжный способ получить ключ.

Output:
- `ApiKeyRepository` с тестами.
- Обновлённый `ApiKeysScreen` (маскирование, список, добавление, удаление с подтверждением).
- Обновлённый `SettingsScreen` (динамический счётчик).
- Удалены skeleton-методы из API: `writeRawKey/readRawKey/deleteRawKey` из `SecureStorageService` помечены deprecated либо удалены (см. action ниже).
</objective>

<execution_context>
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/workflows/execute-plan.md
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/SKELETON.md
@.planning/phases/01-walking-skeleton-short-audio-clipboard/01-01-SUMMARY.md
@lib/core/storage/secure_storage_service.dart
@lib/ui/screens/api_keys_screen.dart
@lib/ui/screens/settings_screen.dart
@lib/ui/widgets/glass_card.dart
@lib/ui/widgets/primary_button.dart
@lib/core/constants/design_tokens.dart
</context>

<interfaces>
Из Plan 01 уже доступны:

```dart
// lib/core/storage/secure_storage_service.dart
abstract interface class SecureStorageService {
  Future<List<String>> listApiKeys();
  Future<void> addApiKey(String key);
  Future<void> removeApiKey(String key);

  // SKELETON-API (Plan 02 удаляет вызовы из ApiKeysScreen; методы можно оставить для тестов или удалить)
  Future<void> writeRawKey(String value);
  Future<String?> readRawKey();
  Future<void> deleteRawKey();
}

class SecureStorageServiceImpl implements SecureStorageService { ... }
```

```dart
// lib/core/constants/app_constants.dart
class AppConstants {
  static const String storageKeyApiKeys = 'groq_api_keys_v1';
  // ... другие ...
}
```

```dart
// lib/core/error/app_exception.dart
class ValidationException extends AppException { ... }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ApiKeyRepository + tests (validation, add, remove, list, idempotency)</name>
  <files>lib/features/settings/api_key_repository.dart, test/unit/api_key_repository_test.dart</files>
  <read_first>
    - lib/core/storage/secure_storage_service.dart
    - lib/core/error/app_exception.dart
    - lib/core/constants/app_constants.dart
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-RESEARCH.md (Security Domain)
  </read_first>
  <behavior>
    - listKeys() возвращает Future&lt;List&lt;ApiKeyView&gt;&gt; — список объектов с полным значением и маскированным представлением.
    - addKey('gsk_validkey123456789012345678901234567890') — успешно сохраняет, listKeys() возвращает 1 элемент.
    - addKey('') бросает ValidationException('Ключ не может быть пустым').
    - addKey('shortkey') бросает ValidationException — минимальная длина 20 символов (защитная мера от случайных строк; реальные Groq ключи 50+ символов).
    - addKey('  gsk_padded  ') нормализует whitespace перед сохранением (trim).
    - addKey(того же значения дважды) не дублирует — список содержит 1 элемент (идемпотентность из SecureStorageService).
    - removeKey('gsk_xxx') удаляет конкретный ключ; listKeys() более не содержит его.
    - maskedValue для 'gsk_abcdefghijklmnopqrstuvWXYZ1234' возвращает 'gsk_••••••••••••••••1234' (4 видимых символа в конце, 16 точек посередине).
    - maskedValue для слишком короткого ключа (&lt;8 символов) возвращает строку из 8 точек (fallback на отсутствие маскирования — защита от утечки).
  </behavior>
  <action>
    Создать `lib/features/settings/api_key_repository.dart` — фасад над `SecureStorageService` с валидацией и маскированием.

    ```dart
    import '../../core/error/app_exception.dart';
    import '../../core/storage/secure_storage_service.dart';

    /// View-модель ключа: полное значение + готовое для UI маскированное представление.
    class ApiKeyView {
      final String raw;
      final String masked;
      const ApiKeyView({required this.raw, required this.masked});
    }

    /// Repository для управления Groq API-ключами.
    /// Единственный потребитель SecureStorageService в feature-слое.
    class ApiKeyRepository {
      ApiKeyRepository(this._storage);

      final SecureStorageService _storage;

      // Минимальная длина ключа — защита от случайного ввода случайных букв.
      // Реальные ключи Groq начинаются с 'gsk_' и имеют длину ~56 символов.
      static const int _minKeyLength = 20;

      Future<List<ApiKeyView>> listKeys() async {
        final raw = await _storage.listApiKeys();
        return raw.map((k) => ApiKeyView(raw: k, masked: mask(k))).toList();
      }

      Future<void> addKey(String value) async {
        final trimmed = value.trim();
        if (trimmed.isEmpty) {
          throw const ValidationException('Ключ не может быть пустым');
        }
        if (trimmed.length < _minKeyLength) {
          throw const ValidationException(
            'Ключ слишком короткий (минимум $_minKeyLength символов)',
          );
        }
        await _storage.addApiKey(trimmed);
      }

      Future<void> removeKey(String value) async {
        await _storage.removeApiKey(value);
      }

      /// Маскирует ключ: первые `прим. 4` НЕ показываем (могут утечь префиксом провайдера),
      /// последние 4 показываем для идентификации; середина — 16 точек.
      static String mask(String key) {
        if (key.length < 8) {
          return '•' * 8;
        }
        final tail = key.substring(key.length - 4);
        return '${'•' * 16}$tail';
      }
    }
    ```

    Тесты в `test/unit/api_key_repository_test.dart` — использовать Fake реализацию `SecureStorageService` (ручной, без mockito — быстрее):

    ```dart
    import 'package:ezctx/core/error/app_exception.dart';
    import 'package:ezctx/core/storage/secure_storage_service.dart';
    import 'package:ezctx/features/settings/api_key_repository.dart';
    import 'package:flutter_test/flutter_test.dart';

    class _FakeStorage implements SecureStorageService {
      final List<String> _keys = [];

      @override
      Future<List<String>> listApiKeys() async => List.unmodifiable(_keys);

      @override
      Future<void> addApiKey(String key) async {
        if (!_keys.contains(key)) _keys.add(key);
      }

      @override
      Future<void> removeApiKey(String key) async => _keys.remove(key);

      @override
      Future<void> writeRawKey(String value) async {}
      @override
      Future<String?> readRawKey() async => null;
      @override
      Future<void> deleteRawKey() async {}
    }

    void main() {
      late ApiKeyRepository repo;
      late _FakeStorage storage;

      setUp(() {
        storage = _FakeStorage();
        repo = ApiKeyRepository(storage);
      });

      group('addKey', () {
        test('добавляет валидный ключ', () async {
          await repo.addKey('gsk_' + 'a' * 30);
          final keys = await repo.listKeys();
          expect(keys.length, 1);
        });

        test('пустая строка → ValidationException', () async {
          expect(() => repo.addKey(''), throwsA(isA<ValidationException>()));
        });

        test('короткий ключ → ValidationException', () async {
          expect(() => repo.addKey('short'), throwsA(isA<ValidationException>()));
        });

        test('trim пробелов перед сохранением', () async {
          await repo.addKey('  gsk_' + 'x' * 30 + '  ');
          final keys = await repo.listKeys();
          expect(keys.first.raw.startsWith(' '), isFalse);
          expect(keys.first.raw.endsWith(' '), isFalse);
        });

        test('идемпотентность — дубликат не добавляется', () async {
          final k = 'gsk_' + 'y' * 30;
          await repo.addKey(k);
          await repo.addKey(k);
          final keys = await repo.listKeys();
          expect(keys.length, 1);
        });
      });

      group('removeKey', () {
        test('удаляет указанный ключ', () async {
          final k = 'gsk_' + 'z' * 30;
          await repo.addKey(k);
          await repo.removeKey(k);
          final keys = await repo.listKeys();
          expect(keys, isEmpty);
        });
      });

      group('mask', () {
        test('последние 4 символа видны, середина точками', () {
          final masked = ApiKeyRepository.mask('gsk_abcdefghijklmnopqrstuvWXYZ1234');
          expect(masked.endsWith('1234'), isTrue);
          expect(masked.contains('•'), isTrue);
          expect(masked.contains('gsk_'), isFalse); // префикс скрыт
        });

        test('слишком короткий ключ полностью скрыт', () {
          expect(ApiKeyRepository.mask('abc'), '••••••••');
        });
      });
    }
    ```
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter test test/unit/api_key_repository_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/unit/api_key_repository_test.dart` зелёный.
    - Минимум 8 `test(...)` блоков, все passing (никаких `skip`).
    - В `api_key_repository.dart` отсутствует `print(` (security — не логировать ключи).
    - `ApiKeyRepository.mask` корректно скрывает префикс (тест проверяет).
  </acceptance_criteria>
  <done>Repository готов с валидацией, тесты зелёные, маскирование верифицировано.</done>
</task>

<task type="auto">
  <name>Task 2: Обновлённый ApiKeysScreen (список + добавление + удаление + маскирование)</name>
  <files>lib/ui/screens/api_keys_screen.dart</files>
  <read_first>
    - lib/ui/screens/api_keys_screen.dart (Plan 01 — skeleton реализация)
    - lib/features/settings/api_key_repository.dart
    - lib/ui/widgets/glass_card.dart, lib/ui/widgets/glass_tile.dart, lib/ui/widgets/primary_button.dart, lib/ui/widgets/glass_icon_btn.dart
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md (Screen 4: Settings / API Keys, Copywriting Contract, Interaction States)
    - design/screens.jsx (ApiKeysScreen визуальная справка)
  </read_first>
  <action>
    Полностью переписать `lib/ui/screens/api_keys_screen.dart`. Удалить всё что было от Plan 01 (skeleton кнопка «Сохранить тестовое значение»).

    Структура `ApiKeysScreen` (StatefulWidget):

    Состояние:
    - `List<ApiKeyView> _keys = []`
    - `bool _loading = true`
    - `String? _errorMessage`
    - `final TextEditingController _inputController`
    - `bool _saving = false`

    `initState` → `_loadKeys()`:
    - `_loading = true`
    - `_keys = await ApiKeyRepository(SecureStorageServiceImpl()).listKeys()`
    - `_loading = false`

    UI компоновка (внутри `Scaffold` с `GradientBackground` + `SafeArea`):

    1. Шапка `Row`: `GlassIconBtn(icon: Icons.arrow_back, semanticLabel: 'Назад', onPressed: () => Navigator.pop(context))` + Text «API-ключи» (heading).

    2. Hero-карточка `GlassTile`: иконка `Icons.key_outlined` (48×48 в gradient контейнере), Text «Groq API» (heading), Text «Ключи хранятся в защищённом хранилище устройства» (label).

    3. Список ключей `_buildKeysList()`:
       - Если `_loading == true` → центрированный `CircularProgressIndicator`.
       - Если `_keys.isEmpty` → Text «Нет добавленных ключей» (label, ink-3).
       - Иначе `Column` с каждой строкой:
         - `GlassCard` (r-card 22) с Row:
           - Иконка `Icons.vpn_key_outlined` (24×24, ink-2)
           - Text(maskedValue) — `AppTextStyles.mono`
           - Spacer
           - `GlassIconBtn(icon: Icons.delete_outline, semanticLabel: 'Удалить ключ', onPressed: () => _confirmDelete(key))` — `iconColor: AppColors.bad`

    4. Форма добавления (`GlassCard` deep variant с `AppColors.glassDeep`):
       - TextField — `controller: _inputController`, placeholder «gsk_••••••••••••••••••••••...», `style: AppTextStyles.mono`, `obscureText: false` (показываем, пока пользователь набирает; маскирование — после сохранения), `keyboardType: TextInputType.visiblePassword`, `autocorrect: false`, `enableSuggestions: false`.
       - Если `_errorMessage != null` — Text(`_errorMessage`, color: AppColors.bad, label).
       - `PrimaryButton(label: _saving ? 'Сохранение...' : 'Добавить ключ', onPressed: _saving ? null : _onAddPressed)`.

    5. Footer: Text-link «Получить ключ на console.groq.com» (label, accent цвет). При нажатии: пока — `SnackBar` «Откройте https://console.groq.com/keys в браузере» (полноценный url_launcher отложен — пакета нет в зависимостях Phase 1).

    Методы:
    ```dart
    Future<void> _onAddPressed() async {
      setState(() { _saving = true; _errorMessage = null; });
      try {
        final repo = ApiKeyRepository(SecureStorageServiceImpl());
        await repo.addKey(_inputController.text);
        _inputController.clear();
        await _loadKeys();
        if (!mounted) return;
        ScaffoldMessenger.of(context).showSnackBar(
          const SnackBar(content: Text('Ключ сохранён')),
        );
      } on ValidationException catch (e) {
        setState(() { _errorMessage = e.message; });
      } catch (e) {
        setState(() { _errorMessage = 'Не удалось сохранить ключ'; });
      } finally {
        if (mounted) setState(() { _saving = false; });
      }
    }

    Future<void> _confirmDelete(ApiKeyView key) async {
      final confirmed = await showDialog<bool>(
        context: context,
        builder: (ctx) => AlertDialog(
          title: const Text('Удалить ключ?'),
          content: const Text('Это действие нельзя отменить.'),
          actions: [
            TextButton(onPressed: () => Navigator.pop(ctx, false), child: const Text('Отмена')),
            TextButton(
              onPressed: () => Navigator.pop(ctx, true),
              style: TextButton.styleFrom(foregroundColor: AppColors.bad),
              child: const Text('Удалить'),
            ),
          ],
        ),
      );
      if (confirmed == true) {
        final repo = ApiKeyRepository(SecureStorageServiceImpl());
        await repo.removeKey(key.raw);
        await _loadKeys();
      }
    }
    ```

    > Внимание: `Repository` инстанцируется на месте через `SecureStorageServiceImpl()` — это упрощение для Phase 1 (нет DI). В Phase 6+ при необходимости можно вынести через `InheritedWidget`/`Provider`. Не усложнять сейчас.

    Удалить из этого файла любые ссылки на `writeRawKey`/`readRawKey`/`deleteRawKey` и связанные TODO-комментарии Plan 02 (теперь они implementation).
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter analyze --no-fatal-infos lib/ui/screens/api_keys_screen.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze lib/ui/screens/api_keys_screen.dart` — 0 ошибок.
    - В файле есть ровно один импорт `ApiKeyRepository`.
    - В файле НЕТ строк `writeRawKey`, `readRawKey`, `deleteRawKey` (skeleton API не используется).
    - В файле НЕТ TODO-комментариев с упоминанием Plan 02 (выполнен).
    - `grep -c "showDialog" lib/ui/screens/api_keys_screen.dart` >= 1 (подтверждение удаления — UI-SPEC «Деструктивные действия»).
    - `grep -c "ApiKeyRepository" lib/ui/screens/api_keys_screen.dart` >= 2 (импорт + использование).
    - `flutter test` остаётся зелёным.
  </acceptance_criteria>
  <done>UI ApiKeysScreen полностью функциональный: добавление, отображение с маскированием, удаление с подтверждением.</done>
</task>

<task type="auto">
  <name>Task 3: SettingsScreen счётчик ключей + integration verify</name>
  <files>lib/ui/screens/settings_screen.dart</files>
  <read_first>
    - lib/ui/screens/settings_screen.dart (Plan 01 — заглушка с subtitle «Нет ключей»)
    - lib/features/settings/api_key_repository.dart
    - .planning/phases/01-walking-skeleton-short-audio-clipboard/01-UI-SPEC.md (Screen 4: Settings)
  </read_first>
  <action>
    Превратить `SettingsScreen` в `StatefulWidget`. В `initState` подгрузить количество ключей через `ApiKeyRepository.listKeys()`, при возврате с ApiKeysScreen — перезагрузить (используя `didChangeDependencies` либо ожидая результат от `Navigator.push().then(...)`).

    Изменения:
    - Преобразовать в `StatefulWidget`.
    - Состояние: `int _keyCount = 0; bool _loading = true;`.
    - `initState` → `_load()`: `final keys = await ApiKeyRepository(SecureStorageServiceImpl()).listKeys(); setState(() { _keyCount = keys.length; _loading = false; });`.
    - Строка «API-ключи»:
      - Subtitle: `_loading ? '...' : (_keyCount == 0 ? 'Нет ключей' : '$_keyCount активен')`.
      - При выборе строки: `await Navigator.pushNamed(context, AppConstants.routeApiKeys); _load();` — гарантированный refresh после возврата.

    Дополнительно: добавить вверху экрана display-заголовок «Настройки» как описано в UI-SPEC Screen 4 (был упущён в Plan 01).
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && flutter analyze --no-fatal-infos && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` — 0 ошибок.
    - В файле присутствует импорт `ApiKeyRepository`.
    - `grep -c "Navigator.pushNamed.*routeApiKeys" lib/ui/screens/settings_screen.dart` >= 1.
    - `grep -c "listKeys" lib/ui/screens/settings_screen.dart` >= 1.
    - В UI присутствует Text/Widget с `'$_keyCount активен'` или эквивалент с интерполяцией счётчика.
    - `flutter test` зелёный.
  </acceptance_criteria>
  <done>SettingsScreen показывает актуальный счётчик ключей, обновляется при возврате из ApiKeysScreen.</done>
</task>

<task type="auto">
  <name>Task 4: Single source of truth audit + skeleton API cleanup</name>
  <files>lib/core/storage/secure_storage_service.dart</files>
  <read_first>
    - lib/core/storage/secure_storage_service.dart
    - test/unit/secure_storage_test.dart
  </read_first>
  <action>
    Аудит и очистка после удаления skeleton-кнопки:

    1. В `SecureStorageServiceImpl`: пометить методы `writeRawKey/readRawKey/deleteRawKey` как `@Deprecated('Skeleton API — удалить в Plan 03 если нет потребителей; использовать addApiKey/listApiKeys/removeApiKey через ApiKeyRepository')`.

    2. Запустить `grep -rn "writeRawKey\|readRawKey\|deleteRawKey" lib/` — должен показать ТОЛЬКО объявления в `secure_storage_service.dart`, никаких вызовов из feature-кода. Если есть вызовы — это баг Plan 02 Task 2/3, починить.

    3. Запустить `grep -rn "FlutterSecureStorage" lib/` — должен показать ТОЛЬКО конструктор в `SecureStorageServiceImpl`. Никакой другой файл не должен использовать `FlutterSecureStorage` напрямую. Если есть — это нарушение single-source-of-truth, починить через `ApiKeyRepository`.

    4. Тесты `test/unit/secure_storage_test.dart` оставить без изменений — они покрывают и low-level и high-level API.

    Записать результаты аудита в task summary (что нашлось / не нашлось).
  </action>
  <verify>
    <automated>cd /home/krivonosov/projects/ezctx && (grep -rn "FlutterSecureStorage" lib/ | grep -v "secure_storage_service.dart" | wc -l | xargs -I{} test {} -eq 0) && (grep -rn "writeRawKey\|readRawKey\|deleteRawKey" lib/ | grep -v "secure_storage_service.dart" | wc -l | xargs -I{} test {} -eq 0) && flutter analyze --no-fatal-infos && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "FlutterSecureStorage" lib/ | grep -v "secure_storage_service.dart"` — пусто.
    - `grep -rn "writeRawKey\|readRawKey\|deleteRawKey" lib/ | grep -v "secure_storage_service.dart"` — пусто.
    - Skeleton-методы помечены `@Deprecated(...)` в `secure_storage_service.dart`.
    - `flutter analyze` — 0 ошибок (deprecated warnings допустимы только если deprecated методы НЕ вызываются нигде в lib/ — что и проверяется выше).
    - `flutter test` зелёный.
  </acceptance_criteria>
  <done>Single source of truth подтверждён: только ApiKeyRepository работает с ключами; skeleton API изолирован.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → TextField | Пользователь вводит произвольный текст — потенциально не Groq-ключ |
| App → flutter_secure_storage | Через ApiKeyRepository (единственный путь) |
| App memory → UI | Полное значение ключа (raw) присутствует в state ApiKeysScreen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Ключ в UI отображается полностью | mitigate | После сохранения в storage показывается ТОЛЬКО маскированное значение (ApiKeyRepository.mask); raw не показывается; TextField очищается после save |
| T-02-02 | Information Disclosure | Ключ в логах/стеке | mitigate | ApiKeyRepository не содержит `print(`; catch блоки логируют только тип исключения, не значение |
| T-02-03 | Tampering | Ввод произвольной строки | mitigate | ValidationException на пустую и слишком короткую строку; trim перед сохранением |
| T-02-04 | Spoofing | Несколько мест работы с ключами | mitigate | Task 4 — аудит подтверждает что FlutterSecureStorage используется только в SecureStorageServiceImpl |
| T-02-05 | DoS | Очень длинный ключ → crash | accept | Длина ключей Groq стабильна ~56 символов; верхний лимит не вводим (хранилище справится с разумными размерами) |
| T-02-06 | Information Disclosure | Случайное логирование `_inputController.text` | mitigate | Поиск `print(_inputController` и `print(.*controller.text` в lib/ — отсутствуют (verify) |
</threat_model>

<verification>
- `flutter analyze` — 0 ошибок.
- `flutter test` — все unit тесты зелёные (включая api_key_repository_test.dart с 8+ кейсами).
- Ручная проверка на устройстве (опционально, не блокирующая): добавить ключ → перезапустить приложение → ключ остался → удалить ключ → подтверждение → пропал.
- Аудит single-source-of-truth (grep) пуст.
</verification>

<success_criteria>
- Все 6 must-haves.truths выполняются.
- KEYS-01 и KEYS-02 закрыты вертикальным срезом UI → Repository → Storage.
- В кодовой базе нет нарушений single-source-of-truth для ключей.
</success_criteria>

<output>
Создать `.planning/phases/01-walking-skeleton-short-audio-clipboard/01-02-SUMMARY.md`. Включить: список тест-кейсов в `api_key_repository_test.dart`, результаты grep-аудита, скриншот (если делался) экрана ApiKeysScreen.
</output>
