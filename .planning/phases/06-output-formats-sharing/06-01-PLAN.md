---
phase: "06"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/transcription/transcript_writer.dart
  - lib/ui/screens/result_screen.dart
  - test/features/transcription/transcript_writer_srt_test.dart
autonomous: true
requirements:
  - OUT-01

must_haves:
  truths:
    - "По завершении транскрибации файл transcript.srt появляется в app documents/transcripts/"
    - "SRT содержит корректные таймкоды с запятой как разделителем миллисекунд"
    - "При пустых segments writeSrt() возвращает null и файл не создаётся"
    - "Существующая запись transcript.txt не нарушена"
  artifacts:
    - path: "lib/features/transcription/transcript_writer.dart"
      provides: "writeSrt() метод, генерирующий SRT из TranscriptionSegment[]"
      contains: "writeSrt"
    - path: "lib/ui/screens/result_screen.dart"
      provides: "_saveTranscripts() — расширенная версия, вызывает writeBoth() + writeSrt()"
      contains: "_saveTranscripts"
    - path: "test/features/transcription/transcript_writer_srt_test.dart"
      provides: "Unit-тесты OUT-01: форматирование, таймкоды, пустые segments"
      exports: []
  key_links:
    - from: "lib/ui/screens/result_screen.dart"
      to: "lib/features/transcription/transcript_writer.dart"
      via: "TranscriptWriter().writeSrt()"
      pattern: "writeSrt"
    - from: "lib/features/transcription/transcript_writer.dart"
      to: "dart:io File"
      via: "file.writeAsString()"
      pattern: "writeAsString"
---

<objective>
Добавить генерацию SRT-субтитров: расширить `TranscriptWriter` методом `writeSrt()`, переименовать `_saveTranscriptTxt()` в `_saveTranscripts()` и вызвать оба метода при открытии `ResultScreen`. Покрыть логику unit-тестами.

Purpose: Требование OUT-01 — по завершении транскрибации в хранилище появляется `transcript.srt` с корректными таймкодами.
Output: `transcript_writer.dart` с `writeSrt()`, обновлённый `result_screen.dart`, тест `transcript_writer_srt_test.dart`.
</objective>

<execution_context>
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/workflows/execute-plan.md
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-output-formats-sharing/06-RESEARCH.md

<interfaces>
<!-- Ключевые типы, которые использует этот план. -->

Из lib/features/transcription/transcription_result.dart:
```dart
class TranscriptionSegment {
  final double start; // абсолютные секунды (offset чанка уже учтён)
  final double end;
  final String text;
}

class TranscriptionResult {
  final String text;           // с [HH:MM:SS] таймкодами
  final String plainText;      // без таймкодов
  final List<TranscriptionSegment> segments; // может быть const []
}
```

Из lib/features/transcription/transcript_writer.dart:
```dart
class TranscriptWriter {
  const TranscriptWriter();

  Future<String> writeTxt({required String baseName, required String text}) async {...}

  Future<({String plainPath, String timestampedPath})> writeBoth({
    required String baseName,
    required String plainText,
    required String timestampedText,
  }) async {...}

  static String _sanitize(String name) {...} // удаляет расширение, заменяет небезопасные символы
}
```

Из lib/ui/screens/result_screen.dart:
```dart
class _ResultScreenState extends State<ResultScreen> {
  ResultArgs? _args;
  String? _savedPath;

  // Вызывается из didChangeDependencies() после установки _args
  Future<void> _saveTranscriptTxt() async { ... } // ПЕРЕИМЕНОВАТЬ в _saveTranscripts()

  String get _currentText { ... } // возвращает текст в текущем режиме
}
```

Из lib/features/transcription/result_args.dart (через _args):
```dart
class ResultArgs {
  final SelectedAudioFile file; // file.name — имя исходного файла
  final TranscriptionResult result;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Добавить writeSrt() в TranscriptWriter + unit-тесты</name>
  <files>
    lib/features/transcription/transcript_writer.dart,
    test/features/transcription/transcript_writer_srt_test.dart
  </files>
  <behavior>
    - writeSrt([seg1, seg2]) создаёт файл по пути `&lt;app docs&gt;/transcripts/&lt;baseName&gt;.srt`
    - Первый блок начинается с "1" (нумерация с 1, не с 0)
    - Таймкод формат: "00:00:01,500 --> 00:00:04,200" (запятая, не точка)
    - Последний блок заканчивается пустой строкой (стандарт SRT)
    - writeSrt(segments: []) возвращает null, файл не создаётся
    - _srtTime(0.0) → "00:00:00,000"
    - _srtTime(3661.5) → "01:01:01,500"
    - SRT не содержит точку в таймкодах (проверять !srt.contains('.'))
  </behavior>
  <action>
    Шаг 1 (RED): создать `test/features/transcription/transcript_writer_srt_test.dart` с тестами для всех поведений выше. Запустить — убедиться что тесты падают (writeSrt не существует).

    Шаг 2 (GREEN): добавить в `lib/features/transcription/transcript_writer.dart` методы `writeSrt()` и приватные `_segmentsToSrt()` / `_srtTime()`.

    Реализация `writeSrt()` (per OUT-01):
    - принимает `{required String baseName, required List&lt;TranscriptionSegment&gt; segments}`
    - если `segments.isEmpty` — вернуть `null`, файл не создавать
    - получить директорию через `getApplicationDocumentsDirectory()`, создать `transcripts/` если нет
    - путь файла: `$safe.srt` где `safe = _sanitize(baseName)` (использовать существующий _sanitize)
    - записать через `file.writeAsString(_segmentsToSrt(segments), flush: true)`
    - вернуть `file.path`

    Реализация `_segmentsToSrt()`:
    - `StringBuffer`, нумерация с 1 (`i + 1`)
    - каждый блок: `$index\n$startTime --> $endTime\n${seg.text.trim()}\n\n`
    - `_srtTime(double totalSeconds)`: `ms = (totalSeconds * 1000).round()`, затем h/m/s/millis через `~/` и `%`, результат `HH:MM:SS,mmm` с padLeft

    НЕ использовать `Duration.toString()` — даёт точку вместо запятой и неверный формат.
    НЕ вычислять offset заново — `segments[i].start/end` уже абсолютные (с учётом чанков).

    Шаг 3 (GREEN verify): убедиться что все тесты проходят.

    Для тестирования `writeSrt()` с реальной файловой системой использовать `setUpAll` с `PathProviderPlatform.instance = FakePathProviderPlatform()` из `path_provider_platform_interface` или просто проверить `_segmentsToSrt()` и `_srtTime()` через статические вызовы (сделать `_segmentsToSrt` и `_srtTime` package-private `@visibleForTesting` или тестировать через выход `writeSrt` на реальной tmp директории с `io.Directory.systemTemp`).

    Предпочтительный подход для тестов: тестировать `_srtTime` и `_segmentsToSrt` как `static` через вспомогательный публичный метод `TranscriptWriter.formatSrt(segments)` возвращающий String, или сделать `_segmentsToSrt` / `_srtTime` `@visibleForTesting static`.
  </action>
  <verify>
    <automated>cd /root/projects/ezctx && /opt/flutter/bin/flutter test test/features/transcription/transcript_writer_srt_test.dart --no-pub</automated>
  </verify>
  <done>
    Все тесты в transcript_writer_srt_test.dart проходят. writeSrt() присутствует в transcript_writer.dart. Таймкоды используют запятую. Пустые segments возвращают null.
  </done>
</task>

<task type="auto">
  <name>Task 2: Переименовать _saveTranscriptTxt → _saveTranscripts, вызвать writeSrt()</name>
  <files>
    lib/ui/screens/result_screen.dart
  </files>
  <action>
    Прочитать `lib/ui/screens/result_screen.dart` перед правкой (уже прочитан в контексте).

    Изменения в `_ResultScreenState`:

    1. Переименовать метод `_saveTranscriptTxt()` → `_saveTranscripts()`.
    2. Обновить вызов в `didChangeDependencies()`: `_saveTranscriptTxt()` → `_saveTranscripts()`.
    3. Внутри `_saveTranscripts()`, после вызова `writeBoth(...)`, добавить вызов `writeSrt()`:

    ```
    await const TranscriptWriter().writeSrt(
      baseName: _args!.file.name,
      segments: _args!.result.segments,
    );
    ```

    Вызов `writeSrt()` идёт внутри существующего `try` блока. Ошибка логируется через `debugPrint` — не пробрасывается (SRT — дополнительный артефакт, не обязательный для работы экрана).

    Импорты не меняются — `TranscriptWriter` уже импортирован.

    Комментарий к строке: `// OUT-01: генерируем SRT если есть сегменты с таймкодами`

    `_savedPath` по-прежнему указывает на папку (не меняется) — пользователь увидит «Сохранено: /data/.../transcripts» без изменений.
  </action>
  <verify>
    <automated>cd /root/projects/ezctx && /opt/flutter/bin/flutter analyze lib/ui/screens/result_screen.dart --no-pub 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    `_saveTranscripts()` вызывает и `writeBoth()`, и `writeSrt()`. `didChangeDependencies()` вызывает `_saveTranscripts()`. `flutter analyze` не выдаёт ошибок.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Файловая система → TranscriptWriter | baseName приходит из имени файла, выбранного пользователем |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | TranscriptWriter._sanitize() | mitigate | `_sanitize()` уже заменяет небезопасные символы в baseName — путь traversal невозможен; writeSrt использует тот же _sanitize |
| T-06-02 | Information Disclosure | transcript.srt в app documents | accept | Файл хранится в app-private storage (не доступен другим приложениям без root); пользователь явно инициирует share |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Этот план не устанавливает новых пакетов — share_plus добавляется в Plan 02 |
</threat_model>

<verification>
После выполнения обоих задач:

1. `flutter test test/features/transcription/transcript_writer_srt_test.dart` — все тесты проходят.
2. `flutter analyze lib/features/transcription/transcript_writer.dart lib/ui/screens/result_screen.dart` — нет ошибок.
3. Файл `transcript_writer.dart` содержит `writeSrt` (grep -c writeSrt — не менее 1).
4. Файл `result_screen.dart` содержит `_saveTranscripts` и не содержит `_saveTranscriptTxt` (переименован).
</verification>

<success_criteria>
- `writeSrt()` добавлен в `TranscriptWriter` и принимает `List&lt;TranscriptionSegment&gt;`
- Таймкоды генерируются в формате `HH:MM:SS,mmm` (запятая, не точка)
- При пустых segments возвращает `null` без создания файла
- `_saveTranscripts()` вызывает `writeBoth()` + `writeSrt()` последовательно
- `ResultScreen.didChangeDependencies()` вызывает `_saveTranscripts()`
- Все тесты в `transcript_writer_srt_test.dart` проходят
</success_criteria>

<output>
Создать `.planning/phases/06-output-formats-sharing/06-01-SUMMARY.md` по завершении
</output>
