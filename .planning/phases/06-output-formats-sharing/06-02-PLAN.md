---
phase: "06"
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - pubspec.yaml
  - lib/ui/screens/result_screen.dart
  - test/widget/result_screen_test.dart
autonomous: true
requirements:
  - OUT-04

must_haves:
  truths:
    - "На ResultScreen видна кнопка «Поделиться» под кнопкой «Скопировать»"
    - "Tap «Поделиться» открывает системный share-sheet Android с текстом расшифровки"
    - "Кнопка «Скопировать» и «Поделиться» видны одновременно, без повторной транскрибации"
    - "При ошибке share показывается SnackBar «Ошибка общего доступа: {error}»"
  artifacts:
    - path: "pubspec.yaml"
      provides: "share_plus: ^13.1.0 в dependencies"
      contains: "share_plus"
    - path: "lib/ui/screens/result_screen.dart"
      provides: "_onShareTap() + PrimaryButton «Поделиться» + import share_plus"
      contains: "_onShareTap"
    - path: "test/widget/result_screen_test.dart"
      provides: "Тест наличия кнопки «Поделиться» на ResultScreen"
      exports: []
  key_links:
    - from: "lib/ui/screens/result_screen.dart"
      to: "share_plus SharePlus.instance.share()"
      via: "_onShareTap()"
      pattern: "SharePlus.instance.share"
---

<objective>
Добавить кнопку «Поделиться» на ResultScreen: установить пакет `share_plus`, реализовать `_onShareTap()` через `SharePlus.instance.share(ShareParams(text: _currentText))`, разместить кнопку по UI-спеку, покрыть widget-тестом.

Purpose: Требование OUT-04 — пользователь может отправить расшифровку в Telegram/GPT/заметки одним тапом без повторной транскрибации.
Output: обновлённые `pubspec.yaml`, `result_screen.dart`, расширенный `result_screen_test.dart`.
</objective>

<execution_context>
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/workflows/execute-plan.md
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-output-formats-sharing/06-RESEARCH.md
@.planning/phases/06-output-formats-sharing/06-UI-SPEC.md

<interfaces>
<!-- Ключевые контракты для этого плана. -->

share_plus 13.1.0 API (pub.dev verified):
```dart
import 'package:share_plus/share_plus.dart';

// Текущий API (не deprecated):
await SharePlus.instance.share(ShareParams(text: someText));

// Deprecated — НЕ использовать:
// Share.share("text");
```

Из lib/ui/screens/result_screen.dart (после Plan 01):
```dart
class _ResultScreenState extends State<ResultScreen> {
  ResultArgs? _args;
  bool _copied = false;
  String? _savedPath;
  bool _showTimestamps = true;

  // Геттер — тот же источник данных для «Поделиться»:
  String get _currentText { ... }

  // Существующая кнопка «Скопировать»:
  AnimatedSwitcher(
    child: PrimaryButton(
      key: ValueKey(_copied),
      label: _copied ? 'Скопировано' : 'Скопировать',
      icon: _copied ? Icons.check : Icons.copy_outlined,
      variant: _copied ? PrimaryButtonVariant.good : PrimaryButtonVariant.accent,
      onPressed: _onCopyTap,
    ),
  ),
  const SizedBox(height: AppSpacing.sm),
  // ← ЗДЕСЬ вставить кнопку «Поделиться» (per UI-SPEC)
```

UI-спек layout (из 06-UI-SPEC.md):
```
4. «Скопировать» PrimaryButton
5. SizedBox(height: AppSpacing.sm)   ← 8px
6. «Поделиться» PrimaryButton [variant: accent, icon: Icons.share_outlined]  ← NEW
7. SizedBox(height: AppSpacing.sm)
8. Format toggle row
```

Параметры кнопки «Поделиться»:
- label: 'Поделиться'
- icon: Icons.share_outlined
- variant: PrimaryButtonVariant.accent
- semantics: 'Поделиться расшифровкой'
- onPressed: _onShareTap (всегда enabled когда _args != null)
- НЕ меняет состояние после tap (нет «Поделился» feedback)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Верификация share_plus перед установкой</name>
  <what-built>
    Пакет share_plus помечен [ASSUMED] в Package Legitimacy Audit (slopcheck не был доступен в среде исследования). Перед установкой требуется ручная проверка.
  </what-built>
  <how-to-verify>
    1. Открыть https://pub.dev/packages/share_plus
    2. Убедиться: Publisher = fluttercommunity.dev (verified), Likes > 4000, Downloads > 2M/month
    3. Убедиться: текущая версия 10.x или 13.x — не устаревший форк
    4. Открыть https://github.com/fluttercommunity/plus_plugins — репозиторий активен
  </how-to-verify>
  <resume-signal>Введите "approved" если пакет верифицирован, или опишите проблему</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Добавить share_plus в pubspec.yaml + flutter pub get</name>
  <files>
    pubspec.yaml
  </files>
  <action>
    Прочитать `pubspec.yaml` перед правкой.

    В секции `dependencies:` добавить строку:
    ```
    share_plus: ^13.1.0
    ```

    Разместить рядом с другими pub.dev зависимостями (например, после `super_clipboard` или `path_provider`).

    После сохранения выполнить:
    ```
    cd /root/projects/ezctx && /opt/flutter/bin/flutter pub get
    ```

    Убедиться что вывод содержит "Got dependencies" без ошибок. Если версия 13.1.0 недоступна — использовать последнюю доступную ^13.x.
  </action>
  <verify>
    <automated>cd /root/projects/ezctx && grep -c "share_plus" pubspec.yaml</automated>
  </verify>
  <done>
    `pubspec.yaml` содержит `share_plus`, `flutter pub get` завершился без ошибок, `pubspec.lock` обновлён.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Добавить _onShareTap() + кнопку «Поделиться» + widget-тест</name>
  <files>
    lib/ui/screens/result_screen.dart,
    test/widget/result_screen_test.dart
  </files>
  <behavior>
    - ResultScreen содержит кнопку с текстом «Поделиться»
    - Кнопка «Поделиться» видна одновременно с кнопкой «Скопировать»
    - Кнопка «Поделиться» расположена после «Скопировать» + SizedBox(sm) (per UI-SPEC)
    - Tap «Поделиться» вызывает SharePlus.instance.share() с непустым текстом
  </behavior>
  <action>
    Шаг 1 (RED): расширить `test/widget/result_screen_test.dart`:
    - добавить тест `'ResultScreen содержит кнопку «Поделиться»'` — ищет `find.text('Поделиться')`, ожидает `findsOneWidget`.
    - Запустить — убедиться что тест падает (кнопки ещё нет).

    Шаг 2 (GREEN): изменить `lib/ui/screens/result_screen.dart`:

    A. Добавить импорт в начало файла:
    ```dart
    import 'package:share_plus/share_plus.dart';
    ```

    B. Добавить метод `_onShareTap()` в `_ResultScreenState` рядом с `_onCopyTap()`:
    ```dart
    Future<void> _onShareTap() async {
      if (_args == null) return;
      try {
        await SharePlus.instance.share(
          ShareParams(text: _currentText),
        );
      } catch (e) {
        if (!mounted) return;
        ScaffoldMessenger.of(context).showSnackBar(
          SnackBar(content: Text('Ошибка общего доступа: $e')),
        );
      }
    }
    ```

    C. В методе `build()`, в Column, после блока `AnimatedSwitcher` (кнопка «Скопировать») и его `SizedBox(height: AppSpacing.sm)` вставить кнопку «Поделиться» и разделитель (per UI-SPEC layout):
    ```dart
    PrimaryButton(
      label: 'Поделиться',
      icon: Icons.share_outlined,
      variant: PrimaryButtonVariant.accent,
      semanticLabel: 'Поделиться расшифровкой',
      onPressed: _onShareTap,
    ),
    const SizedBox(height: AppSpacing.sm),
    ```
    Эти два виджета вставить ПЕРЕД `_buildFormatToggle()`.

    Проверить: `PrimaryButton` принимает `semanticLabel` — если нет такого параметра, использовать `Semantics(label: 'Поделиться расшифровкой', child: PrimaryButton(...))`.

    НЕ добавлять состояние «Поделился» — кнопка не меняет внешний вид после tap.
    НЕ изменять логику `_onCopyTap()` и AnimatedSwitcher.

    Шаг 3 (GREEN verify): все тесты проходят.
  </action>
  <verify>
    <automated>cd /root/projects/ezctx && /opt/flutter/bin/flutter test test/widget/result_screen_test.dart --no-pub</automated>
  </verify>
  <done>
    Кнопка «Поделиться» присутствует в ResultScreen. Все тесты в result_screen_test.dart проходят. `flutter analyze` не выдаёт ошибок на result_screen.dart.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ResultScreen → share_plus | _currentText (пользовательский контент) передаётся в Intent |
| pub.dev → pubspec.yaml | Устанавливается внешний пакет share_plus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | SharePlus.instance.share(_currentText) | accept | Расшифровка — пользовательский контент; пользователь явно нажимает «Поделиться». Нет системных секретов (API-ключи не попадают в _currentText). |
| T-06-04 | Tampering | share_plus package | mitigate | Task 0 — checkpoint:human-verify проверяет pub.dev publisher и репозиторий перед установкой |
| T-06-SC | Tampering | flutter pub get (share_plus) | mitigate | Task 0 — blocking human checkpoint для верификации пакета [ASSUMED] перед pub get |
</threat_model>

<verification>
После выполнения всех задач:

1. `grep -c "share_plus" pubspec.yaml` — выдаёт >= 1.
2. `grep -c "SharePlus" lib/ui/screens/result_screen.dart` — выдаёт >= 1.
3. `flutter test test/widget/result_screen_test.dart` — все тесты проходят.
4. `flutter analyze lib/ui/screens/result_screen.dart` — нет ошибок.
5. Полный прогон: `flutter test` — все тесты зелёные.
</verification>

<success_criteria>
- `share_plus: ^13.1.0` присутствует в `pubspec.yaml`
- `_onShareTap()` использует `SharePlus.instance.share(ShareParams(text: _currentText))`
- Кнопка «Поделиться» (PrimaryButton, variant accent, icon share_outlined) присутствует на ResultScreen
- Кнопка расположена между «Скопировать» и format toggle (per UI-SPEC)
- При ошибке share показывается SnackBar с «Ошибка общего доступа: {error}»
- Все widget-тесты проходят, включая новый тест наличия кнопки «Поделиться»
</success_criteria>

<output>
Создать `.planning/phases/06-output-formats-sharing/06-02-SUMMARY.md` по завершении
</output>
