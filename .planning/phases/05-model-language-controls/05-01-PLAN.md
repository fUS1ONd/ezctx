# Plan 05-01: TranscriptionOptions — модель, данные, API

**Phase:** 5 — Model & Language Controls  
**Wave:** 1 (no dependencies)  
**Goal:** Создать слой данных и сервисов для выбора модели и языка: `TranscriptionOptions` value object, `TranscriptionOptionsRepository`, обновить `GroqApiService`, `ProcessingArgs` и оба контроллера транскрибации.

---

## Context

**Проблема:** `GroqApiService` хардкодит `AppConstants.groqDefaultModel` в двух местах. Нет способа передать `language` в запрос Groq. `ProcessingArgs` не несёт настройки транскрибации.

**Решение:** Добавить `TranscriptionOptions` (model + language), хранить через `flutter_secure_storage`, пробросить через весь стек до HTTP-запроса.

**Входные данные:** существующие файлы Phase 1–4 без изменений в UI.  
**Выходные данные:** работающий бэк-слой — Phase 2 (05-02) добавит UI поверх.

---

## Success Criteria

- [ ] SC-01: `TranscriptionOptions` компилируется; `WhisperModel.largeV3` → строка `'whisper-large-v3'`, `WhisperModel.turbo` → `'whisper-large-v3-turbo'`
- [ ] SC-02: `TranscriptionOptionsRepository.load()` возвращает дефолт (`largeV3`, `auto`) при отсутствии сохранённых данных; `save()` записывает и `load()` возвращает ровно те же значения
- [ ] SC-03: `GroqApiService.transcribe(options: TranscriptionOptions(..., language: TranscriptionLanguage.ru))` добавляет поле `language=ru` в multipart-запрос
- [ ] SC-04: `GroqApiService.transcribe(options: TranscriptionOptions(..., language: TranscriptionLanguage.auto))` — поле `language` **отсутствует** в запросе
- [ ] SC-05: `TranscriptionController.start(file, options)` и `ChunkedTranscriptionController.start(file, options)` компилируются и пробрасывают `options` в `GroqApiService`
- [ ] SC-06: `flutter analyze` — 0 ошибок, 0 предупреждений

---

## Threat Model

| # | Угроза | Митигация |
|---|--------|-----------|
| T-01 | Некорректный ISO-код языка | Enum с фиксированными значениями; нет свободного ввода |
| T-02 | Десериализация повреждённого JSON из storage | try/catch + возврат дефолта |
| T-03 | Breaking change в сигнатуре `start()` → сломается ProcessingScreen | ProcessingScreen обновляется в 05-02; временно передаём дефолтный `TranscriptionOptions.defaults()` |

---

## Implementation Tasks

### T-01 — Создать `lib/features/transcription/transcription_options.dart`

**Файл:** `lib/features/transcription/transcription_options.dart` (НОВЫЙ)

```dart
import 'package:flutter/foundation.dart';

/// Поддерживаемые модели Groq Whisper.
enum WhisperModel {
  largeV3('whisper-large-v3'),
  turbo('whisper-large-v3-turbo');

  const WhisperModel(this.apiValue);
  final String apiValue;
}

/// Язык распознавания. [auto] — поле language не передаётся в запрос Groq.
enum TranscriptionLanguage {
  auto(''),
  ru('ru'),
  en('en'),
  de('de'),
  fr('fr'),
  es('es'),
  uk('uk'),
  zh('zh'),
  ja('ja'),
  ko('ko'),
  ar('ar');

  const TranscriptionLanguage(this.isoCode);
  final String isoCode;
}

/// Настройки транскрибации: модель + язык. Immutable value object.
@immutable
class TranscriptionOptions {
  const TranscriptionOptions({
    required this.model,
    required this.language,
  });

  const TranscriptionOptions.defaults()
      : model = WhisperModel.largeV3,
        language = TranscriptionLanguage.auto;

  final WhisperModel model;
  final TranscriptionLanguage language;

  Map<String, String> toJson() => {
        'model': model.name,
        'language': language.name,
      };

  factory TranscriptionOptions.fromJson(Map<String, dynamic> json) {
    final model = WhisperModel.values.firstWhere(
      (m) => m.name == json['model'],
      orElse: () => WhisperModel.largeV3,
    );
    final language = TranscriptionLanguage.values.firstWhere(
      (l) => l.name == json['language'],
      orElse: () => TranscriptionLanguage.auto,
    );
    return TranscriptionOptions(model: model, language: language);
  }

  TranscriptionOptions copyWith({
    WhisperModel? model,
    TranscriptionLanguage? language,
  }) =>
      TranscriptionOptions(
        model: model ?? this.model,
        language: language ?? this.language,
      );

  @override
  bool operator ==(Object other) =>
      other is TranscriptionOptions &&
      other.model == model &&
      other.language == language;

  @override
  int get hashCode => Object.hash(model, language);
}
```

**Проверка:** `WhisperModel.largeV3.apiValue == 'whisper-large-v3'`, `TranscriptionLanguage.auto.isoCode == ''`.

---

### T-02 — Создать `lib/features/settings/transcription_options_repository.dart`

**Файл:** `lib/features/settings/transcription_options_repository.dart` (НОВЫЙ)

```dart
import 'dart:convert';

import 'package:flutter_secure_storage/flutter_secure_storage.dart';

import '../../core/constants/app_constants.dart';
import '../transcription/transcription_options.dart';

/// Сохраняет и загружает [TranscriptionOptions] из flutter_secure_storage.
class TranscriptionOptionsRepository {
  TranscriptionOptionsRepository({FlutterSecureStorage? storage})
      : _storage =
            storage ?? const FlutterSecureStorage(aOptions: AndroidOptions());

  final FlutterSecureStorage _storage;

  Future<TranscriptionOptions> load() async {
    try {
      final raw =
          await _storage.read(key: AppConstants.storageKeyTranscriptionOptions);
      if (raw == null || raw.isEmpty) return const TranscriptionOptions.defaults();
      final json = jsonDecode(raw) as Map<String, dynamic>;
      return TranscriptionOptions.fromJson(json);
    } catch (_) {
      return const TranscriptionOptions.defaults();
    }
  }

  Future<void> save(TranscriptionOptions options) async {
    await _storage.write(
      key: AppConstants.storageKeyTranscriptionOptions,
      value: jsonEncode(options.toJson()),
    );
  }
}
```

---

### T-03 — Добавить константы в `lib/core/constants/app_constants.dart`

**Файл:** `lib/core/constants/app_constants.dart` (MODIFY)

Добавить после `static const String groqDefaultModel = 'whisper-large-v3';`:
```dart
static const String groqTurboModel = 'whisper-large-v3-turbo';
```

Добавить после `static const String storageKeyApiKeys = 'groq_api_keys_v1';`:
```dart
static const String storageKeyTranscriptionOptions = 'transcription_options_v1';
```

> Примечание: `groqDefaultModel` остаётся как fallback — используется в тестах, не удаляем.

---

### T-04 — Обновить `lib/features/transcription/groq_api_service.dart`

**Файл:** `lib/features/transcription/groq_api_service.dart` (MODIFY)

**Изменения в `transcribe()`:**

Добавить import в начало файла:
```dart
import 'transcription_options.dart';
```

Изменить сигнатуру (добавить параметр):
```dart
Future<TranscriptionResult> transcribe({
  required SelectedAudioFile file,
  required String apiKey,
  TranscriptionOptions options = const TranscriptionOptions.defaults(),
}) async {
```

Заменить блок формирования `request` — model и language:
```dart
final request = http.MultipartRequest('POST', uri)
  ..headers['Authorization'] = 'Bearer $apiKey'
  ..fields['model'] = options.model.apiValue          // было: AppConstants.groqDefaultModel
  ..fields['response_format'] = AppConstants.groqResponseFormat
  ..fields['timestamp_granularities[]'] = AppConstants.groqTimestampGranularity
  ..files.add(await http.MultipartFile.fromPath('file', file.path));

// Добавить после объявления request (перед отправкой):
if (options.language != TranscriptionLanguage.auto) {
  request.fields['language'] = options.language.isoCode;
}
```

**Изменения в `transcribeChunk()`:**

Изменить сигнатуру:
```dart
Future<TranscriptionResult> transcribeChunk({
  required List<int> bytes,
  required String filename,
  required String apiKey,
  TranscriptionOptions options = const TranscriptionOptions.defaults(),
}) async {
```

Заменить `..fields['model'] = AppConstants.groqDefaultModel` на `..fields['model'] = options.model.apiValue`.

Добавить после объявления `request`:
```dart
if (options.language != TranscriptionLanguage.auto) {
  request.fields['language'] = options.language.isoCode;
}
```

---

### T-05 — Обновить `lib/features/transcription/processing_args.dart`

**Файл:** `lib/features/transcription/processing_args.dart` (MODIFY)

```dart
import 'audio_metadata.dart';
import 'selected_audio_file.dart';
import 'transcription_options.dart';

/// Аргументы маршрута /processing.
class ProcessingArgs {
  final SelectedAudioFile file;

  /// Метаданные из ffprobe. null — если ffprobe не вернул результат.
  final AudioMetadata? metadata;

  /// Настройки транскрибации: модель и язык.
  final TranscriptionOptions options;

  const ProcessingArgs({
    required this.file,
    this.metadata,
    this.options = const TranscriptionOptions.defaults(),
  });
}
```

---

### T-06 — Обновить `lib/features/transcription/transcription_controller.dart`

**Файл:** `lib/features/transcription/transcription_controller.dart` (MODIFY)

Добавить import:
```dart
import 'transcription_options.dart';
```

Изменить сигнатуру `start()`:
```dart
Future<void> start(
  SelectedAudioFile file, {
  TranscriptionOptions options = const TranscriptionOptions.defaults(),
}) async {
```

Передать `options` в `_api.transcribe()`:
```dart
final result = await _api.transcribe(
  file: file,
  apiKey: key,
  options: options,
);
```

---

### T-07 — Обновить `lib/features/transcription/chunked_transcription_controller.dart`

**Файл:** `lib/features/transcription/chunked_transcription_controller.dart` (MODIFY)

Добавить import:
```dart
import 'transcription_options.dart';
```

Найти метод `start()` (принимает `NormalizedAudioFile`) и обновить сигнатуру:
```dart
Future<void> start(
  NormalizedAudioFile file, {
  TranscriptionOptions options = const TranscriptionOptions.defaults(),
}) async {
```

Внутри `start()` сохранить `options` во временную переменную и передать в вызовы `_processChunk`. Так как `_processChunk` — приватный метод, обновить его сигнатуру:

```dart
Future<TranscriptionResult> _processChunk(
  int index,
  File chunkFile,
  TranscriptionOptions options,
) async {
```

В каждом вызове `_processChunk(...)` внутри `start()` добавить `options`:
```dart
await _processChunk(entry.key, entry.value, options);
```

Передать `options` в `_api.transcribeChunk(...)` внутри `_processChunk`:
```dart
await _api.transcribeChunk(
  bytes: ...,
  filename: ...,
  apiKey: ...,
  options: options,
);
```

---

## Verification

После выполнения всех задач:

```bash
# Запустить анализ через context-mode
flutter analyze
```

Ожидаемый результат: 0 issues. ProcessingScreen временно не скомпилируется только если вызывает `start()` без именованного параметра — проверить, использует ли он positional. Если нет — код компилируется без изменений в ProcessingScreen (именованный параметр с дефолтом не ломает существующие вызовы).

---

## File Manifest

| Файл | Действие |
|------|----------|
| `lib/features/transcription/transcription_options.dart` | СОЗДАТЬ |
| `lib/features/settings/transcription_options_repository.dart` | СОЗДАТЬ |
| `lib/core/constants/app_constants.dart` | ИЗМЕНИТЬ |
| `lib/features/transcription/groq_api_service.dart` | ИЗМЕНИТЬ |
| `lib/features/transcription/processing_args.dart` | ИЗМЕНИТЬ |
| `lib/features/transcription/transcription_controller.dart` | ИЗМЕНИТЬ |
| `lib/features/transcription/chunked_transcription_controller.dart` | ИЗМЕНИТЬ |
