---
phase: 04-multi-key-pool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/features/settings/groq_key_pool_test.dart
  - test/unit/groq_api_service_rate_limit_test.dart
  - lib/core/error/app_exception.dart
  - lib/core/error/all_keys_blocked_exception.dart
  - lib/core/constants/app_constants.dart
  - lib/features/transcription/groq_key_pool.dart
  - lib/features/transcription/groq_api_service.dart
autonomous: true
requirements:
  - TRANS-01
  - TRANS-02

must_haves:
  truths:
    - "GroqKeyPool.acquireKey() возвращает живой ключ по round-robin"
    - "Заблокированный ключ пропускается, acquireKey() ждёт разблокировки"
    - "При таймауте 10 мин бросается AllKeysBlockedException"
    - "RateLimitException содержит retryAfterSeconds"
    - "GroqApiService парсит retry-after / x-ratelimit-reset-* из 429-ответа"
    - "Fallback 60 с если заголовки отсутствуют"
  artifacts:
    - path: "lib/features/transcription/groq_key_pool.dart"
      provides: "GroqKeyPool extends ChangeNotifier; acquireKey/reportRateLimited/releaseKey/getStatuses/addKey/removeKey/allKeys/aliveKeyCount"
    - path: "lib/core/error/all_keys_blocked_exception.dart"
      provides: "AllKeysBlockedException extends AppException"
    - path: "lib/core/error/app_exception.dart"
      provides: "RateLimitException с полем retryAfterSeconds: int"
    - path: "lib/core/constants/app_constants.dart"
      provides: "kMaxConcurrentChunks = 5"
    - path: "test/features/settings/groq_key_pool_test.dart"
      provides: "Юнит-тесты TRANS-01: round-robin, skip_blocked, wait_for_unblock, timeout"
    - path: "test/unit/groq_api_service_rate_limit_test.dart"
      provides: "Юнит-тесты TRANS-02: parse_retry_after, parse_duration_string, fallback"
  key_links:
    - from: "lib/features/transcription/groq_key_pool.dart"
      to: "lib/features/transcription/groq_api_service.dart"
      via: "acquireKey() → apiKey параметр transcribeChunk"
      pattern: "acquireKey"
    - from: "lib/features/transcription/groq_api_service.dart"
      to: "lib/core/error/app_exception.dart"
      via: "_parseRetryAfter(response) → RateLimitException.retryAfterSeconds"
      pattern: "retryAfterSeconds"
---

<objective>
Wave 0 + Wave 1: создать stub-тесты (скелеты), затем реализовать GroqKeyPool, AllKeysBlockedException, обновить RateLimitException и GroqApiService с парсингом заголовков.

Purpose: Заложить core service layer — после этого плана пул умеет ротировать ключи и блокировать по 429, контроллеры и UI могут быть wire-up в следующих планах.
Output: groq_key_pool.dart, all_keys_blocked_exception.dart, обновлённые app_exception.dart + app_constants.dart + groq_api_service.dart, stub-тесты.
</objective>

<execution_context>
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/workflows/execute-plan.md
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-multi-key-pool/04-CONTEXT.md
@.planning/phases/04-multi-key-pool/04-RESEARCH.md
@.planning/phases/04-multi-key-pool/04-PATTERNS.md

<interfaces>
<!-- Ключевые типы и контракты из существующего кода. Исполнитель использует напрямую. -->

Из lib/core/error/app_exception.dart (текущее состояние):
```dart
sealed class AppException implements Exception {
  final String message;
  const AppException(this.message);

  @override
  String toString() => '$runtimeType: $message';
}

class RateLimitException extends AppException {
  const RateLimitException(super.message);   // <-- добавить retryAfterSeconds
}
class AuthException extends AppException { ... }
class NetworkException extends AppException { ... }
class InternalException extends AppException { ... }
```

Из lib/core/constants/app_constants.dart (паттерн добавления константы):
```dart
class AppConstants {
  static const int kChunkDurationSeconds = 4500;
  // ДОБАВИТЬ: static const int kMaxConcurrentChunks = 5;
}
```

Из lib/features/transcription/chunked_transcription_controller.dart (паттерн _Semaphore):
```dart
class _Semaphore {
  _Semaphore(this._maxConcurrent);
  final int _maxConcurrent;
  int _running = 0;
  final _queue = <Completer<void>>[];

  Future<T> run<T>(Future<T> Function() fn) async {
    if (_running >= _maxConcurrent) {
      final waiter = Completer<void>();
      _queue.add(waiter);
      await waiter.future;
    }
    _running++;
    try {
      return await fn();
    } finally {
      _running--;
      if (_queue.isNotEmpty) _queue.removeAt(0).complete();
    }
  }
}
```

Из lib/features/transcription/groq_api_service.dart (текущий 429-блок):
```dart
// строки ~74-76:
if (response.statusCode == 429) {
  throw const RateLimitException('Превышен лимит запросов Groq');
}
```

Из lib/features/settings/api_key_repository.dart (метод для инициализации пула):
```dart
Future<List<ApiKey>> listKeys(); // возвращает List<ApiKey> где ApiKey.raw: String
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Stub-тесты Wave 0 (скелеты для TRANS-01 и TRANS-02)</name>
  <files>
    test/features/settings/groq_key_pool_test.dart
    test/unit/groq_api_service_rate_limit_test.dart
  </files>
  <behavior>
    - groq_key_pool_test.dart: тест "round_robin выдаёт ключи по очереди" → fail (класс не существует)
    - groq_key_pool_test.dart: тест "заблокированный ключ пропускается при acquireKey" → fail
    - groq_key_pool_test.dart: тест "все ключи заблокированы → ждём разблокировки" → fail
    - groq_key_pool_test.dart: тест "таймаут 10 мин → AllKeysBlockedException" → fail
    - groq_api_service_rate_limit_test.dart: тест "_parseRetryAfter возвращает секунды из retry-after" → fail
    - groq_api_service_rate_limit_test.dart: тест "_parseDurationString корректно парсит '2m59.56s' → 179" → fail
    - groq_api_service_rate_limit_test.dart: тест "fallback 60 если заголовки отсутствуют" → fail
  </behavior>
  <action>
    Создать два файла-скелета. Каждый содержит import-заглушки и группу test() вызовов с
    телами, которые вызывают fail('RED — реализация ещё не написана'). Это даёт Nyquist-
    контрольную точку: после написания реализации тесты должны позеленеть.

    В groq_key_pool_test.dart:
    - import 'package:fake_async/fake_async.dart'; (fake_async входит во flutter_test SDK)
    - Проверить через pubspec.yaml: если fake_async не в dev_dependencies — добавить
      `fake_async: any` в dev_dependencies. В flutter_test SDK для Dart 3 fake_async доступен
      напрямую через `package:fake_async/fake_async.dart`.
    - group('GroqKeyPool', ...) с четырьмя test() блоками (см. behavior).

    В groq_api_service_rate_limit_test.dart:
    - Тесты будут тестировать публичный метод-парсер. Поскольку методы _parseRetryAfter и
      _parseDurationString приватные в GroqApiService, вынести парсинг в отдельную
      top-level функцию parseRetryAfterFromHeaders(Map&lt;String, String&gt; headers) → int
      в файле groq_api_service.dart (или в отдельном файле parse_retry_after.dart).
      Это нужно для тестируемости. Тесты импортируют и вызывают эту публичную функцию.
    - group('parseRetryAfterFromHeaders', ...) с тремя test() блоками.

    Директории создать заранее: test/features/settings/, test/unit/.
  </action>
  <verify>
    <automated>
      Директории test/features/settings/ и test/unit/ созданы.
      Файлы groq_key_pool_test.dart и groq_api_service_rate_limit_test.dart существуют.
      Запустить: flutter test test/features/settings/groq_key_pool_test.dart
      Ожидается: тесты с ошибками компиляции или fail() — НЕ pass (это RED стадия).
    </automated>
  </verify>
  <done>Два файла созданы, тесты падают (RED). Готово к GREEN-реализации.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 2: AllKeysBlockedException + RateLimitException.retryAfterSeconds + kMaxConcurrentChunks</name>
  <files>
    lib/core/error/all_keys_blocked_exception.dart
    lib/core/error/app_exception.dart
    lib/core/constants/app_constants.dart
  </files>
  <behavior>
    - AllKeysBlockedException наследует AppException, имеет const конструктор
    - RateLimitException принимает необязательный {this.retryAfterSeconds = 60}
    - Все существующие вызовы const RateLimitException('...') продолжают компилироваться
    - AppConstants.kMaxConcurrentChunks == 5
  </behavior>
  <action>
    1. Создать lib/core/error/all_keys_blocked_exception.dart:
       - Импортировать app_exception.dart
       - class AllKeysBlockedException extends AppException с const конструктором
         и фиксированным сообщением на русском: 'Все ключи заблокированы. Ожидание…'
       - ВАЖНО: sealed class AppException означает, что AllKeysBlockedException НЕ может
         быть в том же файле что AppException (другой файл). Это нормально — sealed
         ограничивает только субтипы в том же package, subtypes в разных файлах одного
         пакета разрешены. Проверить поведение: если Dart требует тот же файл — добавить
         AllKeysBlockedException непосредственно в app_exception.dart.

    2. Обновить lib/core/error/app_exception.dart:
       Добавить поле retryAfterSeconds к RateLimitException:
         final int retryAfterSeconds;
         const RateLimitException(super.message, {this.retryAfterSeconds = 60});
       Dart позволяет const конструктор с необязательным named параметром.

    3. Обновить lib/core/constants/app_constants.dart:
       Добавить после kChunkDurationSeconds:
         /// Максимальное число параллельных чанков; реальное значение = min(pool.aliveKeyCount, этой константы).
         static const int kMaxConcurrentChunks = 5;

    Проверить: dart analyze lib/core/ — ноль ошибок.
  </action>
  <verify>
    <automated>
      flutter analyze lib/core/error/ lib/core/constants/
      Ожидается: 0 ошибок компиляции.
      Проверить: grep -c 'retryAfterSeconds' lib/core/error/app_exception.dart вернёт >= 1.
    </automated>
  </verify>
  <done>AllKeysBlockedException, RateLimitException.retryAfterSeconds и kMaxConcurrentChunks существуют. Обратная совместимость сохранена.</done>
</task>

<task type="auto" tdd="true">
  <name>Задача 3: GroqKeyPool + парсинг заголовков в GroqApiService (GREEN)</name>
  <files>
    lib/features/transcription/groq_key_pool.dart
    lib/features/transcription/groq_api_service.dart
  </files>
  <behavior>
    - GroqKeyPool(initialKeys: ['k1', 'k2']).acquireKey() возвращает 'k1', затем 'k2', затем 'k1' (round-robin)
    - После reportRateLimited('k1', 30) — acquireKey() возвращает 'k2', пропуская k1
    - GroqKeyPool с одним ключом — reportRateLimited → acquireKey() блокируется до разблокировки
    - Таймаут 10 мин → AllKeysBlockedException (тест с fakeAsync)
    - parseRetryAfterFromHeaders({'retry-after': '45'}) == 45
    - parseRetryAfterFromHeaders({'x-ratelimit-reset-requests': '2m59.56s'}) == 179
    - parseRetryAfterFromHeaders({}) == 60 (fallback)
    - GroqApiService при 429 бросает RateLimitException с retryAfterSeconds из заголовков
    - aliveKeyCount возвращает количество незаблокированных ключей
    - getStatuses() возвращает List&lt;KeyStatus&gt; с правильными типами
    - addKey(raw) добавляет ключ в пул, removeKey(raw) удаляет
    - allKeys возвращает List&lt;String&gt; всех ключей (включая заблокированные)
  </behavior>
  <action>
    Создать lib/features/transcription/groq_key_pool.dart.

    Импорты:
      import 'dart:async';
      import 'package:flutter/foundation.dart';
      import '../settings/api_key_repository.dart'; // только если нужен тип ApiKey

    Определить sealed class KeyStatus (в том же файле или отдельном key_status.dart):
      sealed class KeyStatus { final String key; const KeyStatus({required this.key}); }
      class ActiveKeyStatus extends KeyStatus { const ActiveKeyStatus({required super.key}); }
      class BlockedKeyStatus extends KeyStatus {
        final DateTime blockedUntil;
        const BlockedKeyStatus({required super.key, required this.blockedUntil});
      }

    Класс GroqKeyPool extends ChangeNotifier:
      Поля: List&lt;String&gt; _keys (из initialKeys), Map&lt;String, DateTime&gt; _blockedUntil,
            int _cyclicIndex = 0, List&lt;Completer&lt;String&gt;&gt; _waiters.

      Конструктор: GroqKeyPool({List&lt;String&gt; initialKeys = const []}) : _keys = List.of(initialKeys);

      Геттеры:
        int get aliveKeyCount — количество ключей где !_isBlocked(k)
        List&lt;String&gt; get allKeys — List.unmodifiable(_keys)

      Метод _isBlocked(String key) → bool:
        - Если blockedUntil[key] == null → false
        - Если DateTime.now().isAfter(until) → удалить запись, вернуть false
        - Иначе → true

      Метод _nextAlive() → String?:
        - Если _keys.isEmpty → null
        - ЗАЩИТА: _cyclicIndex = _cyclicIndex % _keys.length
        - Циклический обход: for i in 0.._keys.length, idx = (_cyclicIndex + i) % _keys.length
        - Если !_isBlocked(_keys[idx]) → advance _cyclicIndex = (idx + 1) % _keys.length, return _keys[idx]
        - Если ни один не найден → null

      Метод acquireKey() → Future&lt;String&gt;:
        - Вызвать _nextAlive(); если не null — вернуть
        - Создать Completer&lt;String&gt;, добавить в _waiters
        - Вызвать _scheduleWakeup()
        - Вернуть completer.future.timeout(Duration(minutes: 10), onTimeout: () => throw const AllKeysBlockedException('...'))

      Метод _scheduleWakeup():
        - Найти ближайший _blockedUntil.values.where(t.isAfter(now)).min
        - Если null → return
        - Timer(delay + Duration(milliseconds: 50), _onWakeup)

      Метод _onWakeup():
        - alive = _nextAlive(); если null → _scheduleWakeup(); return
        - ВАЖНО (Pitfall 2 из RESEARCH.md): раздавать ключи waiters по одному через _nextAlive(),
          не один ключ всем. Итерация: пока _waiters.isNotEmpty — взять первого waiter,
          взять _nextAlive() (может быть null если ключ один), если null — _scheduleWakeup(); break.
          Иначе — _waiters.removeAt(0).complete(alive_for_this_waiter).
        - notifyListeners()

      Метод reportRateLimited(String key, int retryAfterSeconds):
        - _blockedUntil[key] = DateTime.now().add(Duration(seconds: retryAfterSeconds))
        - notifyListeners()
        - _scheduleWakeup()

      Метод releaseKey(String key): — для совместимости с D-02 контрактом
        - В текущей реализации ключи не требуют явного release (ротация через round-robin)
        - Метод-заглушка: можно оставить пустым или логировать

      Метод getStatuses() → List&lt;KeyStatus&gt;:
        - _keys.map((k) { ... }) — для каждого ключа проверить _isBlocked(k)
        - Если заблокирован → BlockedKeyStatus(key: k, blockedUntil: _blockedUntil[k]!)
        - Иначе → ActiveKeyStatus(key: k)

      Метод getStatusForKey(String rawKey) → KeyStatus:
        - Нужен для ApiKeysScreen (ищет конкретный ключ)
        - Если _isBlocked(rawKey) → BlockedKeyStatus(...)
        - Иначе → ActiveKeyStatus(key: rawKey)

      Метод addKey(String raw):
        - Если !_keys.contains(raw) → _keys.add(raw)
        - notifyListeners()

      Метод removeKey(String raw):
        - _keys.remove(raw)
        - _blockedUntil.remove(raw)
        - ЗАЩИТА: _cyclicIndex = _keys.isEmpty ? 0 : _cyclicIndex % _keys.length
        - notifyListeners()

    Обновить lib/features/transcription/groq_api_service.dart:

    Вынести парсинг в top-level функцию (для тестируемости — per задача 1):
      int parseRetryAfterFromHeaders(Map&lt;String, String&gt; headers) { ... }
      int _parseDurationString(String s) { ... }

    В блоке if (response.statusCode == 429):
      throw RateLimitException(
        'Превышен лимит запросов Groq',
        retryAfterSeconds: parseRetryAfterFromHeaders(response.headers),
      );

    Порядок в parseRetryAfterFromHeaders (per D-06):
      1. headers['retry-after'] → int.tryParse → если > 0, вернуть
      2. secsReq = headers['x-ratelimit-reset-requests'] → _parseDurationString
         seksTok = headers['x-ratelimit-reset-tokens'] → _parseDurationString
         Вернуть min из присутствующих
      3. Fallback: return 60

    _parseDurationString(String s):
      - RegExp r'(\d+)h' → часы * 3600
      - RegExp r'(\d+)m(?!s)' → минуты * 60 ((?!s) чтобы не захватить 'ms')
      - RegExp r'([\d.]+)s' → double.parse(...).ceil()
      - Если total == 0 → return 60

    БЕЗОПАСНОСТЬ: Не логировать сырые ключи. В debug-логах маскировать:
      debugPrint('reportRateLimited: ...${key.substring(key.length > 4 ? key.length - 4 : 0)}');
  </action>
  <verify>
    <automated>
      flutter test test/features/settings/groq_key_pool_test.dart
      flutter test test/unit/groq_api_service_rate_limit_test.dart
      Ожидается: все тесты зелёные.
      flutter analyze lib/features/transcription/groq_key_pool.dart lib/features/transcription/groq_api_service.dart
      Ожидается: 0 ошибок.
    </automated>
  </verify>
  <done>
    GroqKeyPool реализован и протестирован: round-robin, skip_blocked, wait_for_unblock, timeout.
    parseRetryAfterFromHeaders протестирован: retry-after, duration string, fallback.
    GroqApiService бросает RateLimitException с retryAfterSeconds при 429.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → GroqApiService | HTTP 429-ответ содержит заголовки, значения которых используются для таймаутов |
| user → ApiKeysScreen | Пользователь вводит строки API-ключей; пул хранит их в памяти |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | GroqKeyPool debug logs | mitigate | Маскировать ключ в логах: показывать только последние 4 символа |
| T-04-02 | Tampering | parseRetryAfterFromHeaders | mitigate | Validation: если retryAfterSeconds > 3600 — cap до 3600 (не давать Groq блокировать ключ на сутки) |
| T-04-03 | Denial of Service | acquireKey() timeout | accept | Timeout 10 мин — достаточно; AllKeysBlockedException поднимается в UI как retryable error |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Фаза не устанавливает новых пакетов; только Dart SDK |
</threat_model>

<verification>
После выполнения всех задач:
- flutter analyze lib/core/ lib/features/transcription/ — 0 ошибок
- flutter test test/features/settings/groq_key_pool_test.dart — все зелёные
- flutter test test/unit/groq_api_service_rate_limit_test.dart — все зелёные
- grep -c 'retryAfterSeconds' lib/core/error/app_exception.dart — >= 1
- grep -c 'kMaxConcurrentChunks' lib/core/constants/app_constants.dart — >= 1
</verification>

<success_criteria>
- GroqKeyPool создаётся с initialKeys, выдаёт ключи round-robin через acquireKey()
- reportRateLimited блокирует ключ, notifyListeners() вызывается
- acquireKey() ждёт разблокировки, таймаут 10 мин → AllKeysBlockedException
- parseRetryAfterFromHeaders правильно парсит все три формата заголовков
- RateLimitException имеет retryAfterSeconds с дефолтом 60
- Все тесты Wave 0 зелёные
</success_criteria>

<output>
Создать `.planning/phases/04-multi-key-pool/04-01-SUMMARY.md` по завершении
</output>
