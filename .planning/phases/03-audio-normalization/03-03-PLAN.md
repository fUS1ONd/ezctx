---
id: 03-03
phase: 3
wave: 2
title: "Тесты нормализации + обновление audio_chunking_service_test"
depends_on:
  - 03-01
  - 03-02
goal: "Написать тесты AudioNormalizationService и обновить audio_chunking_service_test под -c:a copy и kChunkDurationSeconds=4500."
requirements:
  - TEST-01
files_modified:
  - test/features/transcription/audio_normalization_service_test.dart
  - test/features/transcription/audio_chunking_service_test.dart
autonomous: true

must_haves:
  truths:
    - "Тест проверяет что команда нормализации содержит -b:a 32k, -ac 1, -ar 16000, -codec:a libmp3lame, -y"
    - "Тест проверяет что ошибка ffmpeg бросает InternalException"
    - "Тест проверяет что результат normalize() возвращает path оканчивающийся на .mp3"
    - "Тест AudioChunkingService.split() проверяет -c:a copy вместо -c:a libmp3lame"
    - "Тест AudioChunkingService.split() проверяет -segment_time 4500 вместо 1200"
    - "Весь тестовый набор проходит без ошибок"
  artifacts:
    - path: "test/features/transcription/audio_normalization_service_test.dart"
      provides: "Unit тесты AudioNormalizationService"
      min_lines: 50
    - path: "test/features/transcription/audio_chunking_service_test.dart"
      provides: "Обновлённые тесты AudioChunkingService (split с -c:a copy)"
  key_links:
    - from: "audio_normalization_service_test.dart"
      to: "audio_normalization_service.dart"
      via: "ffmpegOverride + chunkingService override"
      pattern: "AudioNormalizationService"
---

<objective>
Написать полный тестовый набор для AudioNormalizationService, используя
ffmpegOverride и chunkingService override (тот же паттерн, что в AudioChunkingService).

Обновить audio_chunking_service_test.dart: заменить expects на -c:a libmp3lame/-b:a 128k/-ac 1/-ar 16000
на -c:a copy; обновить -segment_time с 1200 на 4500.

Purpose: Регрессионная защита. Тесты подтверждают корректность ffmpeg-команд
без запуска реального ffmpeg (Android-only инфраструктура в CI-среде недоступна).

Output:
- test/features/transcription/audio_normalization_service_test.dart (новый)
- test/features/transcription/audio_chunking_service_test.dart (обновлён)
</objective>

<execution_context>
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/workflows/execute-plan.md
@/home/krivonosov/projects/ezctx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-audio-normalization/03-RESEARCH.md

@lib/features/transcription/audio_normalization_service.dart
@lib/features/transcription/normalized_audio_file.dart
@test/features/transcription/audio_chunking_service_test.dart
@lib/core/error/app_exception.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Тесты AudioNormalizationService</name>
  <files>
    test/features/transcription/audio_normalization_service_test.dart
  </files>
  <behavior>
    - normalize() с ffmpegOverride: capturedCommand содержит '-b:a 32k'
    - normalize() с ffmpegOverride: capturedCommand содержит '-ac 1'
    - normalize() с ffmpegOverride: capturedCommand содержит '-ar 16000'
    - normalize() с ffmpegOverride: capturedCommand содержит '-codec:a libmp3lame'
    - normalize() с ffmpegOverride: capturedCommand содержит '-y'
    - normalize(): result.path оканчивается на '.mp3'
    - normalize() с ffmpegOverride → InternalException: тест бросает InternalException
    - normalize(): входной путь присутствует в команде (содержит inputPath)
  </behavior>
  <action>
    Создать файл `test/features/transcription/audio_normalization_service_test.dart`.

    Импорты:
    ```dart
    import 'dart:io';
    import 'package:ezctx/core/error/app_exception.dart';
    import 'package:ezctx/features/transcription/audio_chunking_service.dart';
    import 'package:ezctx/features/transcription/audio_normalization_service.dart';
    import 'package:ffmpeg_kit_flutter_new/media_information.dart';
    import 'package:flutter_test/flutter_test.dart';
    ```

    Вспомогательная функция (скопировать из audio_chunking_service_test.dart):
    ```dart
    MediaInformation makeInfo(String? durationSec) {
      if (durationSec == null) {
        return MediaInformation({'format': <String, dynamic>{}});
      }
      return MediaInformation({'format': {'duration': durationSec}});
    }
    ```

    Группа 'AudioNormalizationService.normalize — ffmpeg команда':

    Тест 1 "команда содержит все обязательные флаги нормализации":
    - Создать реальный tmp input-файл (через `File(...).create()`) с tearDown удалением.
    - AudioNormalizationService с ffmpegOverride: захватить команду в capturedCommand.
    - chunkingService: AudioChunkingService(probeOverride: (_) async => makeInfo('300.0'))
      (300 с = 5 мин — небольшой файл).
    - Вызвать `svc.normalize(tmp.path)`.
    - Проверить: capturedCommand содержит '-b:a 32k', '-ac 1', '-ar 16000',
      '-codec:a libmp3lame', '-y', tmp.path (входной путь).
    - Проверить: result.path endsWith '.mp3'.
    - Проверить: result.durationSeconds closeTo 300.0 (из probeOverride).

    Тест 2 "ошибка ffmpeg → InternalException":
    - ffmpegOverride бросает InternalException.
    - expectLater → throwsA(isA<InternalException>()).

    Тест 3 "выходной путь в tmp-директории":
    - Capture result.path.
    - Ожидать: result.path содержит 'ezctx_norm_'.
    - Ожидать: result.path endsWith '.mp3'.

    Тест 4 "входной путь оборачивается в кавычки":
    - Ожидать: capturedCommand содержит '"${tmp.path}"' (путь в двойных кавычках).
      (Защита от pitfall 2 — пробелы в именах файлов.)

    Важно: во всех тестах создавать РЕАЛЬНЫЕ tmp-файлы через `File(...).create()` —
    это необходимо для `File.statSync()` в `getMetadata`. Добавлять tearDown удаление.

    Выходной файл в тестах НЕ создаётся (ffmpegOverride перехватывает вызов),
    поэтому проверять stat выходного файла не нужно.
  </action>
  <verify>
    <automated>grep -c "AudioNormalizationService" test/features/transcription/audio_normalization_service_test.dart && grep -c "expect" test/features/transcription/audio_normalization_service_test.dart</automated>
  </verify>
  <done>
    Файл содержит ≥ 4 теста, все группы покрывают флаги команды, ошибку ffmpeg и формат выходного пути.
    `flutter test test/features/transcription/audio_normalization_service_test.dart` проходит.
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновить audio_chunking_service_test.dart</name>
  <files>test/features/transcription/audio_chunking_service_test.dart</files>
  <action>
    В тесте 'ffmpeg-команда содержит правильные параметры кодирования' (строка ~133)
    заменить устаревшие expects:

    Удалить:
    ```dart
    expect(capturedCommand, contains('-segment_time 1200'));
    expect(capturedCommand, contains('-c:a libmp3lame'));
    expect(capturedCommand, contains('-b:a 128k'));
    expect(capturedCommand, contains('-ac 1'));
    expect(capturedCommand, contains('-ar 16000'));
    ```

    Добавить вместо:
    ```dart
    expect(capturedCommand, contains('-segment_time 4500'));
    expect(capturedCommand, contains('-c:a copy'));
    ```

    Сохранить без изменений:
    ```dart
    expect(capturedCommand, isNotNull);
    expect(capturedCommand, contains('-f segment'));
    expect(capturedCommand, contains('chunk_%03d.mp3'));
    ```

    Обновить имя теста на:
    'ffmpeg-команда содержит -c:a copy и segment_time 4500 (нормализованный вход)'

    Все остальные тесты в файле остаются без изменений.
  </action>
  <verify>
    <automated>grep -c "c:a copy" test/features/transcription/audio_chunking_service_test.dart && grep -c "segment_time 4500" test/features/transcription/audio_chunking_service_test.dart && grep -c "libmp3lame" test/features/transcription/audio_chunking_service_test.dart</automated>
  </verify>
  <done>
    grep "c:a copy" → 1 вхождение.
    grep "segment_time 4500" → 1 вхождение.
    grep "libmp3lame" → 0 вхождений (убрано из теста; в продакшн-коде его тоже нет после Task 2 плана 03-01).
    `flutter test test/features/transcription/audio_chunking_service_test.dart` проходит.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Тестовая среда | Тесты используют реальные tmp-файлы — tearDown обязателен |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | tmp-файлы в тестах | mitigate | addTearDown(() => tmp.deleteSync()) для каждого созданного файла |
</threat_model>

<verification>
Запустить оба теста последовательно:

1. `flutter test test/features/transcription/audio_normalization_service_test.dart`
   Ожидаемый результат: All tests passed.

2. `flutter test test/features/transcription/audio_chunking_service_test.dart`
   Ожидаемый результат: All tests passed.

3. `flutter test` — полный прогон:
   Ожидаемый результат: все тесты проходят без ошибок.
</verification>

<success_criteria>
- audio_normalization_service_test.dart содержит ≥ 4 теста покрывающих: флаги команды, InternalException, формат пути, кавычки вокруг inputPath.
- audio_chunking_service_test.dart: -c:a copy + -segment_time 4500 присутствуют, libmp3lame и -b:a 128k отсутствуют.
- `flutter test` проходит полностью.
- Все 5 критериев успеха фазы выполнены: нормализация → isChunked по длительности → UI «Подготовка аудио…» → OGG-сценарий работает.
</success_criteria>

<output>
Создать `.planning/phases/03-audio-normalization/03-03-SUMMARY.md` по завершении.
</output>
