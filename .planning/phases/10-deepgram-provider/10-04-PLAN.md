---
phase: 10-deepgram-provider
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - CLAUDE.md
  - .planning/phases/10-deepgram-provider/10-VALIDATION.md
autonomous: true
requirements: [R-DG-07]
must_haves:
  truths:
    - "Полный набор тестов (flutter test) зелёный — Groq-регрессия не сломана namespace-рефактором"
    - "CLAUDE.md ограничение «API: только Groq Whisper» обновлено на два провайдера (Groq + Deepgram)"
    - "10-VALIDATION.md frontmatter переведён в nyquist_compliant: true, wave_0_complete: true после зелёного полного прогона"
  artifacts:
    - path: "CLAUDE.md"
      provides: "обновлённое ограничение API (два провайдера)"
      contains: "Deepgram"
  key_links:
    - from: "CLAUDE.md"
      to: "Constraints"
      via: "обновление через dscs-updater"
      pattern: "Deepgram"
---

<objective>
Фазовый гейт: прогнать полный набор тестов (регрессия R-DG-07 — namespace-рефактор не
сломал Groq), проставить флаги валидации в `10-VALIDATION.md`, затем обновить ограничение
`CLAUDE.md` «API: только Groq Whisper» → два провайдера (Groq + Deepgram) через агента
`dscs-updater`.

Обновление CLAUDE.md выполняется ПОСЛЕ зелёного полного прогона — документация
фиксирует уже реализованное состояние.

Purpose: закрыть фазу 10 проверкой регрессии и синхронизацией документации с кодом.
Output: зелёный `flutter test`; обновлённый frontmatter `10-VALIDATION.md`; обновлённый `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-deepgram-provider/10-SPEC.md
@.planning/phases/10-deepgram-provider/10-VALIDATION.md
@.planning/phases/10-deepgram-provider/10-01-SUMMARY.md
@.planning/phases/10-deepgram-provider/10-02-SUMMARY.md
@.planning/phases/10-deepgram-provider/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Фазовый гейт — полный набор тестов зелёный (R-DG-07) + флаги валидации</name>
  <files>.planning/phases/10-deepgram-provider/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-deepgram-provider/10-VALIDATION.md — Per-Task Verification Map, full suite command, frontmatter (nyquist_compliant/wave_0_complete)
  </read_first>
  <action>
    Прогнать полный набор тестов через context-mode: `/opt/flutter/bin/flutter test`. Цель — все тесты зелёные, включая существующие Groq-тесты (R-DG-07: namespace-рефактор `SecureStorageServiceImpl` не сломал Groq-хранилище) и новые Deepgram-тесты из планов 10-01/10-03.
    Также прогнать `/opt/flutter/bin/flutter analyze` — без ошибок (висячих ссылок на удалённый transcriptionProviderProvider быть не должно).
    Если есть падения — НЕ обновлять CLAUDE.md (Task 2 блокируется) и НЕ проставлять флаги валидации; зафиксировать падающие тесты в SUMMARY и вернуть на доработку соответствующего плана.
    ТОЛЬКО при полностью зелёном прогоне (тесты + analyze): обновить frontmatter `10-VALIDATION.md` — `nyquist_compliant: true` и `wave_0_complete: true` (Wave 0 закрыт ещё в 10-01, полный прогон подтверждает Nyquist-покрытие). Использовать Edit-инструмент для точечной правки frontmatter.
  </action>
  <verify>
    <automated>/opt/flutter/bin/flutter test && /opt/flutter/bin/flutter analyze && grep -q "nyquist_compliant: true" .planning/phases/10-deepgram-provider/10-VALIDATION.md && grep -q "wave_0_complete: true" .planning/phases/10-deepgram-provider/10-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test` (полный набор) exits 0 — Groq-тесты и Deepgram-тесты зелёные
    - `flutter analyze` без ошибок
    - R-DG-07 подтверждён: существующие тесты `test/unit/groq*` зелёные
    - `10-VALIDATION.md` frontmatter содержит `nyquist_compliant: true` и `wave_0_complete: true`
  </acceptance_criteria>
  <done>Полный набор тестов и analyze зелёные; регрессия Groq отсутствует; флаги валидации проставлены в true.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить ограничение CLAUDE.md через dscs-updater</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md — секция Constraints, строка «**API**: только Groq Whisper (free tier), без серверной части.»
    - .planning/phases/10-deepgram-provider/10-SPEC.md (lines 61-64) — требование обновления
  </read_first>
  <action>
    ТОЛЬКО после зелёного Task 1. Основной путь — вызвать агента `dscs-updater` для обновления ограничения в `CLAUDE.md`: «**API**: только Groq Whisper (free tier), без серверной части.» → формулировка про два провайдера (Groq Whisper + Deepgram nova-3, оба free tier, без серверной части). Сохранить стиль секции Constraints (markdown bullet, русский). Не менять прочие ограничения.
    Прямое редактирование Edit-инструментом с той же формулировкой допустимо ТОЛЬКО при подтверждённой недоступности `dscs-updater` (агент не найден/не отвечает). Сначала пытаться через `dscs-updater`; Edit — резервный путь, фиксируется в SUMMARY как использованный fallback.
  </action>
  <verify>
    <automated>grep -qi "Deepgram" CLAUDE.md && grep -qi "Groq" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - секция Constraints в `CLAUDE.md` упоминает оба провайдера (Groq + Deepgram)
    - старая формулировка «только Groq Whisper» отсутствует в строке ограничения API
    - прочие ограничения секции Constraints не изменены
  </acceptance_criteria>
  <done>CLAUDE.md отражает мульти-провайдерное состояние; обновлено через dscs-updater (или Edit-fallback при подтверждённой недоступности агента).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (документация) | CLAUDE.md — без секретов; гейт не пересекает доверительных границ runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | Регрессия Groq-namespace | mitigate | Полный `flutter test` подтверждает, что раздельные namespace ключей не сломали Groq-хранилище (R-DG-07). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Новых пакетов нет — установки отсутствуют. |
</threat_model>

<verification>
- `flutter test` (полный набор) и `flutter analyze` зелёные через ctx_execute.
- `10-VALIDATION.md` frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`.
- CLAUDE.md содержит упоминание Deepgram в секции Constraints.
</verification>

<success_criteria>
- Полный набор тестов зелёный — R-DG-07 подтверждён.
- Флаги валидации проставлены; CLAUDE.md синхронизирован с мульти-провайдерным состоянием.
- Фаза 10 закрыта: Nova-3 работает end-to-end, документация актуальна.
</success_criteria>

<output>
Create `.planning/phases/10-deepgram-provider/10-04-SUMMARY.md` when done
</output>
