---
phase: 10-deepgram-provider
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - lib/core/providers/service_providers.dart
  - lib/main.dart
  - lib/ui/screens/processing_screen.dart
  - test/widget/home_screen_smoke_test.dart
  - test/features/transcription/chunked_transcription_controller_test.dart
autonomous: true
requirements: [R-DG-06, R-DG-08]
must_haves:
  truths:
    - "deepgramKeyPoolProvider существует и переопределяется в main через ProviderScope"
    - "main bootstrap читает оба набора ключей и создаёт два KeyPool (Groq + Deepgram)"
    - "ProcessingScreen выбирает pool+provider по options.model.provider"
    - "Nova-3 без Deepgram-ключей → ChunkedMissingKey по Deepgram-пулу"
    - "Сообщение ChunkedMissingKey динамично: 'API-ключ Deepgram' при Deepgram, 'API-ключ Groq' при Groq"
  artifacts:
    - path: "lib/core/providers/service_providers.dart"
      provides: "deepgramKeyPoolProvider (override-only)"
      contains: "deepgramKeyPoolProvider"
    - path: "lib/main.dart"
      provides: "bootstrap двух пулов"
      contains: "deepgramKeyPoolProvider.overrideWithValue"
    - path: "lib/ui/screens/processing_screen.dart"
      provides: "выбор pool+provider по провайдеру модели"
      contains: "TranscriptionProviderId.deepgram"
  key_links:
    - from: "lib/main.dart"
      to: "deepgramApiKeyRepoProvider"
      via: "bootstrap.read(...).listKeys()"
      pattern: "deepgramApiKeyRepoProvider"
    - from: "lib/ui/screens/processing_screen.dart"
      to: "DeepgramProvider"
      via: "выбор по options.model.provider"
      pattern: "DeepgramProvider\\(\\)"
    - from: "lib/ui/screens/processing_screen.dart"
      to: "deepgramKeyPoolProvider"
      via: "ref.read"
      pattern: "ref\\.read\\(deepgramKeyPoolProvider\\)"
---

<objective>
Завести второй KeyPool в DI и проводку выбора `provider+pool` по `options.model.provider`:
`deepgramKeyPoolProvider`, bootstrap двух пулов в `main`, динамический выбор провайдера
в `ProcessingScreen` + динамическое сообщение `ChunkedMissingKey`. После этого Nova-3
работает end-to-end через общий чанкинг/сборку таймкодов.

Purpose: финальная связка — соединяет DeepgramProvider (10-01) и раздельное хранилище
(10-02) в работающий поток обработки.
Output: рефактор `service_providers.dart`, `main.dart`, `processing_screen.dart`;
расширенные тесты smoke (R-DG-08) и ChunkedMissingKey (R-DG-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-deepgram-provider/10-RESEARCH.md
@.planning/phases/10-deepgram-provider/10-PATTERNS.md
@.planning/phases/10-deepgram-provider/10-01-SUMMARY.md
@.planning/phases/10-deepgram-provider/10-02-SUMMARY.md

<interfaces>
<!-- Контракты из 10-01 и 10-02, которые потребляет этот план. -->

From lib/features/transcription/deepgram_provider.dart (создан в 10-01):
```dart
class DeepgramProvider implements TranscriptionProvider {
  DeepgramProvider({http.Client Function()? clientFactory});
}
```

From lib/core/providers/repository_providers.dart (10-02):
```dart
final deepgramApiKeyRepoProvider = Provider<ApiKeyRepository>(...);
```

From lib/core/providers/service_providers.dart (текущее — Groq-only):
```dart
final groqKeyPoolProvider = Provider<KeyPool>((ref) {
  throw UnimplementedError('groqKeyPoolProvider must be overridden in ProviderScope');
});
final transcriptionProviderProvider = Provider<TranscriptionProvider>((ref) => GroqProvider());
```

From lib/ui/screens/processing_screen.dart (точка изменения, lines 125-129):
```dart
_chunkedController = ChunkedTranscriptionController(
  pool: ref.read(groqKeyPoolProvider),
  apiService: ref.read(transcriptionProviderProvider),
  chunkingService: ref.read(audioChunkingServiceProvider),
);
```

From lib/features/transcription/chunked_transcription_controller.dart:
```dart
// if (_pool.allKeys.isEmpty) → state = ChunkedMissingKey(); провайдеро-независим
ChunkedTranscriptionController({required KeyPool pool, required TranscriptionProvider apiService, ...});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deepgramKeyPoolProvider + bootstrap двух пулов</name>
  <files>lib/core/providers/service_providers.dart, lib/main.dart</files>
  <read_first>
    - lib/core/providers/service_providers.dart — `groqKeyPoolProvider` (lines 12-14), `transcriptionProviderProvider` (lines 19-21)
    - lib/main.dart — текущий single-pool bootstrap (lines 14-33)
    - lib/core/providers/repository_providers.dart — `deepgramApiKeyRepoProvider` (создан в 10-02)
    - .planning/phases/10-deepgram-provider/10-PATTERNS.md (lines 223-257) — целевые провайдер и bootstrap
  </read_first>
  <action>
    В `service_providers.dart` добавить `deepgramKeyPoolProvider = Provider<KeyPool>((ref) { throw UnimplementedError('deepgramKeyPoolProvider must be overridden in ProviderScope'); })` (зеркало groqKeyPoolProvider).
    ПЕРЕД удалением `transcriptionProviderProvider` — аудит потребителей (Pitfall 7): грепнуть `grep -rn "transcriptionProviderProvider" lib/ test/` и убедиться, что единственный потребитель — `lib/ui/screens/processing_screen.dart`. Этот потребитель переключается на динамический выбор провайдера в Task 2. Если греп находит других потребителей, кроме processing_screen — НЕ удалять, вернуть на доработку плана. После подтверждения, что единственная ссылка — processing_screen (которая переключается в Task 2), удалить `transcriptionProviderProvider` (провайдер теперь выбирается динамически в ProcessingScreen). Если останется неиспользуемый импорт `transcription_provider.dart`/`groq_api_service.dart` — убрать.
    В `main.dart`: после чтения `groqRawKeys` добавить `final deepgramRawKeys = await bootstrap.read(deepgramApiKeyRepoProvider).listKeys();` (до `bootstrap.dispose()`). Создать `final deepgramKeyPool = KeyPool(initialKeys: deepgramRawKeys.map((k) => k.raw).toList());`. В `overrides` добавить `deepgramKeyPoolProvider.overrideWithValue(deepgramKeyPool)`. Добавить импорт `service_providers.dart` (уже есть). Комментарии на русском.
  </action>
  <verify>
    <automated>grep -q "deepgramKeyPoolProvider" lib/core/providers/service_providers.dart && grep -q "deepgramKeyPoolProvider.overrideWithValue" lib/main.dart && grep -q "deepgramApiKeyRepoProvider" lib/main.dart && [ "$(grep -rn "transcriptionProviderProvider" lib/ test/ | wc -l)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `service_providers.dart` содержит `deepgramKeyPoolProvider` и НЕ содержит `transcriptionProviderProvider`
    - аудит потребителей выполнен: `grep -rn "transcriptionProviderProvider" lib/ test/` ДО удаления вернул только processing_screen; ПОСЛЕ удаления и переключения processing_screen (Task 2) — 0 совпадений
    - `main.dart` читает `deepgramApiKeyRepoProvider.listKeys()` и создаёт второй KeyPool
    - `main.dart` overrides содержит и `groqKeyPoolProvider.overrideWithValue` и `deepgramKeyPoolProvider.overrideWithValue`
    - `/opt/flutter/bin/flutter analyze lib/` без ошибок — нет висячих ссылок на удалённый `transcriptionProviderProvider` (analyze в acceptance как подтверждение отсутствия ссылок)
  </acceptance_criteria>
  <done>Второй пул заведён в DI; main bootstrap создаёт оба пула; transcriptionProviderProvider удалён после аудита единственного потребителя (processing_screen, переключается в Task 2); grep подтверждает 0 оставшихся ссылок.</done>
</task>

<task type="auto">
  <name>Task 2: Выбор pool+provider в ProcessingScreen + динамический ChunkedMissingKey</name>
  <files>lib/ui/screens/processing_screen.dart</files>
  <read_first>
    - lib/ui/screens/processing_screen.dart — импорты (lines 1-26), `_startProcessing` (lines 103-134), ветка `ChunkedMissingKey()` (lines 410-424), объявление state-класса (lines 35-40)
    - .planning/phases/10-deepgram-provider/10-PATTERNS.md (lines 261-301) — целевой выбор pool+provider и динамическое сообщение
  </read_first>
  <action>
    Добавить импорты `../../features/transcription/deepgram_provider.dart` и `../../features/transcription/groq_api_service.dart` (для DeepgramProvider/GroqProvider; `transcription_options.dart` уже импортирован — даёт TranscriptionProviderId).
    Добавить в `_ProcessingScreenState` поле `bool _isDeepgram = false;`. В `_startProcessing` перед созданием контроллера вычислить `_isDeepgram = _transcriptionOptions.model.provider == TranscriptionProviderId.deepgram;`. Заменить создание `ChunkedTranscriptionController`: `pool:` — `_isDeepgram ? ref.read(deepgramKeyPoolProvider) : ref.read(groqKeyPoolProvider)`; `apiService:` — `_isDeepgram ? DeepgramProvider() : GroqProvider()`; `chunkingService:` без изменений. Это переключает единственного потребителя удалённого `transcriptionProviderProvider`.
    В ветке `ChunkedMissingKey()` заменить хардкод-строку на `_isDeepgram ? 'Добавьте API-ключ Deepgram для начала транскрибации' : 'Добавьте API-ключ Groq для начала транскрибации'` (`_isDeepgram` доступен как поле state). Комментарии на русском.
  </action>
  <verify>
    <automated>grep -q "_transcriptionOptions.model.provider == TranscriptionProviderId.deepgram" lib/ui/screens/processing_screen.dart && grep -q "DeepgramProvider()" lib/ui/screens/processing_screen.dart && grep -q "ref.read(deepgramKeyPoolProvider)" lib/ui/screens/processing_screen.dart && grep -q "API-ключ Deepgram" lib/ui/screens/processing_screen.dart && ! grep -q "transcriptionProviderProvider" lib/ui/screens/processing_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `processing_screen.dart` импортирует `deepgram_provider.dart` и `groq_api_service.dart`
    - `_startProcessing` выбирает `deepgramKeyPoolProvider`+`DeepgramProvider()` при `TranscriptionProviderId.deepgram`, иначе `groqKeyPoolProvider`+`GroqProvider()`
    - `processing_screen.dart` больше НЕ ссылается на `transcriptionProviderProvider` (grep даёт 0)
    - сообщение ChunkedMissingKey содержит обе ветки ('API-ключ Deepgram' и 'API-ключ Groq')
    - `/opt/flutter/bin/flutter analyze lib/ui/screens/processing_screen.dart` без ошибок (нет ссылок на удалённый transcriptionProviderProvider)
  </acceptance_criteria>
  <done>ProcessingScreen маршрутизирует pool+provider по модели; ссылка на удалённый transcriptionProviderProvider устранена; сообщение об отсутствии ключа динамично.</done>
</task>

<task type="auto">
  <name>Task 3: Тесты R-DG-06 (ChunkedMissingKey) + R-DG-08 (smoke override)</name>
  <files>test/features/transcription/chunked_transcription_controller_test.dart, test/widget/home_screen_smoke_test.dart</files>
  <read_first>
    - test/features/transcription/chunked_transcription_controller_test.dart — существующий паттерн пустого пула → ChunkedMissingKey
    - test/helpers/transcription_mocks.dart (lines 29-72) — MockTranscriptionProvider(providerId: TranscriptionProviderId.deepgram)
    - test/widget/home_screen_smoke_test.dart — текущие overrides ProviderScope
    - .planning/phases/10-deepgram-provider/10-PATTERNS.md (lines 386-388) — ChunkedMissingKey-проверка по пулу
  </read_first>
  <action>
    В `chunked_transcription_controller_test.dart` добавить тест R-DG-06: контроллер с пустым Deepgram-пулом (`KeyPool(initialKeys: const [])`) + `MockTranscriptionProvider(..., providerId: TranscriptionProviderId.deepgram)` + options с `TranscriptionModel.nova3` → после `start()` состояние `ChunkedMissingKey` (`isA<ChunkedMissingKey>()`). Использовать существующие FakeChunkFile/хелперы из transcription_mocks.dart.
    В `home_screen_smoke_test.dart` расширить overrides ProviderScope: добавить `deepgramKeyPoolProvider.overrideWithValue(KeyPool(initialKeys: const []))` рядом с существующим groq-override (R-DG-08 — провайдер переопределяется, приложение стартует без UnimplementedError). Импортировать service_providers/key_pool при необходимости. Комментарии на русском.
  </action>
  <verify>
    <automated>/opt/flutter/bin/flutter test test/features/transcription/chunked_transcription_controller_test.dart test/widget/home_screen_smoke_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - тест ChunkedMissingKey использует `TranscriptionProviderId.deepgram` и `TranscriptionModel.nova3`, ожидает `isA<ChunkedMissingKey>()`
    - `home_screen_smoke_test.dart` содержит `deepgramKeyPoolProvider.overrideWithValue`
    - `flutter test test/features/transcription/chunked_transcription_controller_test.dart test/widget/home_screen_smoke_test.dart` exits 0
  </acceptance_criteria>
  <done>R-DG-06 и R-DG-08 покрыты зелёными тестами.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → DI (KeyPool) | Выбор пула ключей по модели; ключи не пересекают UI-границу как текст |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | ProcessingScreen pool selection | mitigate | Выбор `deepgramKeyPoolProvider` vs `groqKeyPoolProvider` по `options.model.provider` — Deepgram-обработка никогда не читает Groq-пул и наоборот. Тест R-DG-06 проверяет проверку по нужному пулу. |
| T-10-01 | Information Disclosure | ProcessingScreen | mitigate | Сырые ключи не попадают в UI-сообщения; ChunkedMissingKey показывает только имя провайдера, не ключ. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Новых пакетов нет. |
</threat_model>

<verification>
- `flutter test test/features/transcription/chunked_transcription_controller_test.dart test/widget/home_screen_smoke_test.dart` зелёный (ctx_execute).
- `flutter analyze lib/` без ошибок — нет висячих ссылок на transcriptionProviderProvider.
- grep подтверждает маршрутизацию pool+provider и оба override в main, а также 0 ссылок на удалённый transcriptionProviderProvider.
</verification>

<success_criteria>
- Второй пул в DI; main создаёт оба пула; ProcessingScreen выбирает provider+pool по модели.
- ChunkedMissingKey динамичен; Nova-3 без DG-ключей → ChunkedMissingKey по Deepgram-пулу.
- R-DG-06, R-DG-08 удовлетворены.
</success_criteria>

<output>
Create `.planning/phases/10-deepgram-provider/10-03-SUMMARY.md` when done
</output>
