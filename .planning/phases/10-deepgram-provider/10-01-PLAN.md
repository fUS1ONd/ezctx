---
phase: 10-deepgram-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/constants/app_constants.dart
  - lib/features/transcription/deepgram_provider.dart
  - test/unit/deepgram_provider_test.dart
  - test/fixtures/deepgram_nova3_response.json
  - test/fixtures/deepgram_nova3_response_empty.json
autonomous: true
requirements: [R-DG-01, R-DG-02, R-DG-03, R-DG-04, R-DG-05]
must_haves:
  truths:
    - "DeepgramProvider строит raw-bytes POST на https://api.deepgram.com/v1/listen с model=nova-3&smart_format=true&paragraphs=true"
    - "Запрос несёт заголовок Authorization: Token <key> (не Bearer) и Content-Type: audio/ogg"
    - "API-ключ присутствует только в заголовке Authorization, никогда в URL или в тексте исключений"
    - "Парсинг paragraphs.paragraphs[].sentences[] даёт список TranscriptionSegment{start,end,text}"
    - "При пустых paragraphs срабатывает fallback на words[], затем на плоский transcript"
    - "401→AuthException, 402→KeyExhaustedException, 429→RateLimitException, 504/5xx→NetworkException"
    - "concurrencyFor(0)==0 и concurrencyFor(n>0)==5"
  artifacts:
    - path: "lib/features/transcription/deepgram_provider.dart"
      provides: "DeepgramProvider — реализация TranscriptionProvider для nova-3"
      contains: "class DeepgramProvider implements TranscriptionProvider"
      min_lines: 120
    - path: "test/unit/deepgram_provider_test.dart"
      provides: "Юнит-тесты запроса/парсинга/маппинга ошибок/конкурентности"
      min_lines: 120
    - path: "test/fixtures/deepgram_nova3_response.json"
      provides: "Фикстура с paragraphs/sentences/words"
    - path: "test/fixtures/deepgram_nova3_response_empty.json"
      provides: "Фикстура пустого ответа (тишина)"
    - path: "lib/core/constants/app_constants.dart"
      provides: "deepgramApiUrl + storageKeyDeepgramApiKeys"
      contains: "deepgramApiUrl"
  key_links:
    - from: "lib/features/transcription/deepgram_provider.dart"
      to: "AppConstants.deepgramApiUrl"
      via: "Uri.parse(AppConstants.deepgramApiUrl)"
      pattern: "AppConstants\\.deepgramApiUrl"
    - from: "lib/features/transcription/deepgram_provider.dart"
      to: "TranscriptionProvider"
      via: "implements"
      pattern: "implements TranscriptionProvider"
---

<objective>
Реализовать `DeepgramProvider` (nova-3) — вторую реализацию `TranscriptionProvider`:
raw-bytes POST к Deepgram, парсинг `paragraphs`/`words` в общий `TranscriptionResult`,
маппинг HTTP-кодов в нормализованные исключения, политика конкурентности `n>0?5:0`.

Тесты и JSON-фикстуры пишутся ПЕРЕД реализацией провайдера (Wave 0 ordering из
10-VALIDATION.md): Task 1 создаёт фикстуры + тест-файл (RED), Task 3 — реализацию (GREEN).

Purpose: ядро фазы 10 — без `DeepgramProvider` Nova-3 не работает.
Output: `deepgram_provider.dart`, тест-файл, 2 фикстуры, 2 константы в `app_constants.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-deepgram-provider/10-SPEC.md
@.planning/phases/10-deepgram-provider/10-RESEARCH.md
@.planning/phases/10-deepgram-provider/10-PATTERNS.md
@.planning/phases/10-deepgram-provider/10-VALIDATION.md

<interfaces>
<!-- Контракты, которые реализует/использует DeepgramProvider. Извлечено из кодбейза. -->
<!-- Исполнитель использует эти сигнатуры напрямую — без повторного исследования. -->

From lib/features/transcription/transcription_provider.dart:
```dart
abstract interface class TranscriptionProvider {
  Future<TranscriptionResult> transcribeChunk({
    required List<int> bytes,
    required String filename,
    required String apiKey,
    TranscriptionOptions options,
  });
  int concurrencyFor(int aliveKeyCount);
  TranscriptionProviderId get id;
}
```

From lib/features/transcription/transcription_result.dart:
```dart
class TranscriptionSegment {
  final double start; final double end; final String text;
  const TranscriptionSegment({required this.start, required this.end, required this.text});
}
class TranscriptionResult {
  const TranscriptionResult({
    required this.text, String? plainText, required this.language,
    required this.duration, required this.words, this.segments = const [],
  });
  const TranscriptionResult.empty();   // text='', segments=const []
}
```

From lib/features/transcription/transcription_options.dart:
```dart
enum TranscriptionProviderId { groq, deepgram }
// TranscriptionModel.nova3.apiValue == 'nova-3'
// TranscriptionLanguage.auto.isoCode == ''; .ru.isoCode == 'ru'; и т.д.
```

From lib/core/error/app_exception.dart:
```dart
const AuthException(String);              // 401
const KeyExhaustedException([String]);    // 402
const RateLimitException(String, {int retryAfterSeconds = 60}); // 429
const NetworkException(String);           // 504 / 5xx / socket / timeout
const InternalException(String);          // ошибка парсинга
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — фикстуры + тест-файл DeepgramProvider (RED)</name>
  <files>test/fixtures/deepgram_nova3_response.json, test/fixtures/deepgram_nova3_response_empty.json, test/unit/deepgram_provider_test.dart</files>
  <read_first>
    - test/unit/groq_transcribe_chunk_test.dart — паттерн MockClient, structure `expectLater(... throwsA(isA<...>()))`, utf8-кодирование тела (кириллица)
    - test/helpers/transcription_mocks.dart (lines 29-72) — MockTranscriptionProvider, providerId
    - .planning/phases/10-deepgram-provider/10-RESEARCH.md (lines 346-371) — образец JSON структуры paragraphs/sentences/words
    - .planning/phases/10-deepgram-provider/10-PATTERNS.md (lines 305-345) — хелперы _jsonResponse / _providerWith, capturing-паттерн
  </read_first>
  <action>
    Создать `test/fixtures/deepgram_nova3_response.json` со структурой `results.channels[0].alternatives[0]`, содержащей: `transcript` (плоский текст, кириллица), `words[]` (минимум 2 элемента с `word/start/end`), `paragraphs.paragraphs[]` (минимум 1 параграф с `sentences[]` из 2 предложений, каждое `{text,start,end}`, и полями `num_words/start/end`). На уровне `channels[0]` добавить `detected_language: "ru"`. Числа start/end — 0-based от начала чанка (первое предложение start ≈ 0.33).
    Создать `test/fixtures/deepgram_nova3_response_empty.json` — `channels[0].alternatives[0]` с пустым `transcript: ""`, пустыми `words: []` и `paragraphs.paragraphs: []` (тишина — Open Question A2).
    Создать `test/unit/deepgram_provider_test.dart`, импортируя `package:http/testing.dart` (MockClient — raw-body НЕ multipart, `_MultipartCapturingClient` НЕ нужен). Хелперы: `_jsonResponse(Map data, int code)` через `http.Response.bytes(utf8.encode(jsonEncode(data)), code, headers: {'content-type':'application/json; charset=utf-8'})`; `_providerWith(client) => DeepgramProvider(clientFactory: () => client)`. Тест-кейсы (10): (1) paragraphs→segments — segments.length и start первого ≈ значение из фикстуры; (2) fallback words→segments при пустых paragraphs; (3) fallback plain text при пустых words; (4) URL-параметры: `detect_language=true` при `TranscriptionLanguage.auto`, `language=ru` при `.ru`, плюс `model=nova-3`, `smart_format=true`, `paragraphs=true` — захватывать `request.url.queryParameters`; (5) заголовки: `request.headers['authorization']=='Token <key>'`, `request.headers['content-type']` начинается с `audio/ogg`; (6) 401→AuthException; (7) 402→KeyExhaustedException; (8) 429→RateLimitException; (9) 504→NetworkException; (10) concurrencyFor: 0→0, 1→5, 3→5. Тело раздавать через `MockClient((req) async { captured = req; return _jsonResponse(...); })`. Фикстуры читать через `File('test/fixtures/...').readAsStringSync()` → `jsonDecode`.
    Файл будет RED (DeepgramProvider ещё не существует) — это ожидаемое состояние Wave 0.
  </action>
  <verify>
    <automated>ls test/fixtures/deepgram_nova3_response.json test/fixtures/deepgram_nova3_response_empty.json test/unit/deepgram_provider_test.dart && /opt/flutter/bin/dart format --output=none --set-exit-if-changed test/unit/deepgram_provider_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/deepgram_provider_test.dart` содержит `import 'package:http/testing.dart'` и НЕ содержит `MultipartRequest`
    - тест проверяет `'Token '` в заголовке Authorization и `audio/ogg` в Content-Type
    - тест проверяет `queryParameters` содержит `model=nova-3`, `smart_format=true`, `paragraphs=true`
    - тест содержит кейсы на `isA<AuthException>()`, `isA<KeyExhaustedException>()`, `isA<RateLimitException>()`, `isA<NetworkException>()`
    - оба JSON-файла валидны (`jsonDecode` не бросает); `deepgram_nova3_response.json` содержит ключ `paragraphs` и непустой `sentences`
  </acceptance_criteria>
  <done>Фикстуры и тест-файл созданы; тест компилируется как RED-набор (импортирует ещё несуществующий DeepgramProvider). После того как Wave 0 закрыт (фикстуры + тест-файл существуют и `dart format` чист), обновить frontmatter `10-VALIDATION.md`: `wave_0_complete: true`.</done>
</task>

<task type="auto">
  <name>Task 2: Константы Deepgram в app_constants.dart</name>
  <files>lib/core/constants/app_constants.dart</files>
  <read_first>
    - lib/core/constants/app_constants.dart — текущее состояние; Groq-блок (lines 6-15), storage-keys (lines 44-47)
  </read_first>
  <action>
    Добавить рядом с Groq-блоком: `static const String deepgramApiUrl = 'https://api.deepgram.com/v1/listen';`.
    Добавить в блок Secure storage keys: `static const String storageKeyDeepgramApiKeys = 'deepgram_api_keys_v1';`.
    Комментарии на русском. Существующие константы не трогать.
  </action>
  <verify>
    <automated>grep -q "deepgramApiUrl = 'https://api.deepgram.com/v1/listen'" lib/core/constants/app_constants.dart && grep -q "storageKeyDeepgramApiKeys = 'deepgram_api_keys_v1'" lib/core/constants/app_constants.dart</automated>
  </verify>
  <acceptance_criteria>
    - `app_constants.dart` содержит `deepgramApiUrl = 'https://api.deepgram.com/v1/listen'`
    - `app_constants.dart` содержит `storageKeyDeepgramApiKeys = 'deepgram_api_keys_v1'`
    - `storageKeyApiKeys = 'groq_api_keys_v1'` остался без изменений (namespace Groq не сломан — T-10-03)
  </acceptance_criteria>
  <done>Две константы добавлены; Groq-namespace неизменен.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Реализация DeepgramProvider (GREEN)</name>
  <files>lib/features/transcription/deepgram_provider.dart</files>
  <behavior>
    - Запрос: POST AppConstants.deepgramApiUrl с queryParameters {model: options.model.apiValue, smart_format:'true', paragraphs:'true', + detect_language:'true' при auto / language:isoCode иначе}; headers {Authorization:'Token $apiKey', Content-Type:'audio/ogg'}; body Uint8List.fromList(bytes); timeout 5 минут
    - 200: парсинг paragraphs.paragraphs[].sentences[] → TranscriptionSegment; fallback words[]; fallback плоский transcript; duration = end последнего сегмента/слова, иначе 0.0
    - detected_language с уровня channel; fallback на options.language.isoCode если пусто (Open Question A1)
    - пустые channels/alternatives → TranscriptionResult.empty() (Open Question A2)
    - 401→AuthException, 402→KeyExhaustedException, 429→RateLimitException(retry-after или 60), 504/5xx→NetworkException; ошибка jsonDecode→InternalException
    - concurrencyFor(n) => n>0?5:0; id => TranscriptionProviderId.deepgram
  </behavior>
  <read_first>
    - lib/features/transcription/groq_api_service.dart — аналог: конструктор+clientFactory (lines 97-100), каскад catch+finally (lines 177-187), safeBody-обрезка тела до 200 символов (lines 171-176), константы _authErrorMessage/_networkErrorMessage (lines 102-105)
    - lib/features/transcription/transcription_result.dart — конструкторы TranscriptionResult / TranscriptionResult.empty / TranscriptionSegment
    - .planning/phases/10-deepgram-provider/10-RESEARCH.md (lines 397-493) — точная форма запроса и парсера _parseResponse
    - .planning/phases/10-deepgram-provider/10-PATTERNS.md (lines 31-142) — pattern assignments, pitfalls 1-6
  </read_first>
  <action>
    Создать `lib/features/transcription/deepgram_provider.dart`, `class DeepgramProvider implements TranscriptionProvider`. Импорты: `dart:async`, `dart:convert`, `dart:io`, `dart:typed_data`, `package:http/http.dart as http`, плюс app_constants/app_exception/transcription_options/transcription_provider/transcription_result. Конструктор+`_clientFactory` копировать дословно из GroqProvider.
    `transcribeChunk`: построить `Uri.parse(AppConstants.deepgramApiUrl).replace(queryParameters: {...})` — `model: options.model.apiValue`, `smart_format:'true'`, `paragraphs:'true'`, при `options.language == TranscriptionLanguage.auto` → `detect_language:'true'`, иначе `language: options.language.isoCode`. `client.post(uri, headers: {'Authorization':'Token $apiKey', 'Content-Type':'audio/ogg'}, body: Uint8List.fromList(bytes)).timeout(const Duration(minutes:5))`. Ключ ТОЛЬКО в заголовке — не в URL, не в логах (T-10-01).
    Маппинг кодов: 200 → `_parseResponse(jsonDecode(body), options)` (на исключении парсинга — `InternalException('Не удалось разобрать ответ Deepgram')`); 401 → `AuthException(_authErrorMessage)` (текст про console.deepgram.com); 402 → `KeyExhaustedException()`; 429 → `RateLimitException('Превышен лимит запросов Deepgram', retryAfterSeconds: int.tryParse(headers['retry-after']?.trim() ?? '') ?? 60)` (clamp >0); прочее → `NetworkException('Deepgram ${statusCode}: $safeBody')` где safeBody = тело обрезано до 200 символов (T-10-02 — тело ошибки не утекает целиком). Каскад `on SocketException / TimeoutException → NetworkException(_networkErrorMessage)`, `on AppException rethrow`, `catch (_) → NetworkException`, `finally client.close()`.
    `_parseResponse(Map json, TranscriptionOptions options)`: `channels = results['channels'] as List?`; если пусто → `TranscriptionResult.empty()`. `channel = channels.first`; `alternatives` пустой → empty(). `alt = alternatives.first`; `transcript = alt['transcript'] as String? ?? ''`; `detectedLanguage = channel['detected_language'] as String? ?? options.language.isoCode`. Цепочка: (1) `alt['paragraphs'] as Map?` → `['paragraphs'] as List?` непуст → по каждому параграфу `sentences[]` → TranscriptionSegment{start,end,text} (Pitfall 2 — paragraphs это Map, потом List); (2) иначе `alt['words'] as List?` непуст → каждое слово как сегмент (text=word); (3) иначе segments пустой. duration = end последнего сегмента/слова, иначе 0.0 (Pitfall 4). start/end — 0-based от чанка, offset НЕ прибавлять (Pitfall 5). Вернуть `TranscriptionResult(text: transcript, plainText: transcript, language: detectedLanguage, duration: duration, words: const [], segments: segments)`.
    `concurrencyFor(int aliveKeyCount) => aliveKeyCount > 0 ? 5 : 0`. `id => TranscriptionProviderId.deepgram`. Все комментарии на русском.
  </action>
  <verify>
    <automated>/opt/flutter/bin/flutter test test/unit/deepgram_provider_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `deepgram_provider.dart` содержит `'Authorization': 'Token '` (grep `"'Authorization': 'Token "`)
    - `deepgram_provider.dart` содержит `'Content-Type': 'audio/ogg'`
    - `deepgram_provider.dart` содержит `Uint8List.fromList(bytes)` (raw body, не MultipartRequest — grep `MultipartRequest` даёт 0)
    - `deepgram_provider.dart` содержит `implements TranscriptionProvider` и `TranscriptionProviderId.deepgram`
    - `concurrencyFor` содержит `aliveKeyCount > 0 ? 5 : 0`
    - `flutter test test/unit/deepgram_provider_test.dart` exits 0 (все 10 кейсов зелёные)
    - API-ключ не встречается в строках NetworkException/safeBody (тело обрезано до 200, ключ в URL отсутствует)
  </acceptance_criteria>
  <done>Все юнит-тесты Task 1 зелёные; провайдер строит raw-запрос с Token-заголовком и корректно маппит ответ/ошибки.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Deepgram REST API | API-ключ пересекает границу; ответ (включая тела ошибок) — недоверенный вход |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | DeepgramProvider.transcribeChunk | mitigate | Ключ только в заголовке `Authorization: Token`; никогда в `queryParameters`/URL и не логируется. Acceptance проверяет отсутствие ключа в URL. |
| T-10-02 | Information Disclosure | DeepgramProvider error mapping | mitigate | Тело HTTP-ошибки обрезается до 200 символов в `NetworkException` (safeBody), чтобы потенциальные секреты/ключ не утекли в текст исключения/UI. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Новых пакетов нет (RESEARCH §Package Legitimacy Audit) — установки отсутствуют. |
</threat_model>

<verification>
- `flutter test test/unit/deepgram_provider_test.dart` зелёный (через ctx_execute).
- `flutter analyze lib/features/transcription/deepgram_provider.dart` без ошибок.
- grep подтверждает `Token`-заголовок и отсутствие `MultipartRequest`.
</verification>

<success_criteria>
- DeepgramProvider реализует TranscriptionProvider, строит raw-bytes POST nova-3.
- Парсинг paragraphs/words/plain + маппинг 401/402/429/504 покрыты зелёными тестами.
- R-DG-01..05 удовлетворены.
</success_criteria>

<output>
Create `.planning/phases/10-deepgram-provider/10-01-SUMMARY.md` when done
</output>
