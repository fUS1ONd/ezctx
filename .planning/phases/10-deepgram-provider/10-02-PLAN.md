---
phase: 10-deepgram-provider
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/storage/secure_storage_service.dart
  - lib/features/settings/api_key_repository.dart
  - lib/core/providers/storage_providers.dart
  - lib/core/providers/repository_providers.dart
autonomous: true
requirements: [R-DG-07]
must_haves:
  truths:
    - "SecureStorageServiceImpl принимает storageKey через конструктор с default = AppConstants.storageKeyApiKeys"
    - "Groq-ключи продолжают храниться под groq_api_keys_v1 (вызов без аргументов не меняет поведение)"
    - "deepgramSecureStorageProvider создаёт хранилище под namespace deepgram_api_keys_v1"
    - "deepgramApiKeyRepoProvider и deepgramApiKeysProvider дают доступ к Deepgram-ключам"
    - "Namespace Groq и Deepgram не пересекаются (раздельные ключи хранилища)"
  artifacts:
    - path: "lib/core/storage/secure_storage_service.dart"
      provides: "Параметризованный по namespace SecureStorageServiceImpl"
      contains: "String storageKey = AppConstants.storageKeyApiKeys"
    - path: "lib/core/providers/storage_providers.dart"
      provides: "deepgramSecureStorageProvider"
      contains: "deepgramSecureStorageProvider"
    - path: "lib/core/providers/repository_providers.dart"
      provides: "deepgramApiKeyRepoProvider + deepgramApiKeysProvider"
      contains: "deepgramApiKeyRepoProvider"
  key_links:
    - from: "lib/core/providers/storage_providers.dart"
      to: "AppConstants.storageKeyDeepgramApiKeys"
      via: "SecureStorageServiceImpl(storageKey: ...)"
      pattern: "storageKey: AppConstants\\.storageKeyDeepgramApiKeys"
    - from: "lib/core/providers/repository_providers.dart"
      to: "deepgramSecureStorageProvider"
      via: "ref.watch"
      pattern: "ref\\.watch\\(deepgramSecureStorageProvider\\)"
---

<objective>
Параметризовать хранилище ключей по namespace провайдера и завести Deepgram-двойники
DI-провайдеров хранилища/репозитория. После этого Groq и Deepgram хранят ключи
раздельно (`groq_api_keys_v1` / `deepgram_api_keys_v1`) без пересечения (T-10-03).

Purpose: фундамент раздельных пулов ключей — без него выбор провайдера на /processing
читал бы единый Groq-namespace.
Output: рефактор `SecureStorageServiceImpl`, новые `deepgramSecureStorageProvider`,
`deepgramApiKeyRepoProvider`, `deepgramApiKeysProvider`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-deepgram-provider/10-RESEARCH.md
@.planning/phases/10-deepgram-provider/10-PATTERNS.md

<interfaces>
<!-- Текущие DI-аналоги, которые зеркалятся для Deepgram. -->

From lib/core/providers/storage_providers.dart:
```dart
final secureStorageProvider = Provider<SecureStorageService>(
  (ref) => SecureStorageServiceImpl(),
);
```

From lib/core/providers/repository_providers.dart:
```dart
final apiKeyRepoProvider = Provider<ApiKeyRepository>(
  (ref) => ApiKeyRepository(ref.watch(secureStorageProvider)),
);
final apiKeysProvider = FutureProvider<List<ApiKeyView>>((ref) async {
  return ref.watch(apiKeyRepoProvider).listKeys();
});
```

From lib/core/storage/secure_storage_service.dart (текущий конструктор — static _storageKey):
```dart
SecureStorageServiceImpl({FlutterSecureStorage? storage})
  : _storage = storage ?? const FlutterSecureStorage(aOptions: AndroidOptions());
static const _storageKey = AppConstants.storageKeyApiKeys;  // ← рефактор в final
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Параметризовать SecureStorageServiceImpl по namespace</name>
  <files>lib/core/storage/secure_storage_service.dart, lib/features/settings/api_key_repository.dart</files>
  <read_first>
    - lib/core/storage/secure_storage_service.dart — текущий конструктор (lines 33-43), static _storageKey (line 43); методы listApiKeys/addApiKey/removeApiKey уже читают _storageKey как поле (lines 45-89)
    - lib/features/settings/api_key_repository.dart — `ApiKeyRepository(this._storage)`, stateless; комментарий «Repository для управления Groq API-ключами»
    - .planning/phases/10-deepgram-provider/10-PATTERNS.md (lines 146-177) — целевая форма конструктора, Вариант A (api_key_repository без изменений логики)
  </read_first>
  <action>
    В `SecureStorageServiceImpl` добавить именованный параметр конструктора `String storageKey = AppConstants.storageKeyApiKeys` (default → Groq, обратная совместимость). Заменить `static const _storageKey = AppConstants.storageKeyApiKeys;` на `final String _storageKey;` и инициализировать `_storageKey = storageKey` в списке инициализации (после `_storage = ...`). Методы `listApiKeys`/`addApiKey`/`removeApiKey`/`writeRawKey`/`readRawKey`/`deleteRawKey` НЕ трогать — они уже используют `_storageKey` как instance-поле.
    В `api_key_repository.dart` логику не менять (Вариант A — namespace идёт через storage). Только обобщить doc-комментарий «Repository для управления Groq API-ключами» → «Repository для управления API-ключами провайдера (namespace задаётся SecureStorageService)». Комментарии на русском.
  </action>
  <verify>
    <automated>grep -q "String storageKey = AppConstants.storageKeyApiKeys" lib/core/storage/secure_storage_service.dart && grep -q "final String _storageKey" lib/core/storage/secure_storage_service.dart && ! grep -q "static const _storageKey" lib/core/storage/secure_storage_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - `secure_storage_service.dart` содержит `String storageKey = AppConstants.storageKeyApiKeys` в конструкторе
    - `secure_storage_service.dart` содержит `final String _storageKey;` и НЕ содержит `static const _storageKey`
    - вызов `SecureStorageServiceImpl()` без аргументов по-прежнему использует `groq_api_keys_v1` (default не изменён — T-10-03)
    - `api_key_repository.dart` без изменений сигнатур (логика CRUD не тронута)
  </acceptance_criteria>
  <done>storageKey параметризован с Groq-default; static убран; Groq-поведение неизменно.</done>
</task>

<task type="auto">
  <name>Task 2: DI-двойники Deepgram (storage + repository providers)</name>
  <files>lib/core/providers/storage_providers.dart, lib/core/providers/repository_providers.dart</files>
  <read_first>
    - lib/core/providers/storage_providers.dart — `secureStorageProvider` (lines 5-7); импортов app_constants ещё нет
    - lib/core/providers/repository_providers.dart — `apiKeyRepoProvider` (lines 7-9), `apiKeysProvider` (lines 16-18)
    - .planning/phases/10-deepgram-provider/10-PATTERNS.md (lines 194-219) — целевые провайдеры-зеркала
  </read_first>
  <action>
    В `storage_providers.dart` добавить импорт `../constants/app_constants.dart` и провайдер `deepgramSecureStorageProvider = Provider<SecureStorageService>((ref) => SecureStorageServiceImpl(storageKey: AppConstants.storageKeyDeepgramApiKeys))`.
    В `repository_providers.dart` добавить `deepgramApiKeyRepoProvider = Provider<ApiKeyRepository>((ref) => ApiKeyRepository(ref.watch(deepgramSecureStorageProvider)))` и `deepgramApiKeysProvider = FutureProvider<List<ApiKeyView>>((ref) async => ref.watch(deepgramApiKeyRepoProvider).listKeys())`. Существующие Groq-провайдеры не трогать. Комментарии на русском.
  </action>
  <verify>
    <automated>grep -q "deepgramSecureStorageProvider" lib/core/providers/storage_providers.dart && grep -q "storageKey: AppConstants.storageKeyDeepgramApiKeys" lib/core/providers/storage_providers.dart && grep -q "deepgramApiKeyRepoProvider" lib/core/providers/repository_providers.dart && grep -q "deepgramApiKeysProvider" lib/core/providers/repository_providers.dart</automated>
  </verify>
  <acceptance_criteria>
    - `storage_providers.dart` импортирует `app_constants.dart` и определяет `deepgramSecureStorageProvider` с `storageKey: AppConstants.storageKeyDeepgramApiKeys`
    - `repository_providers.dart` определяет `deepgramApiKeyRepoProvider` (через `ref.watch(deepgramSecureStorageProvider)`) и `deepgramApiKeysProvider`
    - `flutter analyze lib/core/providers/` без ошибок
  </acceptance_criteria>
  <done>Deepgram-двойники storage/repository провайдеров заведены; namespace разделён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → flutter_secure_storage | Ключи двух провайдеров делят одно защищённое хранилище под разными namespace-ключами |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering / Information Disclosure | SecureStorageServiceImpl namespace | mitigate | Раздельные storageKey `groq_api_keys_v1` / `deepgram_api_keys_v1`; default конструктора = Groq для обратной совместимости. Acceptance проверяет неизменность Groq-default. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | Новых пакетов нет — установки отсутствуют. |
</threat_model>

<verification>
- `flutter analyze lib/core/storage/ lib/core/providers/` без ошибок (через ctx_execute).
- grep подтверждает Deepgram-провайдеры и неизменный Groq-default.
- Регрессия Groq-тестов хранилища проверяется в плане фазового гейта (10-04).
</verification>

<success_criteria>
- SecureStorageServiceImpl параметризован, Groq-namespace неизменен.
- deepgramSecureStorageProvider / deepgramApiKeyRepoProvider / deepgramApiKeysProvider заведены.
- R-DG-07 (регрессия namespace) подготовлено к проверке.
</success_criteria>

<output>
Create `.planning/phases/10-deepgram-provider/10-02-SUMMARY.md` when done
</output>
