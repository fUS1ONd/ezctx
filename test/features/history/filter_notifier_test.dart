// Wave 0 stub — каркас тестов FilterNotifier.
// GREEN-реализация в плане 02 (FilterNotifier с debounce).
import 'package:flutter_test/flutter_test.dart';

void main() {
  // SRCH-03: поиск обновляется с debounce ~250 мс, не блокирует UI.
  test(
    'SRCH-03: setSearch дебоунсит обновление состояния на 250 мс',
    skip: 'GREEN в плане 02',
    () async {},
  );
}
